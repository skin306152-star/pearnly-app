# REFACTOR-A5 · Prettier 忽略清单
# 构建产物 / 第三方 / 锁文件 / 运行期产物 —— 不格式化
node_modules/
static/dist/
# agent / IDE 配置目录 —— 非 app 代码 · 不受项目 prettier 管(否则权限/hook 配置改一下就红 lint)
.claude/
package-lock.json
*.min.js
*.min.css

# 运行期 / 本地样本 / 临时
_pkg/
_pkg_tmpstatic/
_deploy_nginx/
_gemini_key.local/
_bank_samples/
_test_reports/
.ocr_cache/
probes/
test-results/
playwright-report/
tests/e2e/.auth/

# ─────────────────────────────────────────────────────────────
# Stage C 拆解目标(REFACTOR_MASTER_PLAN.md 阶段 C)· 暂不格式化
# 理由:这些巨石本就要在阶段 C 拆成 50-100 个模块 · 现在 prettier
#       会产生 20 万+ 行丢弃式 diff 并毁掉 git blame · 待拆出模块时
#       新文件不在本忽略表内 · 自然纳入 prettier --check 强制。
# 注:.prettierignore 用 .gitignore 语法 · 行内注释无效 · 故说明独立成行。
# ─────────────────────────────────────────────────────────────
# REFACTOR-C1 · home.js 33k 行 IIFE → 拆 src/home/*.js
home.js
# REFACTOR-C1(2026-05-25)· 从 home.js verbatim 抽出的 4 语 i18n 数据(9772 行 · window.I18N)·
# 跟 home.js 同策略豁免:verbatim 抽家阶段不重排格式(prettier 会改 9772 行 + 毁 git blame)·
# 重复 key 去重 / 格式化留后续 i18n 专项;check_i18n.py 仍强制 4 语 key 完整性。
static/i18n-data.js
# REFACTOR-C2 · home.css → 拆 component CSS
home.css
# 拆出的 css 切片:verbatim 搬迁,必须与原 home.css 字节级一致以保证级联零变化 →
# 跟 home.css 同策略豁免 prettier(重排会破坏字节等价 → 视觉漂移风险)。
static/home-*.css
# POS 收银前台样式表:逐字移植概念稿(docs/pos 09 §H)· 同 home-*.css 策略 · 保紧凑(<500)
# 便于与概念稿源码侧拼对照;prettier 展开成每声明一行会到 1500+ 行并扰乱视觉核对。
static/pos/*.css
# REFACTOR-C3 · home.html → 组件化
home.html
# C 阶段相邻 · login.html 242KB · 未来模块化
login.html
# 设计参考件(导航 IA 基准 · 非 app 代码 · 永久豁免)
pearnly_nav_prototype_final.html
# 按钮/UI 一致性设计参考(buttons.html 视觉稿 + 审计清单 · 非 app 代码 · 永久豁免)
design-preview/
tests/visual/design/
# 对账中心 UI 落地参考原型(视觉/交互事实来源 · 非 app 代码 · 不被 import · 永久豁免)
design-reference/
