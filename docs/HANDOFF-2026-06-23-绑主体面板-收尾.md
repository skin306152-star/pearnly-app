# 交接 · 2026-06-23 「Express 绑主体面板」窗口收尾

> 给下一个窗口/接手人。只写**事实**与**待办**。承接上一窗口 `HANDOFF-2026-06-23-全检收口`(那份的 4 个任务**已全部被后续窗口做完**,见下「背景核实」)。
> 当前 prod = master `9336930e` · 线上前端版本 `v11850964` · companion `1.1.8` · 网站正常。

---

## 〇、背景核实(进窗口先看 · 治文档漂移)

进窗口时我核实了真实状态(只信脚本/git,不信文档手写):
- **`HANDOFF-2026-06-23-全检收口` 那份交接已过时**:它停在 prod `ff70eb09`,列的 4 个「未完成」**都已被后续窗口做完**——
  - `oauth_routes.py` 539→**213**(已拆)
  - `erp_push.py` 507→**410**(已拆)
  - lint-ui 三道闸 → **现全绿**(rc=0)
  - 体积闸 854 文件**全绿**、CI **全绿**
- 真正「上个窗口(Express 全自动)」留的待办在记忆 `[[express-full-auto-provision-design]]` 的「待做」段:④ 前端绑主体面板(后端已就位仅差 UI)→ S4/S5/S7。本窗口接的就是 **④**。

---

## 一、本窗口【已完成并上线】

### ④ Express 异常卡「绑定主体」面板(prod `9336930e` · v11850964)
**目标**:推送异常页里「方向判不出·主体没绑」的票(`direction_unknown`),给用户一个自助入口:选账套主体(自家公司)→ 一键绑定并重推。后端路由 `POST /api/erp/logs/{id}/express-bind-subject`(commit `1c00ee4a`)早已就位,本窗口**只补前端 UI**。

**实现**(全前端 + i18n,后端零改):
- `src/home/erp-exceptions.ts`:新增判定
  `isBind = cat === 'direction_unknown' && !/direction_not_enabled/.test(error_msg)`
  — 只对「绑主体能救」的票显面板;`direction_not_enabled`(方向判出但该方向没在向导开)**不显**(绑主体没用,得去向导开)。actHtml 把 `isAcct || isBind` 合并成一支(只差 t() 文案键),卡片渲染 `isBind → _erpExcBindPanel(it)`。
- `src/home/erp-exc-actions.ts`:新增 5 个函数 `_erpExcClients` / `_erpExcEnsureClients` / `_erpExcBindPanel` / `_erpExcBindSubject` / `_erpExcWireBind`。
- **刻意复用**待补科目卡那套:CSS 类 `erp-exc-acctfix*`(不新增 CSS)、开关句柄 `data-erpexc-acctfix`、取消句柄 `data-acctfix-cancel`、面板容器 `data-acctfix-panel`。只有提交是新的 `data-bindfix-submit`(`_erpExcWireBind` 单独接线)。
- 主体下拉数据走 `window.fetchWorkspaceClients()`(账套切换器的 canonical loader·带 workspace 作用域头 + 401 处理),**与异常 fetch 并发**取(`/simplify` 收口结果)。
- i18n:`static/i18n-data.js` 加 `erp-bind-*` **8 键 × 4 语**(open/subject/pick/submit/noclients/need-pick/ok/fail;cancel 复用 acctfix-cancel)。

**文件行数**:erp-exc-actions.ts **307** / erp-exceptions.ts **496**(均 <500 闸)。

**守门 + CI + E2E 全过**:
- 闸:prettier / 4696 单测 / imports / i18n-strict(4 语各 4823 键平衡·0 缺 0 余) / file_size / lint-ui 三道 / 棘轮(净增透明豁免) / build。
- CI:本窗口 4 次提交 master CI **全 success**。
- **真站 E2E**(pearnly.com · 真浏览器 · pearnly_e2e_3):**8 PASS · 0 FAIL** — 卡渲染、面板 `getComputedStyle` 真可见、下拉填充 3 个账套主体、提交真发 `POST express-bind-subject` 带 `workspace_client_id`。新 bundle(v11850964)上复跑仍 8 PASS。

**本窗口 4 次提交**:
- `72f47c0b` feat:绑主体面板 + erp-bind-* 8 键 4 语
- `c047f185` fix:补漏 add 的 `dist/home.html`(踩坑见下)
- `2d524a58` test:真站 E2E + seed 脚本
- `9336930e` refactor(/simplify):复用 fetchWorkspaceClients + 并发取数

**验证设施已入库**(可复现):
- `scripts/_seed_express_bind_e2e.py` — 给 pearnly_e2e_3 造一条 Express `direction_unknown` 异常(幂等·仅测试号)。在 prod 跑:`ssh root@66.42.49.213` → `cd /opt/mrpilot && set -a && . /opt/mrpilot/.env && set +a && PYTHONPATH=/opt/mrpilot venv/bin/python /tmp/<脚本>`。
- `scripts/_express_bind_e2e.cjs` — 真站 playwright(`node scripts/_express_bind_e2e.cjs`)。
- ⚠️ **prod 上 pearnly_e2e_3 现仍留着一个 Express 测试端点 + 一条 direction_unknown manual 日志**(seed 产物·只在测试号)。要清:删该账号 adapter='express' 的端点 + 其 push log。留着也无害(下个窗口复验绑主体卡直接能看到)。

---

## 二、本窗口【未完成 / 待做】(按记忆 `[[express-full-auto-provision-design]]` 优先级)

### S4 — 自建客户 ARMAS(小助手侧)【条件不具备·单窗口干不了】
镜像 S3 供应商 `ensure_supplier`:小助手按票上买方自建 ARMAS 客户(`ensure_customer` 接 verify+dup,helpers `find_suspected_customer_dup`/`verify_customer_row` 已存 `master_create`)。
**前置条件**(缺一不可,本窗口无法满足):① DATAT 账套空闲(Owner 不在用) ② Owner 真机(`D:\pearnly-companion` / `\\accserver\ACCOUNT\70EXP`) ③ 碰小助手发版(独立仓 `D:\pearnly-companion` 无 remote·要 Owner 协调,**别单独发**)。

### S5 — 科目保守版收口【可做】
多为 doc28 现有行为收口(落已有科目·不新建)。见 `docs/integrations/express-push/29-...md`。

### S7 — 自建套账/建账套【极重·研究先行】
事务所新客户建账套。先做 spike 证「可做对」再建。见 doc29 §3.6。

### /simplify defer 的两项(altitude·非阻塞·建议合并成一个后续)
1. **后端 `derive_bind_fix` 结构化提示**:现在前端用 `!/direction_not_enabled/.test(error_msg)` 正则再切分类(因为后端 `classify_push_exception` 把 `direction_unknown` + `direction_not_enabled` 合并成一类)。更干净的做法 = 仿 `account_fix` 在后端 `list_push_exceptions` 派生 `bind_fix`/`can_bind`,前端读结构化字段不解析裸串。**阻塞点**:`services/erp/push_log_queries.py` 在 **492/500** 行,加不下 → 须先拆该文件腾空间。顺带 `account_set`/`low_confidence` 也只靠字符串匹配,可一并归整。
2. **acctfix→通用 panel 句柄改名**:绑主体复用了 `data-erpexc-acctfix`/`data-acctfix-*`/`erp-exc-acctfix` 这套(机制确实通用·行为正确),但名字带 acctfix 语义,靠注释防误解。低价值改名 → **第三个消费者出现时再做**。

---

## 三、给接手人的坑提醒(本窗口踩的)
- **bump `?v=` 必须同时 `git add static/dist/home.html` + `static/dist/main.js`**:源 home.html 会被 build 编译进 `static/dist/home.html`(минified)。第一次只 add 了 main.js、漏 dist/home.html → CI「打包一致性闸」红。改前端源 → `npm run build` → `git add` **两个 dist 产物**一起提交。
- `static/i18n-data.js` / `home.html` 是 **CRLF**:加 i18n 键用字节级 Python(`io.open(...,newline='')`)保 `\r\n`,禁 sed/prettier;改完 `node --check static/i18n-data.js`。Python 在 Windows cp874 控制台 print 含 `·` 会崩(只是 print 崩,文件已写入)——别被 traceback 骗,核对文件实际内容。
- **只 `git add` 自己的文件**(共享工作树):本窗口 `AGENTS.md` / `docs/line-platform/00-...` 是别的窗口的未提交改动,**没碰**。
- E2E 账号自助:`pearnly_e2e_3` / `Pe@rnly-E2E-3p4`,`POST /api/login` 拿 token(脚本里有)。playwright 进站后**工作台门(workspace-gate / onboarding)会重渲拦截点击** → 注入持久 `#workspace-gate-root{display:none!important;pointer-events:none!important}` 样式杀门(`addInitScript` 里设 `pearnly_active_workspace_client_id` 也帮助)。集成页切 tab 用页面内 `.click()` 触发 document 监听(playwright `p.click` 在 tab 上偶发不生效)。
- prod 查 DB:`ssh root@66.42.49.213` → `/opt/mrpilot` → `set -a && . /opt/mrpilot/.env && set +a` 再跑 venv python(直跑没 DATABASE_URL)。
- gh CLI 路径(本机):`C:\Users\skin3\AppData\Local\Microsoft\WinGet\Packages\GitHub.cli_Microsoft.Winget.Source_8wekyb3d8bbwe\bin\gh.exe`(CLAUDE.md 写的 `C:\Program Files\GitHub CLI\gh.exe` 在本机不存在)。

---

## 四、当前各闸状态快照(prod HEAD `9336930e`)
- ✅ 全绿:4696 单测、prettier/black/ruff/eslint、ratchet、build 一致性、imports、i18n-strict、file_size、lint-ui 三道。
- CI 最近多次 run 全 success。
- (eslint 对 `static/admin/admin.js` 有「`_`/`_admCurTab` 未用」**警告**·非 error·lint job success·非本窗口引入·历史存量。)
