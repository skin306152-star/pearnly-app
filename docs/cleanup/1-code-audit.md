请在 docs/cleanup/ 目录下创建一个新文件 1-code-audit.md,内容我下面会贴给你。创建完成告诉我,不要做其他任何事。

文件内容:
---
我接下来要做一次代码清理重构,但在动手之前,我要你先做一份完整的"代码健康体检报告"。这一轮严禁修改任何代码,只读不写。

## 背景
项目积累了一些"屎山"(死代码、重复逻辑、过时实现、TODO 没清等),我想在不影响功能的前提下系统性清理。

## 第一步:扫描全项目,识别问题代码
按以下分类找出可疑代码,每一项都给出具体文件路径和行号:

1. 死代码 (Dead Code)
   - 定义但从未被调用的函数、类、方法
   - 永远不会被执行的分支(unreachable code)
   - 注释掉的大段代码
   - import 了但没使用的模块

2. 重复代码 (Duplication)
   - 几乎一样的函数/方法存在多份
   - 复制粘贴的代码块(超过 10 行的雷同)
   - 同样功能用了多种实现方式

3. 过时代码 (Obsolete)
   - 标注了 TODO/FIXME/HACK/XXX 的地方,列出所有并标注年龄
   - 标注了 @deprecated 或注释里写"旧版本""废弃"的代码
   - 引用了已经不存在的功能或老接口

4. 可疑代码 (Suspicious)
   - 空的 try/except(吞掉所有错误)
   - 写死的测试数据/魔法数字
   - 没有任何注释的复杂函数(> 50 行且无文档)
   - 命名极其混乱的变量/函数(a, b, tmp, data2)

5. 未使用的依赖
   - package.json / requirements.txt / pyproject.toml 里列出但代码中没用到的包

## 第二步:分级
对每一项标注风险等级:
- 🟢 安全清理:绝对没人在用,删了没影响
- 🟡 需要确认:看起来没在用,但可能有隐藏依赖
- 🔴 谨慎处理:涉及核心业务逻辑,删之前必须我确认

## 第三步:输出清单
保存为 docs/code-cleanup-audit.md,包含:
- 总览(共发现多少处问题,按等级分布)
- 详细清单(每一项的位置、类型、风险等级、建议处理方式)
- 不要做任何排序或优先级建议,我自己决定

## 严格规则
- ❌ 不要修改任何代码文件
- ❌ 不要删任何东西
- ❌ 不要"顺手"做任何重构
- ✅ 只读、分析、写文档
- ✅ 不确定的全部标 🟡 让我决定

完成后告诉我文档路径,等我审阅。
---