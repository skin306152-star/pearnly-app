# 交接 · 2026-06-23 「全检收口」窗口未完成任务

> 给下一个窗口/接手人。本文只写**事实**与**待办**,不含猜测;凡是"建议方案"都明确标注【需 Owner 拍板后再动】。
> 当前 prod = master `ff70eb09` · 线上前端版本 `v11850940` · 网站正常(/home 200、登录可用)。

---

## 一、本窗口【已完成并上线】(给接手人交代背景,勿重复做)

1. **小助手装不上 / 还是旧版** —— 已修。
   - 安装包 `installer.iss`:加 `CloseApplications=force` + `[Code]` 在装前/写文件前 `taskkill /F` `companion.exe`、`pack_runner.exe`(旧版常驻托盘锁住 exe → Setup 报 "unable to close" / "DeleteFile code 5")。
   - 新安装包已传 prod `/opt/mrpilot/static/companion/PearnlyCompanion-Setup.exe`(75912338 字节)。
   - companion 仓库 commit:`fix(companion): 覆盖安装强制关旧版 + 桌面图标改实底 app 图标`(本地,未 push 远端——companion 仓库分支状态复杂,见 [[express-rpa-second-entry-handoff]])。

2. **桌面图标透明底 + 糊** —— 已修。`pearnly.ico` 改成"白色圆角实底 + 全尺寸 16→256",合成自 `assets/pearnly.png`。已在 Owner 机器上重装验证:图标变白底、快捷方式指向新 ico。

3. **下载没进度 + 下到旧包** —— 已修。
   - 前端 `erp-express-wizard.ts`:下载从 `fetch→blob`(灌内存、下完才弹)改 `/static` 静态直链 `<a download>` → 浏览器原生下载、进度进"下载内容"队列。
   - Cloudflare 把 `.exe` 按 `immutable max-age=2592000` 缓存 30 天 → 裸 URL 永远给旧包。下载链接加 `?v=20260622b` 破边缘缓存(实测 MISS → 取源站新包)。**换安装包时必须 bump 此值**(在 `erp-express-wizard.ts` 的 `SETUP_URL`)。

4. **体积/格式闸回归归零**(本窗口前几次提交把闸搞红了,已补)。
   - `history-drawer.ts` 507→496:5 处 fetch 的鉴权+账套头抽成 `_hdrAuthWs()` 去重。
   - `store.py` 501→500:`route_assigns_workspace` 改由 `dal_reexports` 直接从定义模块 `seller_routing` 取(不再经 store 透传);契约测试改指向 seller_routing。`persist` OCR 路径零改。
   - `black` 统一 10 个存量未格式化文件(多窗口遗留 + 本窗 agent_store)。
   - `globals.d.ts` `_wsHeader` 类型收紧 `Record<string,string>` → 清 4 处 TS 告警。

   相关 commit:`5c096662`(black)、`80583ee0/3b89c3a6`(收口)、`ff70eb09`(下载破缓存)。

---

## 二、本窗口【未完成】的任务(Owner 点名要做、我没做完)

### 任务 1 — lint-ui 三道闸红
**真实规模:不是"几个小红点",是大存量视觉债。** 项目 STATE 已明列为 **Zihao-gated 视觉清理 backlog · 未起**。

| 闸(CI 会跑) | 现状 | 性质 |
|---|---|---|
| `check_ui_consistency.py`(rc=1) | 220 违规:B1 没带 `.btn` 4 / C1 CSS 裸写 hex **205** / B4 旧按钮类 CSS 11 | 绝对计数"必须为 0",存量巨债,**改不快** |
| `check_theme_responsive.py --gate`(rc=1) | 暗夜 3 位 hex `246 → 251`(**新增 5 处**) | 棘轮 delta,**可修** |
| `ui_design_lint.mjs --gate`(rc=1) | 小固定 max-width `259→265`(+6)、内联旧弹窗/抽屉 `174→177`(+3) | 棘轮 delta,**可修** |

- Owner 口中**"某按钮黑底"** = `home-07-history-automation.css` 的 `.hd-tab.active::after`(D2 黑底,基线 0)。
- Owner 口中**"暗色模式回退"** = theme_responsive 新增的 5 处:`home-02-switcher.css`(`color: var(--ink,#111)`、`color:#fff` 多处)、`home-03-layout.css:94/269/291`、`home-04-results.css:50/59`。
- **能绿掉 2 个棘轮闸的做法**:把超基线的 delta 改回——暗夜 5 处 3-hex 改 `var(--token)`(注意有的是 `var(--ink,#111)` 这种带 hex 兜底,闸把兜底里的 `#111` 也算)、design-lint 的 6 处 max-width 包 `@media` / 3 处内联弹窗改设计系统组件。
- **改不快的**:`check_ui_consistency` 的 205 裸 hex→token 等,是几十文件的大迁移 = 那个被 gated 的大 backlog,别想一次清。
- 【需 Owner 拍板】要么"只把 3 个棘轮 delta 改回(绿掉 2 闸)+ 黑底按钮改蓝",要么"立项做整轮视觉债清理"。

### 任务 2 — 拆 `routes/oauth_routes.py`(539 行 → <500)·**登录主路径**
- 结构(关键函数行号):
  - `google_oauth_start` 168 / `google_oauth_callback` **190–301(约 111 行,最大块)**
  - LINE 段 306 起:`line_oauth_start` 313 / `_handle_connect_line` **332–394** / `line_oauth_callback` 395–539
  - 顶部一堆 `_gen/_verify/_parse oauth state`、breakout 工具函数 48–148
- 【需 Owner 拍板 · 改登录前先报方案是铁律】**建议拆法(纯搬迁、0 逻辑改)**:把 **LINE 段(306–539)** 整段抽到 `routes/oauth_line_routes.py`,各自 `APIRouter` 再在 app 里 `include_router`;或把 Google/LINE 的 callback 主体抽成 `services/auth/oauth_handlers.py` 的纯函数,路由壳只调用。
- **必验**:全单测 + oauth 路由契约测试 + **真站登录 E2E(Google 登录 + LINE 登录都要点通)**。破了 = 用户登不进,这是最高危,务必登录 E2E 过了再 push。

### 任务 3 — 拆 `services/erp/erp_push.py`(507 行 → <500)·**推送主路径**
- 结构(关键函数行号):
  - `build_mrerp_adapter` **115–205(约 90 行)**
  - `push_mrerp_history` **205–343(约 138 行,最大块)**
  - `push_to_endpoint` 358–458 / `test_endpoint_connection` 463–507
- 【建议 · 纯搬迁】把 `build_mrerp_adapter`(或 `test_endpoint_connection`)抽到 `services/erp/erp_push_adapters.py`,`erp_push.py` import 回来。只需腾 7+ 行即达标,挑一个内聚块搬即可。
- **必验**:全单测 + 推送相关测试(`test_erp_*`、express/mrerp mapper)+(可选)真站推送 E2E。

### 任务 4 — 拆完后跑全套闸 + CI + push + E2E
- 顺序:`check_file_size`(拆完应**全绿**,目前就剩 oauth/erp_push 两个超限)→ `check_line_ratchet`(新文件可能要 `RATCHET-EXEMPT` 或注意监控名单)→ `black/ruff` → 全单测(`python -m unittest discover -s tests/unit`)→ `npm run build` + dist 一致性 → 真站 E2E。
- 注意:本 clone **无 pre-push 钩子**,闸要**手动跑**;push 即上线(webhook),登录/推送改动**先报方案**。

---

## 三、当前各闸状态快照(prod HEAD `ff70eb09`)

- ✅ 绿:单测 **4638 全过**(skipped 3)、`black`(tracked 全过)、`ratchet`、build 一致性、asset-bundling、imports、i18n。
- 🔴 红(本窗口**未动**,即上面任务 1/2/3):
  - `lint-size`:`oauth_routes.py` 539、`erp_push.py` 507 两个文件超 500(登录/推送主路径,先报方案)。
  - `lint-ui`:3 道闸(见任务 1)。

---

## 四、给接手人的坑提醒
- `home.html` / `i18n-data.js` 是 CRLF,改要用 Python 二进制安全改、保留 `\r\n`,**禁 sed/prettier**;改 `i18n-data.js` 后 push 前必 `node --check`。
- 改 `src/*.ts` 必 `npm run build` + 一起提交 `static/dist`,并 bump `home.html` 两处 `?v=`(否则 prod 跑旧 bundle)。
- companion 仓库在 `D:\pearnly-companion`(独立 git),安装包构建:PyInstaller(64 位 companion + `D:\py311-x86` 32 位 pack_runner)→ Inno `ISCC.exe`。新安装包要**手动 scp 到 prod** `/opt/mrpilot/static/companion/` 并 bump 下载链接 `?v=`。
- prod:`ssh root@66.42.49.213`,app 在 `/opt/mrpilot`,前面有 **Cloudflare**(静态 `.exe` 被 30 天 immutable 缓存,换文件必须靠 URL `?v=` 破)。
