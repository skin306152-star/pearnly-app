# Node / Playwright E2E
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# REFACTOR-A8 · coverage.py 产物(不进 git · CI 现算)
.coverage
.coverage.*
coverage.xml
htmlcov/
venv/
.env
.env.local
.env.*.local
*.egg-info/

# 本地密钥(绝不进 git · 2026-05-23)
_gemini_key.local
_gemini_key.local/
*.local
pearnly-vision-key*.json
*-key.json
_probe_*.py
# 本地一次性诊断 / 清理脚本(历次会话遗留 · 需生产 DB 才能跑 · 从不入库)
_diag_*.py
_clean_*.py
_*.mjs

# Playwright(Python · 探测脚本副产物)
playwright-report/
test-results/
.playwright/
playwright_browsers/

# S7 运行时缓存 + 含 token 的本地实测脚本(绝不进 git · 2026-05-24)
.ai_mapping_cache/
_uitest/

# 部署包（临时文件）
_pkg/
_pkg_tmpstatic/
*.tar.gz
deploy_v*.tar.gz
deploy_v*.zip

# 截图 / 测试数据（不上传 GitHub）
yichang/
异常/
uploads/

# 数据库 / 密钥（绝对不上传）
*.db
*.sqlite
*.pem
*.key
secrets.json

# 系统文件
.DS_Store
Thumbs.db

# 备份文件
*.bak*
*.bak_*

payments/
storage/

tests/fixtures/*
!tests/fixtures/front_desk_intent_golden.json
!tests/fixtures/__init__.py
# 乱料验收包(IN-0a 常设制度):进料口守门资产,须入库
!tests/fixtures/messy_intake_pack/
tests/pipeline-results.json
tests/pipeline-results-before-fix.json
tests/pipeline-benchmark-*.json
tests/compare-b1-b2-result.json

# MR.ERP adapter runtime artifacts (test-run screenshots, debug traces)
docs/integrations/screenshots/_tests/
scripts/probe/_debug/trace_*.json
scripts/probe/_debug/armas_listing_*.html
scripts/probe/_debug/armas_listing_*.json
scripts/probe/_debug/armas_create_*.html
scripts/probe/_debug/armas_create_*.json
scripts/probe/_debug/armas_delete_probe_*.json
scripts/probe/_debug/armas_copy_*.json
scripts/probe/_debug/armas_copy_v2_*.json
scripts/probe/_debug/product_listing_*.html
scripts/probe/_debug/product_form_*.json
# 探测产物含真实账套主数据(隐私)· 整目录不入库
scripts/probe/_debug/

# 本地真实账单样本(绝不进 git · 隐私)
_bank_samples/

# v118.35.0.64 · OCR 持久化缓存目录(运行期产物 · 不进 git)
.ocr_cache/

# v118.35.0.65 · OCR 评测集:运行产物 + 真实标准答案(隐私)不进 git;
#   只有 run_eval.py / README / ground_truth/example_synthetic.json 进 git
tests/eval/_runs/
tests/eval/ground_truth_local/

# REFACTOR(2026-05-25 第二十一会话)· 本地 dev scratch / 诊断探针 / 临时输出(从不入库 · 历次会话遗留)
probes/
_deploy_nginx/
_test_reports/
recon-i18n-fixes-*.md

# Claude Code 本地覆盖配置(每台机器各自的权限/偏好 · 按约定不入库)
.claude/settings.local.json

# 个人杂物 / 系统文件 / 会话转录(不进 git · 2026-05-29)
desktop.ini
*.lnk
code快捷键.html
BC完成后.md
Power of attorney template.pdf

# 本地草稿 / 工具目录(从不入库 · 2026-05-30)· Codex 工作目录 + UI 预览草稿
.codex/
design-preview/check_ui_consistency.py

# 主控 UI 验真探针产出(本地·勿提交)
scripts/_mc_verify/
CLAUDE.md/STATE_PEARNLY.md.bak

# DMS probe scratch (live-probe screenshots/html · never commit)
_dms_probe/

# ── 本地 scratch / 输出 / 审计产物(2026-06-26 · 收尾整理)──
# 历次会话遗留的本地输出/审计产物 · 只本机用 · 从不入库。
# 之前未忽略 → git status 噪声 + 污染本地 black 扫描。仅忽略不删除(文件仍在本地)。
# 注:scripts/_*.py、scripts/probe/_*.py、scripts/_*.cjs、tests/e2e/_*.spec.js
# 不在此忽略 —— 那些前缀里混着大量【已跟踪】的真 e2e/authz 工具,整忽略会埋坑。
/outputs/
/_ocr_test/
/product-audit/
/tmp_video_frames/
tests/visual/_shot/
tests/visual/playwright-output/
# 根目录一次性截图 / 安装图标产物
/_*.png
/_*.ico
# 根目录一次性调试转储 / 验证脚本(同上思路 · 只本机用)
/x
/_*.cjs
# 散落输出目录:output/(单数,与上面 /outputs/ 并存的另一处)+ 一次性 UI 审计/mock 产物
output/
scripts/_mock/
scripts/_ui_audit/
scripts/_ui_audit_full/
# ★草稿垃圾桶约定(根治散落):各窗口的临时脚本/数据一律丢进 _scratch/,整目录忽略,
# 不再散落到 scripts/ 根或仓库根。要留作正式工具的脚本才显式提交到 scripts/(不带 _scratch/)。
/_scratch/
scripts/_scratch/

# 前端 sourcemap 禁入库(vite 已 sourcemap:false · 双保险;历史曾误提 306 份 3.2MB map 撑爆 .git)
# 大文件闸见 scripts/check_blob_size.py + CI lint-size
static/dist/*.map
*.js.map
*.css.map
