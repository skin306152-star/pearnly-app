我读完你写的 `docs/ocr-current-architecture.md` 了,基本准确。

现在我有一个新的推荐架构,来自我和另一个 AI 顾问(Claude)的多轮深入讨论。我把它完整贴在下面,你先理解,不要急着改代码。

==== 推荐架构开始 ====

[这里你完整粘贴我之前给你的"推荐架构:三层兜底"那段内容,从"给你一个我认为对你这个场景最性价比的架构"开始,到"加上这些优化,生产环境真实平均成本可以做到 $1.50–2.00/1000 页"结束]

==== 推荐架构结束 ====

我要你做的事:

## 第一步:理解新架构
用你自己的话复述这套三层架构的核心思想,以及每一层的职责、触发条件、成本估算。我要确认你真的理解,不是死记硬背。

## 第二步:对比当前架构 vs 推荐架构
做一个对照表,逐项列出:
- 当前方案的某个模块 → 推荐方案对应的部分
- 哪些可以直接替换
- 哪些需要新增
- 哪些当前的代码可以删除
- 哪些当前的逻辑可以保留

## 第三步:列出迁移所需的所有变更
基于上面的对比,列出从当前架构迁移到推荐架构需要做的所有事情,包括:
- 需要新增的依赖(google-cloud-vision 等)
- 需要新增的环境变量(GOOGLE_APPLICATION_CREDENTIALS 等)
- 需要新增的模块/文件
- 需要修改的现有模块/文件
- 需要删除或废弃的代码
- 数据库 schema 是否需要改(比如新增 confidence 字段)
- API 接口是否需要改

## 第四步:识别风险和疑问
对于推荐架构里你觉得**不确定、有疑问、可能踩坑的地方**,全部列出来问我。包括:
- 推荐方案里没说清楚的细节
- 和当前业务逻辑可能冲突的地方
- 你觉得可以做得更好的优化点

## 输出格式
整理成 `docs/ocr-migration-plan.md`。

**这一轮还是不要动任何代码。** 我看完你的迁移计划和疑问后再决定怎么动手。