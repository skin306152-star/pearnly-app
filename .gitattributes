# pre-push 机械闸钩子必须 LF 行尾(2026-05-30)
# Windows 下 core.autocrlf=true 会把它转成 CRLF · 行尾的 \r 会让 sh 报
# "bad interpreter" 跑不起来 · 闸就哑了 → 这里强制 LF 锁死。
# 仅作用于钩子目录 · 不动项目里其它(home.html/home.css 故意保留 CRLF)。
scripts/git-hooks/* text eol=lf

# 所有 shell 脚本同理钉 LF(批次 C 记债 #0):部署目标是 Ubuntu 服务器,
# Windows 工作树若引入 CRLF,服务器上 bash 会因行尾 \r 报 "bad interpreter"。
*.sh text eol=lf

# 跨仓交接的 golden 载荷:两仓在同一台 Windows 机器上,但 core.autocrlf=true 会在检出时
# 把它转成 CRLF,而 `--write` 永远写 LF —— 不钉死就是每次跑闸都冒出一份纯行尾的假 diff,
# 真正的键名改动被淹在里面。
tests/fixtures/express_doctype_golden.json text eol=lf

# 扫码地基:tests/unit/test_scan_assets.test_sources_are_lf_only 断言这些源文件是 LF
# (bundle 把各文件 minify 后用 `;\n` 拼起来,混进 CRLF 只会制造纯行尾的假 diff)。
# 库里存的一直是 LF,但 Windows 上 core.autocrlf=true 会在【检出时】转成 CRLF ——
# 于是那条断言在每一台 Windows 机器上都是红的,而 CI(Linux)一路绿,没人看得见。
# 断言要成立就得把契约钉在检出这一步,不能只写在测试里。
static/scan/* text eol=lf
