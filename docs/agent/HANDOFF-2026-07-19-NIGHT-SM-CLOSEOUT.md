# SM 2569-05 第三次重跑 · 当夜收官交接

> 日期:2026-07-19 深夜。上一份交接:`HANDOFF-2026-07-19-PEARNLY-AI-SM-LIVE-RUN.md`(§0 日间补记)。
> 结论:**引擎首次端到端跑通**——104 件原始照片 → 交付包生成,ภ.พ.30 四数对齐真实申报
> (进项税逐字命中);产品旅程差最后一步(签批死循环,见 P0-1)。明天先修 GC-D 再谈别的。

## 1. 现在停在哪里

- 工单 `bd093ba9-dd60-484f-84c5-c66fa0f51a95`(客户 106 · 2569-05)状态 `review`。
- Zihao 已在 UI 点过「确认无误,标记待签」与「复核通过」(`review_signoff` 事件 11:10:59 在库)。
- **只差「签批冻结」一步**,被 P0-1 死循环挡住:复核通过触发自动重跑,跑完把「已复核/已标记待签」
  显示刷回原样,用户被迫绕圈。修复后 Zihao 一下点完即归档。

## 2. 金标对比(零金标输入·全程可回放)

| ภ.พ.30 | 系统 | 事务所申报 | 差 |
|---|---|---|---|
| 销售额 | 858,194.24 | 858,780.16 | −585.92(0.068%) |
| 销项税 | 60,073.60 | 60,114.61 | −41.01 |
| 进项税 | 29,263.28 | 29,263.28 | **逐字命中** |
| 应纳 | 30,810.32 | 30,851.33 | −41.01 |

残差=4 处页边界行级读数抖动(Σ฿3,288·0.36%·coverage 有记录,界面降级卡可见)。

## 3. 今晚上线的四个补丁(全部代码+测试+CI 绿+已部署)

| commit | 内容 |
|---|---|
| `4d35cd6b` | 跨页交接闸补 2% 重要性阈值(零容忍二值闸在 19 页照片下必死,对齐页内闸 R1 标准) |
| `36af8fde` | 批量题面 JSON 对象包装——openai 结构化输出拒顶层裸数组(GC-C 测试全假响应没踩到) |
| `44acd479` | **裁决单槽死锁根治**:同一件 recalc↔assign_kind 互顶永远出不了包(IN26-00557 实锤)。新 `services/workorder/steps/reconcile_decisions.py` 方向槽+金额槽合并回放;reconcile.py 拆分保 <500 |
| `eed36483`/`471d2ac0` | 棘轮收口(新文件 RATCHET-EXEMPT 我也踩了一回) |

## 4. 今晚的运维动作台账(全在事件流可回放)

- 银行解析共 5 代:gen1/2(economy·IMG_2485 同位置吞 ฿28,363 EDC 销售行)→ gen3(`OCR_ENGINE_MODE=direct35`
  单进程 env 覆写·页内断链 0·入账 918,267.84)→ gen4(回退)→ gen5=复制 gen3 事件恢复(actor=ops)。
- 597 笔个人转账「确认为销售」+ 采纳建议值 + IN26-00557 两笔裁决(recalc 5284.49/369.91/5654.40 +
  assign_kind purchase_invoice),全部 actor=`ops:delegated-review`——**UI 端分组确认/采纳未被真人走过**。
- SSH 旁路跑引擎必带 systemd env(`GOOGLE_APPLICATION_CREDENTIALS` + `GRPC_DNS_RESOLVER` 不在 .env),
  详见记忆 prod-two-machines-ssh-trap 血泪 2。

## 5. 遗留问题清单

### GC-D(可靠性/旅程·先修·目标=下一单无人在场跑通)

1. **P0-1 签批死循环**(见 §1)。复核/待签状态判定不许被复核自己触发的重跑刷掉。
2. 断链页自动升档重读(economy→direct35 阶梯·一次为限·断哪页读哪页)。
3. `run_pipeline_for_file` task 写死 `invoice`,配置 `overrides_by_task.bank_statement` 管不到工单银行链路(白设)。
4. 工单页缺料键裸露(`sales_summary` 未转人话——缺料键人话化上一批只盖了看板和交付包)。
5. 工单页「银行对账未启用」卡是另一模块(E1/E2 逐笔对平)的空态,混在月度旅程里误导。
6. brain 批量失败批(7/19)自动重试收尾;`bank_sales_brain.run` 三连败熔断后无自动恢复。
7. UI 真人旅程补课:分组确认 → 采纳 → 签批,三段由真人在界面走一遍。

### GC-E(账簿二期·影子底稿从「守恒佐证」升级到「可对真账」)

1. 报表页三处**表头与数据列错位**(试算平衡表/建议分录/科目余额·Zihao 红框截图·纯 UI)。
2. 分录叙述「货记入存货」vs 实际科目 5290 杂项费用,自相矛盾,二选一。
3. 39 张进货全进杂费——零售业态存货口径错,利润表(净利润 ฿440,147.32)不可信。
4. 销售挂应收 918,267.84 / 已付票(PAID 章)挂应付 447,310.20——银行流水在手却虚挂往来。
5. 科目桥(coa_erp_bridge 现成)套进影子报表;拿 SM 真 GL 导出把「与 GL 对平」真跑一次。

## 6. 明天执行顺序建议

1. 修 P0-1 → Zihao 点「签批冻结」归档本单(真人旅程终点补完)。
2. GC-D 2-6 一个批次(全是点名到文件的小活,断链升档稍大)。
3. GC-D-7 真人旅程验收(可与下月 2569-06 重跑合并做)。
4. GC-E 另开批次,不与可靠性混。
5. 验收线不变:**连续 3 个客户-月份、零后台介入、界面走完、差异 ≤0.5% 且可解释 = 能用**。

## 7. 收尾状态

- /simplify 收口审计:今晚四补丁均为小改(合计 +181/−24),两轮棘轮收口已内嵌完成压缩;
  `reconcile_decisions.py` 为拆分新模块(带 6 例单测),无可再安全合并的重复实现。
- master=`471d2ac0` CI 全绿;生产已部署同 commit;共享树干净(除他窗未提交文件)。
- 施工护栏 pre-push 钩子已删;wip/gc0719-* 远端分支已删(已合入);我的验收 worktree 已清。
  `Desktop\gc0719-a`/`gc0719-bc` 两个 worktree 属 Codex 施工窗口,窗口关了可 `git worktree remove`。
- 生产数据:客户 106 / 2569-05 工单为本次重跑产物,**保留**(含完整证据链,勿清——下一步签批归档要用)。
