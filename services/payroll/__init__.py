# -*- coding: utf-8 -*-
"""工资薪金预扣税(ภ.ง.ด.1)进料引擎(批次 H · 纯后端)。

事务所维护的月度工资 Excel(每客户一份,列序不可控)→ 兼容式列映射进料 → 只验不算的
五校验 → 三产出(ใบแนบ 扁平 pipe · 键入底稿 xlsx · 官方 FORMAT กลาง txt)。数据不经 OCR、
不进工单勾稽,做独立工具卡;校验通过点亮 pnd1 月度义务。工资个税计算是全 HR 范畴,不做
(事务所已算好,扣税列一律采信,见 docs 方案 §3.1)。
"""
