我看完你的迁移计划了,以下是我对你提的疑问的回复:

[这里你逐条回答它问的问题,不知道的就贴回来问我]

现在我们开始动手,但**用最小风险的方式**:

## 实施原则
1. **不要替换现有代码,而是新增并行的新模块** —— 我要先在小范围测试,确认新架构跑通且效果更好,才切流量过去
2. **从最简单的一层开始** —— 先实现第 1 层 Vision API 调用,跑通,有产出,再做第 2 层
3. **每完成一个原子任务就停下来汇报**,等我确认后再继续

## 本轮任务:只实现第 1 层 Vision API 调用
不要碰现有的 Gemini 代码,新建一个独立的模块:
- 位置:`services/ocr/vision_layer.py`(按你项目的实际结构调整)
- 功能:接受一张图片或 PDF 路径,调用 Google Cloud Vision DOCUMENT_TEXT_DETECTION,返回包含原始文本、confidence、坐标的结构化结果
- 凭证:从环境变量 `GOOGLE_APPLICATION_CREDENTIALS` 读 JSON 路径
- 写一个独立的测试脚本 `tests/test_vision_layer.py`,用一张样本图片(我会放在 `tests/fixtures/`)跑通测试
- 不要接入到现有业务流程,不要改任何现有代码

完成后告诉我:
- 新增了哪些文件
- 怎么运行测试脚本
- 测试输出长什么样