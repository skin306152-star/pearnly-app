# Deterministic dead-rules engine (P3). Pure functions over an Invoice + a
# RuleContext; no database or LLM inside the rules. Orchestration lives in
# services/knowledge/rules_engine.py; persistence in services/knowledge/rules_dal.py.
