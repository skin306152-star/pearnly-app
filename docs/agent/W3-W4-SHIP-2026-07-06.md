# Agent W3/W4 施工实录(2026-07-06 · commit 05afaa81)

体检作战包(桌面 Pearnly-Agent升级改造作战包-2026-07-05.md)W3/W4 波次一次落地。
全部闸控默认关,prod 放量顺序与回退口径见下表。

## 落了什么

| 件 | 模块 | 闸 | 默认 | prod 目标 |
| --- | --- | --- | --- | --- |
| W3-1 画像 + W3-2 昨日摘要 | `services/agent/user_profile.py` | `agent_user_profile` | 关 | all(fail-open 纯加料) |
| W3-3 事务所切换防错 | `line_workspace.switch_note` | 无(观测增强) | 恒开 | 随代码 |
| W3-4 知识库问答 | `services/agent/knowledge_confirm.py` | `agent_ask_knowledge` | 关 | 金丝雀(skin+163)→ 真机门 → all |
| W4-1 月报卡 | `services/notification/monthly_report.py` | `agent_monthly_report` | 关 | all(拍板默认开+可退订) |
| W4-2 VAT 数字版 | `proactive._copy_for` | 随 `agent_proactive_nudge` | — | 随现有金丝雀 |
| W4-2 掉线召回 | `services/notification/recall.py` | `agent_recall_nudge` | 关 | ★文案 Zihao 过目后才开 |

## 钱路红线(ask_knowledge)

模型只备料;`write_sink` 出确认卡(nonce 一次性);点「ยืนยัน」→ 余额前置检查 →
后台检索+生成 → `contract.charge_credits`(与网页 `/api/knowledge/ask` 同一钱路);
no_answer/失败不扣。registry `knowledge_ask_routes` 由 `exempt:billed-answer` 转正
`tool:ask_knowledge`,档 A→B(烧钱动作按写档管)。

## 表/列(prod 首用自愈 + alembic 留档)

- `line_agent_profiles`(0053):画像缓存,每用户单行 TTL 6h。
- `line_bindings.monthly_report_opt_out`(0054):月报退订标记。

## 回退

任一闸 rollout 改回 allowlist/关;switch_note 属纯观测,回退=revert commit。

## 剩余人类门(Zihao)

1. 召回文案过目(recall._COPY 四语)→ 点头才开 `agent_recall_nudge`。
2. 163 真机:画像第二天生效(不再重问类目)/ ask_knowledge 确认卡链路 / 月报强制触发一条。
