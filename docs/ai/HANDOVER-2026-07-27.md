# 全量交接单 · 2026-07-27(§0/§二/§三/§六/§七 已于 2026-07-31 第三轮按实测复判)

> 换窗口/换账号从这里接。上一份是 `docs/ai/HANDOVER-STEWARD-BRIDGE-2026-07-26.md`(其第三、四节已被本单取代)。
> **接手第一件事永远是 `git log --oneline -20` 两仓**,以 git 为准,文档会落后。
>
> **轮次编号**:§八 = 07-30 第一轮 · §九 = 07-31 第二轮(上半场)· **§十 = 07-31 第三轮(下半场,清欠账第二波)**。
>
> **2026-07-30 复判**:本单落笔时的头号结论「两仓一笔都没 push / master 带一道真红」**当天就被推翻**。
> 两仓都已推平、红已修。§0 表格、§二 八条拍板项、§三 二十二条欠账逐条重判过,§七 顺序重排。
>
> **2026-07-31 第二轮复判**:§0 表按实测重排;§二.7 关掉(CI 真加了 Postgres);§三 逐条再判一遍,
> **3.8 3.16 关掉,3.1 改判成「前半闭环 · 后半判为暂不做」**(理由三条写在 §3.1,别当没排期);§六 加第 11 条。
>
> **2026-07-31 第三轮复判(本次)**:§0 表按实测重取;**§3.13 §3.15 §3.17 §3.18 §3.21 全部关掉**;
> §3.1 加**第 ④ 个前置**(销项 DOCNUM 撞号 → 那个按钮只能对采购方向放开),前两个前置的码已落地;
> **§3.14 判为暂不做**并**划掉那句过期指令**(`_SCALAR_FIELDS` 白名单自 `814fe657` 起自动派生,照做是白费);
> §3.22 第 6 条按三轮 383 张重写;§六 加第 12、13 条(**本轮最值钱的两条**);§七 重排。
>
> **⚠️ 二次更正**:`560abfde` 那版新加的 §3.0 把 CI 那道红判成「产品回归」,**判错了**。实测根因是
> F1 那口桩漏抄真后端的形状,产品代码一行没改就绿了(`eaec02c4`)。§3.0 已按实测重写,**别照旧版去改 `syncSession`**。

## 0. 一眼看清现在在哪

> 2026-07-31 第三轮按实测重取。07-27 原表(app 落后 114 笔 / companion 落后 42 笔 / 1 FAIL / 13 道闸)整张作废,别再引用。

| 事实 | 实跑数字(2026-07-31 第三轮实测,不是抄的) |
|---|---|
| pearnly-app 远端 | `origin/master` = 本地 `HEAD` = `62c5c8d5`,**ahead 0 / behind 0**(sha 写死在这里,下一笔提交就过期,以 `git log` 为准)。push 即上线,全在生产上跑着 |
| pearnly-app 本窗口累计 | `7264eaf3..HEAD` = **85 笔** = 第一轮 20(`..eaec02c4`,§八.1)+ 中途 5 + 第二轮 13(`47d6e095..5a1097f1`,§九.1)+ 第二轮尾 7(`5a1097f1..dc4c684e`,§九.3 那三条尾巴 + 两份文档)+ **第三轮 40**(`dc4c684e..HEAD`,§十.1) |
| pearnly-companion | `origin/master` = `419022f`,**ahead 0 / behind 0**。本轮四笔已全推:`6c93b3d` 幂等取最后一条活行 / `16c2236` 采购明细行补 REFNUM / `cd8e103` bump 1.1.59 / `419022f` 四类新单据真靶场写冒烟。VERSION = **1.1.59**。⚠️ companion **主树**仍停在 `5d90b9a`,先 `git pull` 再看版本号 |
| app 单测 | `Ran 11649 tests` · **OK · 0 FAIL** · skipped=11 · 退出码 0。⚠️ 跑之前先 `rm -rf test-results`:Playwright 失败留下的 trace 资源会被破缓存闸扫到,造一条假红 |
| companion+bridge 单测 | 本轮改动后 `pytest tests bridge/tests -q` = **1013 passed / 26 skipped · 退出码 0**(基线 1001/26,新增 12 条)。⚠️ 不设 `PEARNLY_APP_DIR` 时跨仓 golden 闸整模块 skip,数字会掉 —— 那不是回归 |
| 机械闸 | `docs/GATES.md` **27 道**(数表格数据行数出来的,不是算的)。第三轮新增 2 道:`check_test_git_writes`(第 26 道 · 测试不许对当前仓库做 git 写操作)· `check_destructive_db_tests`(第 27 道 · `DROP/TRUNCATE` 必须接哨兵);同轮把**视觉照搬闸从「只在 pre-push 按路径触发」升级成 CI 无条件跑**(是升级不是加闸,不占道数) |
| CI | 最新 run id **`30627117412`**(sha `62c5c8d5`)· **conclusion = success** · **10/10 job 全绿**。中途 run `30625637803`(`6e209b78`)= failure,两条红都修在环境/夹具侧而不是把闸改回放行(§十.2)。**判绿只认 `conclusion` 字段** |
| 小助手线上版本 | 线上 `latest.json` = **1.1.59**(2026-07-31 Zihao 拍板后当天连发两版:1.1.58 → 1.1.59)。复核:`curl` 回 `{"version": "1.1.59", …}`;下载那个 URL 读 **PE 里的 ProductVersion = 1.1.59**(不是只看文件名)、61,851,247 字节,与线上磁盘一致 |
| 小助手装机版本 | **发出去 ≠ 装机的人升了。0 台是 1.1.58,更没有 1.1.59。** 生产库只读实测(`erp_endpoints` 6 条 express,读 `config->>'companion_version'`):`1.1.55` / `1.1.56` / `1.1.55` 三条报过,另 3 条从没报过。**最新一次心跳仍是 `2026-07-30 13:00:28Z`,那台报的仍是 1.1.55** —— 与第二轮读到的一模一样,这一天里没有任何一台升级过 |
| 自动更新是 opt-in(别记成自动) | `worker.py:_check_update` 每小时拉一次 `latest.json`;`gui_tray.py:_on_update_available` 只做两件事 —— 弹托盘气泡 + 把「更新到最新版」菜单项设为可见;真正下载静默装的 `_do_update` **只挂在 `menu.addAction(...).triggered`**(`gui_tray.py:339`),没有任何自动调用点。**要全所升上去,得有人去每台机器点那一下**;想改成静默自动装是产品行为改动,另开单 |
| 桥版本 | `bridge/engine/version.py` = **0.5.0**,未打包发布,未装任何真机(未变) |

**一句话状态**:代码全在生产上跑着,单测 0 FAIL,**CI 绿**。本轮把 §三 里能在本机做完的欠账清光了
(3.13 / 3.15 / 3.17 / 3.18 / 3.21 全关),外加四道闸自己的毛病、pre-push 钩子装机与口径修正、
视觉走查第三批 105 张。**没做的三件说清楚**:① Accserver 那一族(3.4~3.7 / 3.19 / 3.20)六条**物理不通**
—— 本机在 `172.20.10.3` 手机热点网段,`192.168.0.11:445` 够不着;② companion 1.1.59 **未发版**,而且
**连 1.1.58 现网也是 0 台升级**;③ 视觉走查三轮共 383 张图,但 `/pos` 与 `/admin` **仍只到公开页与未授权
落地态**(本机 `is_super_admin` = 0 行,产品里现在一个超管都不存在,按「别硬造账号绕过入口授权」没改库提权)。
产品面欠着的最重两条:**en/ja 用户在管家整片、看板问答条和余额不足卡上看到的是中文**(§二.9,影响面比 07-30
记的大得多),以及**「后端 500 显示成空态」是系统性的**,`/home` 三个面同病,本轮只修掉其中一个(§十.3)。

---

## 一、这一轮做完了什么

按批次,每条含 commit 范围与**验证到什么程度**(验到哪写到哪)。

### A. 桥 P2-A 云端写路(收官)
`927fa029` `08977074` `99539044` · companion `eeb097c`→`df01f6f`
- 云端 job kind 白名单 + 写桥角色闸 + 写门面;写载荷键冻成常量;超时不再吞在途写活(expired ≠ 已写进账套);桥端写事务全程持盘上互斥。
- **验证**:桥三件套 86 例 + 老链路 150 例绿;真账镜像写 E2E 117 项只读读回校验零失败;真机跨网写盘**未验**(桥没装到任何真机)。

### B. 桥 P2-B 四类新单据(收款/付款/手工凭证/库存调整)
companion `8b1c65a`→`ae81094` · app `77acfb50` `dbf3708` `e8c01e63` `6ff89cfb` `e178f1d6` `7e7cb48f` `325d6a72`
- 桥端四本菜谱 + 幂等闸 + 读回校验拆成独立件(闸不从被验代码里取期望值);云端四类载荷组装 + 按 direction 分派校验。
- **P2-B 一度整批假绿**:云端产物直接喂桥端 validate,四类里**只有 2 类过**。修完后靠三层闸钉住(见 §6)。
- **验证**:桥仓 416→1001 例绿;真账镜像 E2E 158 项断言 146 过(12 失败即当时那两条契约硬伤,已修);真靶场写**只验了采购/销项那条老路,四类新单据没上过真靶场**。

### C. 跨仓契约三层闸(P2-B 假绿的根治)
`6a59d073` `3ef943b8` `f4e30f24` · companion `5190485` `89863c4` `80adecec`
- golden 载荷落盘成**一份**跨仓交接物(云端 `--write` 产,桥仓路径直读),三层:① fixture 新鲜度 ② 云端镜像 `bridge_contract.py` ③ 真桥 validate。
- 顺带修复:老链路跨仓契约闸把桥仓路径写死 D 盘,搬家后**一直静默 skip**;隔离闸只认 `src.` 一种拼法,`companion.x` 整条穿过去 —— 两条都补了反证测试。

### D. 会计期间 / ISPRD 财年期(桥 + 小助手)
companion `6903dc4`→`8b35fce` `b95b061` `440c1c8` · `5ac14f5` `858de45` `9a6303b`
- 根因是**同一个**:桥/小助手从没读过 `ISPRD`,月桶按日历月取。改成按财年期号 + CY/NY 年槽落列;桥另加关账期与 24 期窗口外拒单闸。
- **验证(强)**:18 账套真镜像全量扫;17 本里 **14 本**当期桶与旧算法不同;11 月起账账套命中率 0.7~8.3% → 91.8~100%;CY=本年对照组零回归;反例逐条归因,无一指向算法错。TEST 靶场真写 6 case 全 PASS(动手前整目录备份 165 文件)。

### E. 防重单从「拦截」改成「提示不拦」
companion `00871ad` `694d8f2` `cbdcc2d` `1c08005` `7f789c5` `db423d4` `5d90b9a` · app `212bc460` `fb405362`
- 判据从「钥匙」升到「内容指纹」;两侧共用**逐字节相同**的 `duplicate_gate.py`(不 import,靠字节比对闸防漂);闸序统一成「查重 → 期间 → 防重单」;表头模式单落成 Express 真会产生的形状(`NXTSEQ='-1'`)。
- **验证**:靶场四场景钉成回归;L0 去重语义钉成契约测试。**⚠️ 云端到今天为止发不出 `duplicate_confirmed` 这个键 → 「知道了,还是推」这条路从云端不可达**(§3.1)。

### F. 管家 B3:异步化 + 授权卡 + 成本封顶
`1ddc1036` `6fcad45d` `70b5cc1c` `c058de49` `65580b38` `a6a90abb` `5d897d2d` `d2624b9c`
- 派活即返 task_id + 步骤流水轮询 + 超时/stale 自愈;写授权卡(一次性批文 + 令牌隔烧 + 租户日顶 + 会话/租户两级钱顶);前端 running 态/授权卡/取消。
- **验证**:管家单测 181 例绿;真浏览器 E2E 截图在 `tests/e2e/_artifacts/b3_closeout/`。授权链路当时是**网络层注入冻结契约真形状**验的(注册表那时还没有写工具)。

### G. 管家 B4:写工具 + 五只读工具 + 全所税额表
`94e99055` `408e8ffc` `4b38e328` `4bef57f6` `07a83ea8` `15ab7229` `137781ad` `6ffbbea1` `64dd12ba` `bdfc5ceb` `19ae8adf` `ea73225c` `40164a6a`
- 第一个写工具 `erp_push`(派活→授权卡→经桥真写 Express);五只读工具(到期义务/待审队列/应交税额/银行对账/单票体检);今天先干哪个 / 这期能不能签 / 交付物在哪;现场算 VAT。
- **验证**:全真栈 E2E 走完整一圈(不只验砖块);`_artifacts/b4_wire/` `steward_skills/`。**视觉层当时判不合格**(全所税额表两列截出框外),已在 §H 的返工里收。

### H. 管家大脑循环(把查表器变成会拆步骤的大脑)
`bbfba4b7` `c8d230a8` `4de1fdba` `ccf50b57` `0a5db2ba` `06de18ff` `079f0e80` `889656b2` `609e7744`
- 一条消息能连着调好几个工具;循环跑在 worker 不在请求里;任务级成本封顶**此前从来没生效过**(已修);追问候选变成能点的按钮。
- **验证**:47 题考卷(桩大脑 · 真编排);真模型实测单步 ฿0.1315–0.3395。

### I. 管家万能口(对话框吃任何料文件)
`b86ea918` `2801793c` `3e187180` `62f64a3f` `746ac9b3` `52fc2e42` `834e316b` `db99e2e9`
- 收料口(运输皮归一 + 落盘登记 + 到期清理)+ 确定性认料 + 传完之后的裁决(回执卡/按钮/花钱确认)+ 上传下载端点 + 前端拖拽/粘贴。
- **验证**:真栈 E2E 12 例绿,料全来自真语料库;zip/HEIC 真能传(审查揪出「文案说了没做」已修);下载死链已改指客户目录(`e1e9afb0`)。

### J. 账簿配方生成器(销售票四表账簿)
`73f0aab2` `25b5e4b8` `ee940db7` `7cd3bae6` `56d7a4b2` `852b6cff`
- 输入契约/科目解析/分录/注册表 + 四表写表器(跨表一律按票号 SUMIF,会计删行不炸)+ 金标三票回归。
- **验证**:对金标逐格比对 **52/52 数字位完全一致**(两侧都先过 LibreOffice 真重算,openpyxl 公式无缓存值)。顺带揪出并修了 P0:工作簿显示的数 ≠ `result.numbers` 权威数。

### K. /ai 计费打通(工单 OCR 接用户钱包)
`4ea0cb09` `092ddfd1` `9e7a7fbd` `cbc42fed` `aaadb23f` `c8e43c0f` `40cf63bb` `677b3aba` `8bdffa7d`
- 工单 OCR 余额闸 + 逐件扣费 + 余额见底诚实停机 + 补料端点 402 + 设置页计费区(余额/三步充值/充值记录)。
- **验证**:契约测试(闸/扣/停机/幂等/隔离);真机实测 402 体里 balance/estimated_cost/pages_used 三数都在。

### L. /ai 四态与文案收口
`68c266b7` `d6158a49` `873ada59` `a0c6a7c2` `30c10747` `ca2b02e5` `4d4f4ec0` `d256f01a` `b133424c` `cbe76610` `4ccf6d2d` `e9f2e20c` `d93b13b6` `b812d7d8` `7746a93a`
- 失败态给出路 / 空态点名去哪并给按钮 / 看板三筛选 + 批量开单 / 54 条用户可见文案去口水去黑话 + 禁词机械闸 / ฿ 与数字之间垫窄空格(收成单出口 + 机械闸)/ 手机端对话区回到「消息在上、输入在下」。
- **验证**:真浏览器 10 例过含一条反证;`_artifacts/steward_copy_rework/` `fail_ways_out/` `empty_states/` `board_tools/`。

### M. 文档与证据入库
`0ee3a081` `5c460751`(上一轮)`212bc460` `95fda65b` `7d66e01c` `4f7794b6` `c3735a38`
- Express 四类单据落表契约(真账套镜像逐条复核 41~44);重推/防重单验收清单;两份可重复跑的 E2E 入库(`_board_tools_verify.spec.js` 17 例 / `_b2m1_rework_verify.spec.js` 10 例 —— 10 份里只入库这 2 份,判据是「换个人换台机器还跑不跑得起来」)。

---

## 二、🛑 等 Zihao 拍板的

> 2026-07-30 逐条复判:1、2 已作废(事情做了);3~8 仍要他定;末尾新增第 9 条。
> **2026-07-31 第二轮再判:7 也作废了**(CI 真加了 Postgres,§二.7)。
> **2026-07-31 第三轮:仍是 6 条要他定 —— 3(的后半)、4、5、6、8、9,一条都没被替他拍。**
> 变化只有两处:**5 的前置从三个变四个**(销项 DOCNUM 撞号,§3.1),而**前两个前置的码本轮已经写完**;
> **9 的代价比 07-30 记的大得多**(逐张肉眼过完 383 张之后重量,见 §二.9)。
> 另外**第三轮新增第 10 条**(小助手 1.1.59 发不发)。
> **2026-08-01 第四轮:新增 11~14 四条** —— 四类新单据要不要补进托盘小助手(11)、库存调整的批号硬要求怎么解(12)、
> 照搬闸那 28 条要不要立项重画(13)、§3.1 那个按钮认不认「只对采购放开」(14)。前三条从 §11.6「本轮新发现、没修的」
> 提上来:它们不是欠账,是**等人拍板**才动得了的取舍,埋在第十一节里没人会看见。**连同上一轮的 6 条,现在共 10 条等他定。**

1. ~~**两仓要不要现在 push。**~~ **已作废** —— 两仓都已推平(§0),app `origin/master` = `eaec02c4`、companion = `7e43877`,ahead/behind 全 0。§3.1 那道红也已修。
   原文留档:「push 即上线,这一轮 105 笔里含钱路径、写 ERP、管家大脑;先修红 + 跑完视觉验收再推。」实际执行:红先修(`24a6b216`),视觉验收在 push **之后**才补(只覆盖 /ai 十个面,见 §八.3),顺序与建议相反。

2. ~~**pearnly-companion 的 42 笔要不要推 GitHub。**~~ **已作废** —— 已推。「1.1.57 二进制的源码只存在于一台机器」这个单点风险解除。

3. ~~**小助手要不要立刻 bump 1.1.58 发版。**~~ **已拍板并已执行(2026-07-31:Zihao 说发,当天发掉)。**
   原问题:`5d90b9a`(闸序统一 + 防重改提示不拦)落在 1.1.57 bump **之后**,线上二进制不含它。它修的是真触发面:一次成功推送之后的任何重投都会在小助手上硬失败 `PRIOR_DOC_STILL_IN_ERP`,并叫会计去删一张本来该留着的好单。
   执行结果:`packaging/release.ps1` 五步全过、退出码 0,线上 `latest.json` = 1.1.58。取证与 `.bak` 命名验证写在 §3.3。
   **但这条只关掉了一半 —— 剩下的一半是「谁去通知会计点更新」,那半仍然欠着,见下。**
   **装机现状(2026-07-31 生产库只读实测,不是估的)**:6 条 express 端点里报过版本号的 3 条 = 1.1.55 ×2 / 1.1.56 ×1,另 3 条从没报过;最新一次心跳是 **07-30 13:00Z 且那台仍报 1.1.55**。机器活着、拉得到新版,就是没人点。拿真 prod 跑 `check_update` 复核过:1.1.55 / 1.1.56 / 1.1.57 三种在装版本都能看到 `{"version": "1.1.58"}`,1.1.58 自己返回 `None` —— **通知链是通的,卡的是「人点那一下」**。
   **所以要连着定的下一件**:是去通知会计逐台点,还是另开单把自动更新从 opt-in 改成静默自动装(**那是产品行为改动**,会在会计不知情时重启小助手,得先想清楚推送时机与正在写 DBF 时怎么办)。
   **影响面提醒**:1.1.57 的二进制里**连 `duplicate_gate.py` 都没有**(`git show 9a6303b:src/companion/duplicate_gate.py` 直接不存在)。1.1.58 装上去之前,「查重差异」这类新回执现网一台都发不出来 —— **不装机,产品侧做什么都是死按钮**(§3.1 那个按钮的第三个前置就是这一条)。

4. **小助手要不要补关账期闸。**
   为什么要他定:给现役生产环境上一道新硬闸,会拦下会计现在能做的事。
   现状:桥侧 `8b35fce` 已拦;小助手侧**没搬**(`fiscal_period.py` 文件头自陈只搬了期号判定一半)。靶场实测:把 `ISPRD.LOCK7='Y'` 后,2026-07-15 的现购**照样落盘**且余额桶被改,不拒单不上报。根因是小助手 `DbfWriteResult` 只有 ok/错误码两态,没有「拒单转人工」的出口。
   我的建议:接 escalate 出口后补闸;在那之前**明确写下「小助手侧不管锁,靠桥」**,别让它含糊着。

5. **AR/AP 侧的期间窗口拒单闸误伤面。**
   为什么要他定:这是产品判断不是代码 bug。
   现状:GL 侧 65,481 张凭证 0 张落窗口外,论据成立;但 **ARTRN 16,955 行里 1,226 行、APTRN 30,729 行里 1,272 行** 的 DOCDAT 落在本账 24 期窗口外,其中 964 行的 CHGDAT 在窗口内(会计在这本账里动过它),含真·倒填单(`6902ASC` 的 `AI67T11-001`,单据日 2024-11-13、末改 2025-07-31)。Express 收得下,桥现在一律 `FISCAL_PERIOD_UNKNOWN` 硬拒。取证 `verify/v4_gate_blast_radius.txt`。
   我的建议:AR/AP 侧改成「拒单但给『我知道,照推』的确认出口」,与防重单同一个范式,别一刀切硬拒。
   **⚠️ 07-31 给这条建议加一个前置**:「我知道,照推」这个范式本身在本仓今天是**不安全**的 —— 队列是
   at-least-once,而「确认放行」正是关掉唯一兜住重投的那道幂等闸(逐条论证见 §3.1)。期间窗口这条路
   比防重单更钝(它不带内容指纹,重投一次就多一张,连「差异一致所以跳过」这层自愈都没有)。
   **拍板同意之后,得先做掉 §3.1 那三个前置里的前两个(投递语义改至多一次 + 写侧幂等取最后一条),
   再接这个出口**;否则就是把 §3.1 判为暂不做的那颗雷换个入口埋一遍。
   **✅ 07-31 第三轮更新:那两个前置的码已经写完并落地**(`72521aa1` 云端 + companion 工作树 `6c93b3d`),
   所以这条的技术障碍从「要先改队列语义」降到「companion 那笔得发版装机」。**但 §3.1 同轮又量出第 ④ 个前置**
   —— 销项方向 DOCNUM 就是票面票号,第二张必然同号 → 读回校验判失败回滚,**这个出口只能对采购方向放开**。
   AR 侧(应收/销项)想给同款出口,得先回答「两张不同的单怎么共存于同一个票号」。

6. **账簿配方的科目码与日期口径 —— 要会计确认一次,不该我们单方面定。**
   现状:金标用 1010/2010,本仓 preset 给 1020 银行 / 2030 销项税 / 4010 销售收入;落客户账套走 `coa_erp_bridge`,桥没建就退回 preset 并在表上标「未接账套」。日期口径建议「有 Date 用 Date;只有 in/out 用 out;跨月进待判」,金标取的是 in(同月无害但口径错)。`card` 付款方式无对应科目,建议进待判。
   出处:scratchpad `artifact_download_design.md` §13。

7. ~~**CI 要不要加一个 Postgres 服务。**~~ **已作废(07-31 做了)** —— `ci.yml` 里多了一个 `pg-smoke` job
   (`postgres:16-alpine` service),跑 `tests/unit/test_*_pg_smoke.py`,首跑就绿:run `30577191972` 与
   `30579711972` 的日志里都是 `Ran 13 tests / OK`(桥 5 条 + 台账 8 条,含三条反证),**没有 skip**。
   两处设计写在这里免得下一个人重推一遍:① **skip 判红 / 零发现判红** —— 光跑 `unittest` 在连不上库时
   退 0,那正是本仓栽过两次的「闸报绿≠闸看过」形态;② **独立成 job 不塞进 `test`** —— DB 抖一下不该
   把 11451 条单测的信号一起染红。测试只认 `PEARNLY_PG_SMOKE_URL`,**绝不回落 `DATABASE_URL`**(防误连生产)。
   **没做的那一半**:`tests/integration` 那 70 份 `require_db` 用例仍然从没在 CI 跑过(要 `PEARNLY_INTEGRATION_DB`
   + `DATABASE_URL`),首跑大概率红一片,**是单独一次拍板,别跟这条混着算**。

8. **管家泰文措辞两处要 Kornpearnly 拍一次板**:语气助词已全站拿掉(闸已焊死);客户页 tab 的「画像」中泰不是一回事(泰语是 `ข้อมูลภาษี`=税务信息),而「画像」这个词 Zihao 07-24 已拍板退役。

9. **(07-30 新增)`/ai` 的 en/ja 要不要真补四语,还是维持只写 zh+th。**
   为什么要他定:这是 §四.6 白纸黑字记过的口径决定(「B2-M1 落地时拍的,任务书写四语齐时未破例」),不是漏做,推翻它要他点头。
   现状:`static/ai/ai-i18n-fail.js` 与 `ai-i18n-steward.js` 只 `Object.assign` 了 `__AI_I18N_ZH__` / `__AI_I18N_TH__`,`at()` 的回落链是「当前语言 → zh → 原 key」,所以 en/ja 落成中文。真浏览器实测:en 桌面管家页 145 个汉字、余额不足卡 7 句里 5 句是中文,**连主 CTA 按钮都是「去充值」**。
   我的建议:**补**。理由不是「四语齐好看」——是会计最关心的那句「至少充 ฿4.50 才够把这批识别完」正好落在没翻的那批里,英日用户读不了就没法完成充值动作。工作量是给两个分片补 en/ja 两份词条(fail_* 13 条 + steward 那族)。
   **顺带要知道的**:这个洞机器闸看不见 —— 生 key 闸绿是因为 `at()` 回落到了 zh 词典而不是回落到原 key,回落把闸掩护了。
   **07-31 走查把面铺开之后,这条的代价比 07-30 记的还大**:`#/states` 整页在 en/ja 下回落中文(en 实测 456 个汉字),侧栏第一项「管家」两个汉字夹在 `Front Desk` / `Dashboard` 中间,`ai-states-render.js:258-385` 的节号还是硬编码中文数字「一…十五」,泰/日/英下也照印。截图 `closeout_visual_2/ai-states-{en,ja}-desktop1280-data.png`。
   **⚠️ 第三轮逐张肉眼过完 383 张之后再量一次,影响面还要往上翻 —— 拍板前请按这个规模判,别按 07-30 那句「一个导航项」判**:`stw_*` 分片撑的是**工作台/看板顶部那条问答条**(占位符「输入要查的事,例如『本期谁缺料』」、按钮「去问」、四个快捷 chip)**加整个管家两栏工作区**(标题、「会改数据的操作需你先批准」、执行状态栏、对话栏、输入框、「文件可拖入或粘贴」)—— 在 en/ja 下 **100% 是中文**。另有看板「本期缺单」条在 ja 页上渲染成中文「2569-07 还缺 1 张单: PP30」。按记忆里 Zihao 拍的「管家对话框 = 万能口 = 产品核心」,这已经不是「一个导航项没翻」,是**核心交互面对英日用户整片不可读**。截图 `closeout_visual/p2-board-{en,ja}-desktop1280.png`、`p2-steward-en-desktop1280.png`、`p2-failcard-{en,ja}-*.png`、`ui_tier3/01-board-ja-desktop1280.png`。

10. ~~**(07-31 第三轮新增)小助手要不要发 1.1.59。**~~ **已拍板并已执行(2026-07-31:Zihao 说发,当天发掉)。**
    执行结果:线上 `latest.json` = **1.1.59**;下载回来读 PE 的 ProductVersion = 1.1.59、61,851,247 字节。
    **`.bak` 命名修复第二次真机确认**:线上 `PearnlyCompanion-Setup.exe.bak-1-1-58` = 61,848,982 字节 = 上一版真发出去的那份 1.1.58,名实相符(修之前这个名字里装的会是 1.1.57)。
    发之前先在**本地 Express 靶场**把这一版要带的语义端到端验过了(§十一):走完「系统重投 → 改了金额未确认 → 确认落第二张 → 丢 ack 连投 3 次」,3 次全 `duplicate_skipped=true / written=false`,APTRN 活行数钉死在 2;反证 —— 退回「取第一条」重启桥,同一份确认载荷重投一次**当场多落第 3 张**。
    ⚠️ **但下面这条原本的顾虑一个字没变,而且现在多了一版**:现网 **0 台**是 1.1.58,更没有 1.1.59。发版只保证包在服务器上。
    以下为拍板前的原文,保留备查:

    为什么要他定:又是一次不可逆的对外动作(`release.ps1` 的 `scp` 覆盖生产 exe 与 `latest.json`),而且**上一版 1.1.58 发出去到现在,现网升级台数是 0**。再发一版不解决「没人点托盘」这件事。
    现状:companion 工作树 `.claude/worktrees/steward-closeout` 有两笔已提交、未推、未 bump 的改动 —— `6c93b3d`(`_find_existing` 改取最后一条活行 = §3.1 的前置②)与 `16c2236`(采购明细行补写 `REFNUM`)。**按铁律「不 bump = 改了白改」,这两笔今天在会计电脑上一行都没生效。**
    这两笔各自值多少:前者是 §3.1 那个按钮的硬前置,今天没有触发面(按钮还没放开);**后者有真触发面** —— 真账镜像实测采购行 `REFNUM` 填充率 RR 98.0% / HP 100%,而 Express 随程序发的标配库存卡模板(`STCRD->REFNUM`,39 个文件 12 种模板)用它当单据号列,我们留空那一行在库存卡上就只剩内部单号,同页原生行显示的却是供应商票号。
    我的建议:**跟「谁去通知会计点更新」一起拍,别单发。** 要么这一次就把「通知逐台点」这件事做掉(那 1.1.58 + 1.1.59 一次装完),要么先按 §3.2 另开单改静默自动装 —— 在那之前多发一版只是多一个没人装的包。

11. **(08-01 新增)四类新单据只有桥那条路有实现 —— 要不要给在跑的托盘小助手也补一份。**
    为什么要他定:这是「养不养第二份写路」的产品取舍。补 = 每一处 DBF 写法从此要同步两遍(今天已经是四份副本同改);不补 = 这四类单据的上线日期直接等于桥的装机日期,而桥装机卡在事务所网络拓扑上,没有日程。
    现状(08-01 逐文件数的,不是估的):在会计电脑上跑的那份 `src/companion/` 下 `doc_*` 文件 **0 个**,`worker.py:108-114` 只按 `direction` 分 `write_sale` / `write_purchase` 两条路,载荷里的 `doc_type` **一个字都不读**;四类单据(收款 / 付款 / 手工凭证 / 库存调整)的实现全在同仓 `bridge/writepath/` 的 **17 个 `doc_*` 模块**里,分发表 `DOC_WRITERS` 也只有桥有(`bridge/write_jobs.py:59-64`,四个键 `ar_receipt` / `ap_payment` / `gl_journal` / `stock_adjust`)。
    **所以实况是**:桥一台真机都没装(§3.5),托盘小助手又不认这四个键 —— **这四类单据今天对会计不存在**,云端把产品面做得再全也落不了地。§3.6「四类新单据从没上过真靶场」那条,08-01 应改读成「跑过的那次是桥,而桥不在任何一台会计电脑上」。
    我的建议:**不补,把力气压到桥装机上** —— 一份写路一个真源;§二.4(关账期闸只有桥有)和 §3.4(两边行为不一致)本来就是「两份写路」这个结构自己长出来的病,再补一份只会多一处漂。但这条要他点头,因为它等于把这四类单据的上线绑死在「桥装第三台常开 Win 机」这件还没影的事上。

12. **(08-01 新增)库存调整在「库存流水全由 Pearnly 推」的账套上永远走不到写盘 —— 放宽写路语义,还是在云端关掉入口。**
    为什么要他定:一条动写路语义(影响成本核算口径),一条是产品取舍(砍掉一个已经写完的功能面),两条都不是我该单方面拍的。
    现状:`bridge/writepath/doc_stock_lines.py` 的 `_pick_lot`(147-165 行,每条减少行在 :83 无条件走一遍)硬要求指向**真实存在的来源批** —— 载荷给了 `source_lot` 就校验它在批次链里,没给就 FIFO 取最早一条,**选不出就 `DocReject(ERR_TARGET_DOC)`**(`ERR_TARGET_DOC = "TARGET_DOC_NOT_USABLE"`,定义在 `doc_payload.py:41`);而批次链只收 `MLOTNUM` 非空的行(`_lot_cache` 168-182)。Pearnly 自己的采购 / 销售写路**从不写 `MLOTNUM`**(`stock_purchase.py` / `stock_sale.py` / `dbf_detail.py` / `dbf_writer.py` / `dbf_sales.py` 全仓零处引用 —— 批号只有 Express 自己产)。**两头一对:凡是库存流水全由我们推的账套,批次链恒为空,库存调整必被 escalate。**
    ⚠️ **有一个数字要复核再用**:§11.6 记的「靶场 STCRD 活行 `MLOTNUM` 全空」这句,树里找不到可复跑的取证(两仓都没有 `verify/` 目录,也没有别的文件提过 `MLOTNUM` 的实测),**只有交接单自陈**。上面那条代码链是逐行读出来的、成立;要拿「全空」当拍板依据,得回靶场重量一次。
    我的建议:选 ①「无批次时允许空 `MLOTNUM`」—— 我们自己写进去的行本来就没有批号,却要求减少行指向批次,是自相矛盾;**但它会让减少行在 Express 的库存卡上落成无批次行,成本口径的影响要会计确认一次**(同 §二.6 那类问题)。在他定之前无论如何先做一件事:**云端入口按「这个账套有没有批次链」提前灰掉,别让会计推完才收到 `TARGET_DOC_NOT_USABLE`。**

13. **(08-01 新增)照搬闸登记表那 28 条,要不要立项把那几屏的设计稿按生产整页重画。**
    为什么要他定:这不是机械修补,是「稿以生产为准、还是生产以稿为准」的设计取舍,工作量按整页算,而且改的是别人拍过的设计。
    现状(08-01 数的):`tests/visual/design/_freshness-allow.json` 的 `pages` 下共 **28 条** class 登记(另有 1 条 `global.ptag`,合 29),分在 7 个页:`dashboard-final` 8 · `acct-list` 6(`.ai/.buy/.sell/.exp/.mo/.txt`)· `pur-form` 4 · `pur-detail` 4 · `acct-review` 4(`.choice/.ot/.od/.hl`)· `pur-list` 1 · `acct-books` 1。多是「稿画了而生产另起一套 class」的概念件。
    ⚠️ **§八与 §11.6 里那句「32 条」已经过期**:它点名的 `pur-form`/`pur-detail` 的 `.grid`、`dashboard-final` 的 `.bl`/`.bn` **四个 class 在现表里已经没有了**(上一轮清掉了)。按 28 判,别按 32 判。另外按登记表自己的分类法,**这 28 条没有一条是形式上的「旧账」类**(那类的理由必须以 `旧账:` 开头,现表 0 条),全登记成了装饰件 / 外壳区 —— 也就是说「欠着」这件事在表里根本没被标成欠着,这本身就该顺手改掉。
    我的建议:**单独立项,别混进日常改动**;先只挑 `acct-list` / `acct-review` 两屏(10 条,占 28 的 36%,而且最像「生产已另起一套」),按生产实况重画整段基准,清完再看剩下的。

14. **(08-01 新增)§3.1 那个「我知道,照推」按钮:①② 已落地,③④ 仍卡着 —— 要不要就按「只对采购方向」放开。**
    为什么要他定:放开 = 允许「明知重复也再落一张」进真账套,而 Pearnly 删不掉 Express 里的单,误点之后唯一的补救是会计自己去 Express 删。这是不可逆动作的开关。
    四个前置的现状(逐条核过,不是照抄上一轮):
    - **① ✅ 投递语义改成至多一次** —— `72521aa1`(07-31,15 文件 +718/-8):`lease_pending` 对带 `duplicate_confirmed` 的行只认 `lease_owner IS NULL`,已领走又超时的由 `close_unacked_confirmed` 落 manual + 新错误码 `ERR_PUSH_UNCERTAIN`,不重投也不躺回 pending。
    - **② ✅ 写侧 `_find_existing` 取最后一条活行** —— companion `6c93b3d`(10 文件 +311/-24,四份副本同改),**且本地 Express 靶场端到端验过**(§二.10:丢 ack 连投 3 次全 `duplicate_skipped=true`、APTRN 活行数钉死在 2;退回「取第一条」当场多落第 3 张)。⚠️ 它在 1.1.59 里已发版,但**现网 0 台装**,所以这一条今天在会计电脑上仍是零。
    - **③ 🟠 装机** —— 纯粹卡在「有人去每台会计电脑点托盘」(§3.2)。
    - **④ 🛑 销项方向落不了第二张** —— 销项 DOCNUM 就是票面票号,确认后落的第二张必然同号 → 读回校验按 DOCNUM 找到旧那张 → `NETAMT` 对不上 → `READBACK_VERIFY_FAILED` → 整体回滚,会计看到的是一句像写盘坏了的错误。**这个出口只能对采购方向放开。**
    我的建议:**等 ③ 装机之后只对采购方向放开,销项那半明确写成「不做」**,而不是含糊着摆一个禁用按钮(§二.5 的期间窗口出口同理,它排在这条后面)。要他定的就是「只对采购放开」这句认不认 —— 认了我才有资格把 §3.1 的后半从「暂不做」挪回排期。

---

## 三、未闭环的欠账(按严重度排序)

> **07-30 判过一遍,07-31 第二轮判过一遍,07-31 第三轮逐条再判一遍**,标记含义:✅ = 已闭环(带 commit)· ✅/🟠 = 做了一半 · 🟠🟡⚪ = 原样欠着 · 🛑暂不做 = 判断不是没排期。
> 判定方法:`git log --name-only --format="" dc4c684e..HEAD` 取第三轮真改过的文件清单,逐条比对,不靠回忆。
> **一句话(07-31 第三轮口径)**:
> **已关掉**:3.0 3.8 3.9 3.10 3.11 3.12 3.16(前两轮)+ **3.13 接地闸收紧 · 3.15 STCRD REFNUM · 3.17 三处棘轮基线 · 3.18 那个 APBAL 组合追到底 · 3.21 过账去向补选重推**(本轮)。
> **判为暂不做(是判断,不是没排期)**:3.1 后半(三条理由 + **四个**前置,第 ④ 个本轮新量出来)· **3.14 OCR Time in/out**(提示词那步真能读出跨日时刻,但会把 A4 采购税票的税号稳定读错 —— 拿抵扣凭据换「104 张里 1 张入账日差一天、且申报期不变」不划算)。
> **仍欠着**:3.2 装机那半(现网 **0 台**是 1.1.58,与第二轮读到的一模一样)· 3.22 第 6 条三轮做完仍不是全覆盖。
> **Accserver 那一族(3.4~3.7、3.19、3.20)三轮一条没动** —— 本机在 `172.20.10.3` 手机热点网段,`192.168.0.11:445` 物理不通。

### 3.0 ✅ 万能口那道 E2E 红已闭环(`eaec02c4`)—— 根因是桩不是产品,产品代码零改动

- **是什么**:CI run `30554691792`(`8f100473`)failure。9 个 job 里 8 个绿,只挂 `test` job 里一条 E2E:
  `tests/e2e/_f1_steward_attach_local.spec.js:458` 「纯文件送出 → 气泡下有只读原件行 · 回执卡按钮把参数原样带回」。
  断言 `expect(locator('.stw-msg.me .stw-file-pill')).toHaveCount(2)` → **Received: 0**,初跑加两次重试全红。
- **⚠️ 本节的第一版把根因判成「产品回归:`syncSession` 整份替换消息、服务端会话行不带 `attachments` 所以原件行没了」,这个判定是错的,已被实测推翻。** 照它去改 `syncSession` 的合并语义,是在改一个没坏的东西。留着这段是为了让下一个人别再走一遍。
- **真根因 —— 桩的形状对不上真后端**:
  1. 老桩对 `GET /steward/sessions/{id}` 回的是 `{ session_id: 's1', messages: [] }`,**空数组**(`git show eaec02c4~1:tests/e2e/_f1_steward_attach_local.spec.js` 的 :186)。`syncSession` 是整份替换,一替换整条对话流被清空**退回空态**:真浏览器在原桩上量到 `.stw-msg` = 0 行、`.stw-msg.me` = 0、`.stw-msg.agent` = 0、`.stw-feed-empty` = 1。**不是「行还在只是没带件」。** CI 日志里连带报 `.stw-msg.agent .stw-bubble` 找不到,是同一件事的另一面。
  2. 真后端根本回不出这个载荷:`routes/steward_routes.py:139-160` 的 `get_session` 是逐条 `{**store.public_message(m), **attachments.files_of(by_message, m["id"])}` 投影的,**带 attachments**;它的 docstring 原话就是「用户气泡下面那行附件是永久留痕,刷新后不该消失」。
  3. 同族的 `tests/e2e/_b2m1_steward_local.spec.js:236` 早就按真后端回了完整消息流 —— **F1 这口是漏抄的一处,不是契约变了。**
- **为什么偏偏这一轮才炸**:`waiting_user` 算终态(`ai-steward-render.js:66-73`),而回执卡这条路**首拍就是** `waiting_user` —— 万能口是第一个走进 `acf177ab` 新分支的场景,`GET /sessions` 头一回被真打出去,那口漏抄的桩才显形。
- **修法**(`eaec02c4`,**只动测试文件**):新增 `sessionView(posts)`,按 `get_session` 的投影逐键还原(`id`/`role`/`text`/`ts`/`task_id` + 有件才带 `attachments` 键),从桩自己收到的 posts 现算,POST 与重建口共用一份 `sentAttachments()`。用例劈成两段量,免得「POST 回包」与「重建口」两条路混成一条断言。
- **反证三条**(都在真构建产物上改坏再跑,分别红在不同断言):POST 回包的 attachments 不挂到消息上 → 红在第一段;撤掉 `acf177ab` 那句终态 `syncSession` → 红在 `sessionGets` 那句;重建时把消息上的 attachments 吃掉 → 红在第二段。
- **闭环判据(已达成)**:`eaec02c4` 的 CI run `30557706501` = **success**,9/9 job 全绿。

**教训一(比根因本身值钱):21 道机械闸全 EXIT=0 + 11376 条单测 0 FAIL,一条都没抓到它。** 因为它只在 Playwright E2E 上显形,而这一轮**改完 bundle 之后没有人跑既有的 E2E 套件** —— 只跑了自己新写的验收脚本。`_f1_steward_attach_local.spec.js` 不在本轮改动文件清单里,就默认它不受影响;而 `acf177ab` 改的正是它依赖的那条 `loadTask` 终态分支。**改共享入口(`ai-steward.js` 这种被多条 spec 共用的文件)时,「我没改这个文件」不是安全论据,要按「谁依赖我改的这个函数」挑 spec 重跑。**

**教训二 · 桩漏抄真后端形状 = 假绿的又一型。** 桩比真后端**少给**东西时,测试反而更容易绿:渲染路径提前退出,该断的分支压根没跑到,直到某天有人真去打那口才炸。所以**桩的形状要按真后端的投影逐键还原,不能图省事回空**;反过来,判一条 E2E 红「是产品回归」之前,先拿真后端的投影核一遍桩,别默认桩是对的。写下这条教训的同一轮里,`_b2m1_steward_local.spec.js:174` 的 `/status` 桩就是同一个病(漏 `attachments`,回形针与输入区注脚从来没渲染过),`_artifacts/b2m1_steward/` 那批截图证据因此失真,已于 `46688aa6` 收掉。

### 3.1 ✅ 前半:查重差异已在产品里说人话(`81282c41`)/ 🛑 后半「我知道,还是推」判为暂不做

**前半(诚实展示差异)—— 已闭环。**
- **原缺口**:云端一个字都不认 `DUPLICATE_CONTENT_DIFFERS`,它掉进 `other` 桶 → 失败卡把小助手回执里写死的那句中文原样上屏,而那句话末尾写着「确认要再落一张就带 `duplicate_confirmed=true` 重推」——泰国会计既读不懂,也照做不了(产品里没那个按钮)。
- **改法**:`push_exception_classify.py` 新增 `DUPLICATE_DIFFERS_CODE` + `duplicate_differs` 桶 + `derive_duplicate_fix()`,从回执 `meta.duplicate` 取**结构化差异**;`push_log_queries.py` 列表与详情两处都挂 `duplicate_fix`(只挂一处的话,卡上写着差异、点开详情是一对空括号);`src/home/erp-log-card.ts` 接码表 + 字段名文案表。**刻意丢掉两样**:回执里写死中文的 `label`(字段名改由前端按机器名现翻)、`confirm_param`(产品里没那个按钮,透传下去下游迟早拼出一句会计照做不了的指令)。四语齐。
- **验到什么程度**:真栈 `curl /api/erp/logs` 拿到 `{"category":"duplicate_differs","duplicate_fix":{"docnum":"HP690715-001","fields":[{"field":"base_amount","in_erp":"1,070.00","incoming":"2,140.00"},…]}}`,载荷里确实没有 `label` / `confirm_param`;真浏览器中泰双语截图(`_artifacts/dupdiff_card/`),断言含 `not.toContain('duplicate_confirmed')` 与 `not.toContain('DUPLICATE_CONTENT_DIFFERS')`。8 条改坏反证逐条打进真产物再还原(含喂一份改名的假桥源码,验跨仓字面量比对真在咬)。
- **验收里真抓到一个断言看不见的缺陷**:第一版把「该怎么办」放句尾,`.erp-log-reason span` 的 `nowrap+ellipsis` 正好吃掉它。改成动作前置 + 数字证据后置并补 `title`。**这条是看截图看出来的,当时断言是绿的。**

**后半(「我知道,还是推」按钮)—— 🛑 判为暂不做。这是判断,不是没排期。** 三条理由按分量排:
1. **今天做出来必然是死按钮。**(**07-31 发版后仍然成立,理由从「没发」换成「没装」**:线上 `latest.json` 已是 1.1.58,但生产库里在跑的小助手报的仍是 1.1.55/1.1.56,**一台都没升**。)`duplicate_gate.py` / `write_gates.py` 落在 1.1.57 bump 之后的 `5d90b9a`,`git show 9a6303b:src/companion/duplicate_gate.py` 直接不存在 —— **现网在跑的小助手一个都不读这个键**,而且它没有键白名单,多发一个 `duplicate_confirmed` 会被静默忽略。会计点「还是推」→ 转圈 → 同一张红卡。这正是记忆 `dms-p1-closure` 记过的「文案说了没做」。
2. **唯一在产的那条路上,「还是推」是错答案。** `PRIOR_DOC_STILL_IN_ERP` 的 `prior_docnum` 是按同一个 `history_id` 回查上次成功推送写出的 DOCNUM —— **挡住的必然是同一张票**,「还是推」= 明知故犯记两遍。真正需要这个按钮的是 `DUPLICATE_CONTENT_DIFFERS`(两张不同的票撞同一个票号,泰国小供应商真会这样),而它今天在现网不存在(理由 1)。
3. **幂等:队列是 at-least-once,而这个键恰好关掉唯一兜住它的那道闸。** `agent_store.lease_pending` 的取活条件是 `status='pending' AND (lease_owner IS NULL OR lease_expires_at IS NULL OR lease_expires_at < NOW())`,租约 `_LEASE_SECONDS=120`,而一次真写(备份→写 DBF→重建 CDX)是分钟级(steward 侧轮询 deadline 给到 840s)。今天丢一次 ack 不出事,**靠的正是** `find_existing(REFNUM+SUPCOD)` 把重投挡成 skipped。更糟的是 `_find_existing` 返回**物理序第一条**活行:确认落第二张之后,重投拿到的仍是旧的那张 A,比出差异 → 已确认 → 再落第三张,**每丢一次 ack 多一张,无上界**。会计确认的是「多一张」,拿到的是「多 N 张」。撤销面也没出路:Pearnly 删不掉 Express 里的单,误点之后唯一的补救就是会计自己去 Express 删 —— 而那恰好是她一开始被指引去做的事。桥那侧 `bridge/store.lease_jobs` 同病。**「错了能不能被发现能不能改」这条红线上,这一条过不去。**

**真要做,先解掉这四件,缺一件都不许放开(07-31 第三轮:①② 的码写完了,③④ 仍拦着):**
- ① ✅ **投递语义改成至多一次 —— 码已落地(`72521aa1`)。** 带 `duplicate_confirmed` 的行在 `lease_pending` 里只认 `lease_owner IS NULL`(其余行的 at-least-once 一字未动);已领走又超时的由新增 `close_unacked_confirmed` 就地落 manual + 原因码 `confirmed_push_unacked`,同事务把 `ocr_history` 的推送状态镜像成 manual —— **不重投,也绝不躺回 pending 装作还在排队**。桥那侧同款 `write_gate.close_unacked_confirmed_writes`(落 `expired` 不落 `failed`,好让 `finish_job` 仍能收下桥迟到的 docnum)。云端翻成新错误码 `ERR_PUSH_UNCERTAIN`(**刻意不复用 `ERR_PUSH_EXPIRED`** —— 那句写着「上线后再说一次」,照做就是账上两张),失败卡四语齐。真 SQL 冒烟 7 条 + 反证(把两条队列都退回 at-least-once → 4 条当场红)。
- ② ✅ **写侧 `_find_existing` 改成返回最后一条匹配活行 —— 码已落地,但在 companion 仓且没发版**(工作树 `6c93b3d`,四份副本同改 + `duplicate_gate.head_row` 同口径)。**按铁律它今天在会计电脑上一行都没生效**:未 bump、未跑 `release.ps1`、未装机(§二.10)。
- ③ 🟠 **1.1.58(以及带 ② 的那一版)真装到会计电脑上**(§3.2:发版 ≠ 装机,要人点托盘)。**07-31 第三轮实测:现网仍是 0 台 1.1.58**,最新心跳 `2026-07-30 13:00:28Z` 报的是 1.1.55。这条纯粹卡在「有人去点」。
- ④ 🛑 **(07-31 第三轮新量出来)销项方向根本落不了第二张 —— 所以就算 ①②③ 全达成,这个按钮也只能对采购方向放开。** 销项 `DOCNUM` 就是票面票号,确认后落的第二张与第一张必然同号 → 读回校验按 `DOCNUM` 找到旧那张 → `NETAMT` 对不上 → `READBACK_VERIFY_FAILED` → 整体回滚(`make_backup=True` 下 ARTRN 活行数不变,回滚是干净的)。**会计看到的是一句像写盘坏了的错误,而不是「你确认的那张落进去了」。** 已钉成用例 `test_sales_confirmation_still_cannot_land_a_second_document`。要对销项放开,得先回答「两张不同的单怎么共存于同一个票号」—— 那是另一件事,不是加个按钮。

**判据现状**:原判据「能从产品界面走一遍『重推被提示 → 确认 → 真写进去』」**仍未达成**。三轮下来推进到:第二轮「重推被提示 → 会计看得懂、知道该动哪张」;第三轮「确认放行这条路的投递语义与幂等取值都改对了,而且丢 ack 时会诚实收尾而不是装作还在排队」。**按钮本身一个字没放开**,后半的判据挂在上面四个前置上。
**刻意没做的两件小事**:① 没给 `DUPLICATE_CONTENT_DIFFERS` 挂教程深链 —— 现成的 `exc-prior-doc` 讲的是「去删上一版」,和这条「去改原单 / 两张都是真的」不是一回事,`guide-links.ts` 文件末尾自己写着「挂教程只会把人引去一篇帮不上忙的文章」;要挂得新写一章 + 过 `check_guide` + 重出配图,是另一单。② 没做卡片专属的差异表格 UI(会动 `static/home-10-push-logs.css` → 触发 home.css 全量重打 + 又一个 `?v`),差异先并进摘要句,信息一条不少。

### 3.2 🟠 1.1.58 已发布,但现网一台都没装 · 「验在用小助手真更新」仍没做过(「线上是半截版本」那半已随发版关掉)
- **是什么**:`5d90b9a` 在 bump 之后,1.1.57 的线上二进制不含闸序修复(见 §二.3)。另外,铁律第三步「验在用小助手真更新」**从 1.1.56 到 1.1.58 一次都没做过** —— 需要真机。
- **07-31 已做掉的那半**:1.1.58 **已发版**(§3.3),线上 `latest.json` = 1.1.58,真下回来读 PE 确认 `ProductVersion=1.1.58`。「线上是个半截版本」这条到此关掉。
- **🟠 仍欠着的那半 —— 装机**:生产库只读实测(`erp_endpoints` 6 条 express,读 `config->>'companion_version'`),报过版本号的 3 条 = **1.1.55 ×2 / 1.1.56 ×1**,另 3 条从没报过;**没有一台是 1.1.58**。最新一次心跳 **07-30 13:00:28Z,那台报的仍是 1.1.55** —— 1.1.57 挂了三天它一直活着就是没升。
- **⚠️ 07-31 第三轮复测:这一天里一台都没变。** 同一条查询逐字重跑,版本号与心跳时间戳与第二轮**逐位相同**(最新心跳仍是 `2026-07-30 13:00:28.085475+00`,仍报 1.1.55)。**「发出去了」到今天为止对现网的效果是零** —— 别把「已发版」当成「已升级」写进任何汇报。
- **为什么不会自己升(读码确认,不是猜)**:`worker.py:_check_update` 每小时拉一次 `latest.json` → `gui_tray.py:_on_update_available` 只弹托盘气泡 + 把「更新到最新版」菜单项设为可见 → 真正下载静默装的 `_do_update` **只挂在 `menu.addAction(...).triggered`**(`gui_tray.py:339`),全文件没有第二个调用点。**这是 opt-in 设计,不是故障。**
- **通知链本身是通的**(拿真 prod 复核过):`check_update('https://pearnly.com', current_version=v)` 对 1.1.55 / 1.1.56 / 1.1.57 都返回 `{"version": "1.1.58", "url": "…?v=v1.1.58"}`,对 1.1.58 返回 `None`。所以卡的确实只是「人点那一下」。
- **卡在哪**:本机不能拿在跑的生产小助手来试(互斥量 + APPDATA 那套坑,见记忆 `companion-gui-shots-without-touching-live-instance`)。
- **怎么继续**(二选一,是个产品判断不是技术难题):① 通知会计逐台点托盘「更新到最新版」;② 另开单把 opt-in 改成静默自动装 —— **那是产品行为改动**,得先想清楚「正在写 DBF 时能不能重启」和「会计不知情就被换掉进程」这两件。
- **判据**:到任一会计电脑托盘图标右键/关于里看到 **1.1.58**(**不是看安装目录文件时间**)。在这之前只能说「发布物已上线且可下载」,**不能说「全所已升级」**。

### 3.3 ✅ 已闭环:`release.ps1` 备份错位一版 + 发版零校验 —— 修复已在真发版里验过
- **原问题**:`$stamp = $ver` 用的是**新**版本号,所以 `.bak-1-1-56` 里装的其实是 1.1.55(61,831,055 字节,实测);出事回滚会推错一版。且全过程没有任何自动校验 `latest.json.version == version.py VERSION == installer ProductVersion`,**漏 bump 会静默发一个假版**。
- **已改成什么**(`515d48c` 脚本两处修复 / `7e43877` bump + RELEASE.md):
  - `$stamp` 改成读**线上 `latest.json` 当前发布的版本号**(读不到就退时间戳),备份名从此对得上被换掉的那一版。
  - 补三处硬断言,任一不等即 `throw`:① 安装包内嵌 `ProductVersion` == `$ver`(打完就断,不合格不碰生产)② 回读线上 `latest.json` 的 `version` == `$ver` ③ 线上安装包字节数 == 本地字节数(`scp` 退 0 不代表传对了)。
- **2026-07-31 真发版跑过一次(Zihao 拍板)**,五步全过、退出码 0,脚本自己打的收尾行:
  `OK  version.py=1.1.58 | installer ProductVersion=1.1.58 | prod latest.json=1.1.58 | 61848982 bytes`
- **`$stamp` 修复的真机取证(这是它唯一一次能被验的机会,做了)**:
  - 发版前先把线上在跑的 exe 下回本机读 PE:`ProductVersion=1.1.57`,61,838,237 字节,sha256 `a63c9480…`。
  - 脚本运行时自己打印 `prod currently publishes 1.1.57 -> backup as .bak-1-1-57` —— 取的是**线上实际在跑的版本号**,不是新版本号。
  - 发版后 `sha256sum` 线上 `PearnlyCompanion-Setup.exe.bak-1-1-57` = `a63c9480…`,**与发版前那个 1.1.57 逐字节相同**。名字和里面装的东西第一次对上了。
- **⚠️ 一个要知道的副作用**:线上原本就有一个**错名**的 `.bak-1-1-57`(61,830,845 字节,按旧错位规则里面装的是 1.1.56),这次备份把它**覆盖**掉了(`cp` 直接盖,`.bak-*` 总数仍是 62)。结果是这个名字从此名实相符,代价是**生产上不再留有 1.1.56 安装包的副本**。回滚目标不受影响 —— 1.1.57 完整保留在 `.bak-1-1-57` 里。**其余 59 个错位 `.bak-*` 一个没动**(Zihao 没点这个头)。
- **验证到什么程度**:`pytest tests bridge/tests -q` = 1001 passed / 26 skipped / 退出码 0(须带 `PEARNLY_APP_DIR`),与基线一致;三处新断言**已在真发版里全部跑到并通过**,不再是「没验过的代码」。



> **3.4 ~ 3.7 · 3.19 · 3.20 一条都没动(07-30)。** 共同原因是同一个:这六条全要连 Accserver。
> 本轮本机一直在 `172.20.10.3`(手机热点网段),`192.168.0.11:445` 不可达,桥装机 / 四类单据真靶场 /
> 靶场脏数据还原 / Express 界面锁验证**物理上做不了**,不是排期没排到。等回公司网段一次性做掉。

### 3.4 🟠 小助手写路仍会往已关账期写单(桥已拦,两边行为不一致)
见 §二.4。取证:靶场把 `LOCK7='Y'` 后 `HP690715-001` 照样落盘,HP7 桶 535→1605。

### 3.5 🟠 桥从没装到任何真机 —— 跨网写盘一次都没在真拓扑上跑过
- **现状**:桥 v0.5.0 只在开发机跑过。Accserver(192.168.0.11)是 Server 2003 装不了 Python;192.168.0.212 只开 445。**桥要装第三台常开 Win 机**。
- **未验**:真机跨网写 DBF(现役小助手已生产实证可行,但那是小助手不是桥)。
- **另**:桥 exe 当前是 3.11 构建 = **Win10+**;覆盖 Win7 需在装 32 位 Python 3.8 的机器上跑 `build_bridge.ps1 -RequireLegacyWindows`,依赖侧已核实,PyInstaller 侧官方只承诺 Win8+,**未实测**。

### 3.6 🟠 四类新单据从没上过真靶场
P2-B 的真账验证全部在**镜像副本**上做(`D:\pearnly-erp-lab\_mirror_20260726\` 与 scratchpad 副本)。TEST 靶场跑过的只有采购/销项那条老路。四类单据的真靶场写冒烟是空白。

### 3.7 🟠 生产默认的 `make_backup=True` 路径在真账套上没验过
靶场全部写入用 `make_backup=False`,因为 True 会在 `69EXP\` 下建 `test_pearnly_bak_*` 兄弟目录,越出授权边界。**写前备份 + 失败 `_restore` 回滚这条路径只有合成账套单测覆盖**。

### 3.8 ✅ 大脑循环考卷有两个盲区,把断掉的能力验成了绿(07-30 关掉盲区 · 07-31 真库那条尾巴也关掉)
- 串两步那题(`tests/unit/test_steward_brain_loop.py:21-31`)第二步用 `push_log_query` 且 `args={}` —— 那工具的 `client_name` 是选填,正好绕开唯一会炸的地方,锁住的只是「第二次提示词里有没有第一步的结果」,不是「第二步用不用得上第一步查出来的名字」。
- 成本闸几题靠 Harness 注入现成 gate 判绿,**从没验过 `steward_cost_entries` 真的会累加** —— 台账恒 0 一题都碰不到。
- **补法**:① lookup 之后拿查出来的全名调一个 `client_name` 必填的工具,断言工具真被执行;② settle 之后从台账读回来断言 > 0。
- **落地**:① `ChainTests` 第二步改成 `period_invoices`(`client_name` 必填),新增 `test_the_second_step_really_runs_on_the_name_the_first_step_found` 断执行时收到的 args,并配反证 `test_without_the_prior_result_corpus_the_second_step_cannot_run`;② Harness 新增 `ledger=` 台账模式(`precheck/reserve/settle` 全走真 SQL 分支),`CostTests` 新增两题:一题断三次调用留三行且全部 settled 合计 ฿0.36,一题让任务级封顶从**真累加**触线(不再注入 gate)。台账假游标从 `test_steward_authz.py` 搬进 `_steward_loop_fakes.LedgerCur` 单份共用。四轮改坏反证(接地语料清空 / 第二步不传 `client_name` / settle 不落库 / reserve 不占坑)逐条确认新断言会红。
- **一处更正**:补法① 的**能力覆盖**在 `tests/unit/test_steward_loop_grounding.py:69-90`(commit `06de18ff`)早已存在;§3.8 认定的是 `ccf50b57` 那两个文件里的盲区。本次修的是「47 题考卷本身不再自证虚假」,不是新增能力覆盖。
- **✅ 最后那条尾巴 07-31 也关掉了(`f41db5be` + `b4965755`)**:`tests/unit/test_steward_budget_pg_smoke.py`(8 条)在**真 Postgres** 上验掉了假游标证不了的三样,并且**已经在 CI 里跑着**(§二.7 的 `pg-smoke` job,run `30579711972` 日志 `Ran 13 tests / OK`,无 skip)。
  - **精度**(3 条):12 次 `Decimal('0.070001')` 的 reserve→settle,读回合计逐位 == `0.840012` 且 `str()` 保住 scale 6,同时断言同一串 float 加法**确实**漂到 `0.8400119999999999`(不漂就说明这条用例证不出精度);第 7 位小数四舍五入不截断;整数位越界(precision 12 − scale 6 = 6 位)走裸 SQL 落不进去。
  - **`pg_advisory_xact_lock` 真串行**(2 条,自带反证):两会话同租户同时 `reserve`,`decide` 换成先 sleep 0.6s 的版本把停点卡在「读完合计、还没写占坑行」之间。锁在 → 只放行一路、台账 1 行 ฿0.300000、**两路墙钟跨度 1.24s ≈ 2×窗口**(串行化的直接证据,只看「谁被拒」判不出是锁起作用还是碰巧排队);把那句锁换成空查询 → 两路全放行、合计 ฿0.600000 破 ฿0.5 线、跨度 0.62s。**竞态真复现得出来,所以前一条不是空的。**
  - **RLS 真隔离**(3 条,自带反证):最小权限连接下 A 看不见 B 的行、`WITH CHECK` 拦跨租户 INSERT;把表 `DISABLE RLS` → A 立刻读得到 B 的行。
- **闸本身怎么防「绿得没意义」**:连不上库时用例是 clean skip、`unittest` 退 0 —— 那正是本仓栽过两次的形态,所以 CI 那步**显式把 skipped / 零发现判成红**(死端口实测 `::error::4 条被 skip` exit 1)。测试只认 `PEARNLY_PG_SMOKE_URL`,**绝不回落 `DATABASE_URL`**。放 `tests/unit` 而不是 `tests/integration`,理由是 CI 只 `discover -s tests/unit`,放那边等于换个地方继续跳过。
- **仍未覆盖(缩小到这两条)**:① 走的是测试自带的 `_tenant_cursor`(`SET LOCAL ROLE` + `SET LOCAL app.current_tenant_id`),被验的是 `core.rls.apply_tenant_rls` 生成的那条 policy,**`core.db.get_cursor_rls` 本体的切角色机制没进 CI**(它由 `tests/integration/*_rls_real_tables.py` 覆盖,而那批从没在 CI 跑过);② `tests/integration` 那 70 份 `require_db` 用例仍未进 CI(§二.7 末段)。

### 3.9 ✅ 授权卡参数行摆着未翻译的机器键和 UUID(2026-07-30 已闭环 · `a5116faf`)
- **原问题**:`ai-steward-authz-render.js` 直接 `'<dt>'+esc(r.k)`,中泰界面上都显示 `direction / history_id / invoice_no / account_set / seller_name / total_amount`,`history_id` 是原始 UUID。违反 `services/steward/copy_erp_push.py` 文件头自己写死的「卡上摆一串 uuid 等于让人闭眼签字」。
- **改法**:参数行改由后端出 `display`(现算的人话 label + value),前端 `argEntries()` 优先吃它;原来那条按 `args` 键名直出的分支**保留为老卡回落**(已经发出去的授权卡里没有 `display`,删了会让历史卡变空)。
- **同族一并做掉**:`ai-steward-render.js` 红条尾部的 `error_code` 不再原样上屏,收成一个可复制的按钮(`stw-copy-code`)。
- **⚠️ 顺带带出的新缺陷**:这个复制按钮连点两次会卡在「已复制」——`ai-steward-actions.js:77` 的 `copyCode` 里 `var prev = btn.textContent` 是点击当下取的,1500ms 内第二次点就把「已复制」当成原文存了回去。`ai-billing.js:293` 的 `copyAcct` 是同一份写法、同一个毛病。**两处都没修。**

### 3.10 ✅ 402 的三个数没变成人话(2026-07-30 已闭环 · `750b5487`)
- **原问题**:接口体里 `balance` / `estimated_cost` / `pages_used_this_month` 都在,但 `static/` 全仓零引用,`ai-fail-render.js` 命中 402 后只挂一条固定文案 —— 用户看不到差多少钱。
- **改法**:后端 402 体补 `shortfall`(还差多少),`ai-fail-render.js` 把四个数拼成三句人话(这批要多少 / 账上还有多少 / 至少充多少),主 CTA 深链 `#/settings?focus=billing`。
- **验到什么程度**:真栈真触发(7861 起 `PEARNLY_WORKORDER_BILLING=1`,三张真语料进真收料口,8 次全拿 HTTP 402,后端体逐次是 `{code:insufficient_balance, balance:0, estimated_cost:4.5, pages_used_this_month:9, shortfall:4.5}`,没做 route stub)。四语 × 桌面/移动 390 共 8 张截图,68 条机器断言全绿,配 5 条改坏反证。
- **⚠️ 但这张卡上还剩两条没修**:① en/ja 是语言沙拉,7 句里 5 句中文(§二.9)② 「去充值」按钮移动端 38px,低于本仓自己的 ≥44px 触控标准。

### 3.11 ✅ 单步成本上沿超设计估算,预留额偏低(2026-07-30 已闭环 · `f82c5aea`)
- **原问题**:实测单次模型调用 ฿0.1315–0.3395,而 `budget.py` 注释写「฿0.13-0.25」、预留额定 ฿0.25,低于实测最坏单步 —— 注释说的「宁可多占一点」事实上没做到。
- **改法**:`_DEFAULT_CALL_RESERVE` 抬到 `0.30`,顶注改成实测口径(62 次真调用 ฿0.13-0.34,p95 ฿0.20)。฿0.30 是双向卡出来的:下沿要 ≥ 最坏一步 ฿0.339535 × 0.85,上沿要满足 `reserve × MAX_CALLS ≤ 任务上限`;0.25 与 0.40 两头都真红过。

### 3.12 ✅ 时序缝:任务首拍即终态时,管家收尾追写要等用户离页回页(`acf177ab`)
- **原问题**:`loadTask` 首拍 GET 若已是终态则不启动轮询,而 `syncSession` 只挂在轮询的 `onTerminal` 上。
- **改法**:首拍即终态时补一次会话同步,并加 `fromSync` 守卫防止「同步→再同步」自激。
- **验到什么程度**:E2E 修前必红(`toHaveCount(2)` 实收 1)、守卫反证也真红过(`hits.session` 收到 2)。**但 POST /messages 那一跳是桩的** —— 没真跑一次大脑让工具在首拍之内自然收口(几十秒且烧额度),所以「工具变快后会显形」这个原始场景是模拟的,不是自然发生的。
- **连带**:这一句 `syncSession()` 让 `GET /sessions` 头一回在万能口那条路上被真打出去,把 F1 里一个漏抄真后端形状的桩照了出来(CI 红,§3.0)。**产品侧没有代价** —— 改的是桩。`eaec02c4` 落地时把 `acf177ab` 这两条用例连着 `_b2m1_steward_local` 14/14 一起重跑过。

> **3.13 ~ 3.21 里 3.16 已于第二轮闭环;第三轮再关掉 3.13 / 3.15 / 3.17 / 3.18 / 3.21,3.14 改判为暂不做。**
> 仍原样欠着的只剩 Accserver 那两条(3.19/3.20)—— 逐条核过对应文件在 `dc4c684e..HEAD` 的改动清单里一次都没出现。

### 3.13 ✅ 接地闸子串宽松面已收紧(2026-07-31 闭环 · `ea110868`)
- **原缺口**:`services/agent/slots.py` 的接地闸是裸子串比,`107` 能靠子串蹭进 `10,700`。这道闸是 LINE Agent 与管家共用的安全核心。
- **改法不是一刀切,是分三类定判据**:① **数**(金额/税率)= 字面比保留,但两端不许落在一个数的内部(千分位与小数点都算内部);字面不中再按 `Decimal` 值比,排版差异照旧放行。② **标识符**(税号/身份证/电话)= 新增数字串比,票面带杠写法与 13 位数字互通,10 位起才算一个号。③ **名字/关键词** = 端点不是数字就照旧子串比 —— **泰文没有词间空格,收紧这条会打死 `client_name` / `keyword`**。
- **判据是拿真语料量出来的,不是推的**:4567 条去重样本全部取自真账套(SM 销采单据 / 销项税登记簿 `acvatsaled` 5 月 779 单 / Express 六家账套 `Moritomo_58MORI` 的 `ISVAT.csv`),逐条跑「原样交回 / 剥千分位交回 / 税号带杠↔13 位 / 削一位」四问。**误伤 = 0**;有毒侧「削一位且值真的变了」旧判据放进来 4021 条,新判据 **0 条**。
- **装闸当下先在未修的代码上跑过**:8 条新用例红(6 条毒样本被旧判据放行 + 2 条真税号被判成编造),修完 32 条全绿;相关模块串跑 868 条 OK。
- **真语料还扫出两个没预料到的边角并当场补**:① 值以小数点开头(`9.00` 削成 `.00`)时左边界判定失效 → `head` 判据从 `isdigit()` 改成落在 {数字, 千分位, 小数点} 里;② `_ID_RUN` 里的 `/` 只会把「日期+序号」黏成一个假标识,去掉后真标识符一条没少。
- **收紧方向可证明是单向的**:新的字面命中集是原子串命中集的真子集(`_literal_hit` 只在原 `value in blob` 上加边界否决),值比那条一字未动;唯一新增的放行路是标识符数字串比,而它要求把用户打过的 ≥10 位数字一位不差按原序复述出来。
- **仍没做的两件(写清楚,别当已收)**:① **带千分位的负数接不了地** —— 用户打「-10,700」、模型交回「-10700」会被判成编造;数值集不收符号,而「-」贴着数字时是负号还是项目符号/连字符,单看文本判不出(`2026-07-30` 里是连字符)。判不准就把射程写窄。② **判据是从值的形状推的,不是从槽声明读的**;真要按 `SlotSpec` 上的 `kind="identifier"/"amount"`,得同时改 `services/agent/contracts.py`、`manifest.py` 十来个槽和 `steward/registry_slots.py` 三个工厂,blast radius 覆盖 LINE Agent 全部工具。
- **同族仍未做**:LINE postback 烧 steward token 这个反向没堵(管家侧已堵),LINE 各消费点属高敏钱路径需逐点定 kind。

### 3.14 🛑 判为暂不做:OCR 场次票 Time in/out —— 提示词那步会拿抵扣凭据换一天入账日
> **⚠️ 过期指令已划掉**:本节原文写的「加进 `invoice_grouper._SCALAR_FIELDS` 白名单」是 **no-op** —— 该白名单自 `814fe657`(2026-07-05)起改成 `ThaiInvoice.model_fields` 减黑名单**自动派生**,新字段自动随行。已实跑验证(`'time_in' in invoice_grouper._SCALAR_FIELDS` → True,grouper 一行没改)。**照它去改是白费,别做。**

- **先把真语料盘清楚(不是估的)**:`SM-照片语料/A_客户给的原料` 104 张全部缩图、其中 91 张逐张读图确认。印 `Time in:` / `Time out:` 两行的 Ocha 场次票共 **6 张**;**真正跨日的只有 1 张** = `02000139`(IMG_2574,in 27-05-2569 16:19 → out 28-05-2569 08:29);跨月 0 张。其余 Ocha 票只印一行 `Time:`(带完整日期),采购票是 A4 全额税票,均无场次时刻。
- **实测出比本单原文更硬的结论**:原文说「模型没抄进去,规则就抽不到」;真跑下来是**一次都没抽到过** —— 6 张场次票 × 12 次运行,`date_raw` 恒为 `'27-05-2569'`(模型只抄 Time in 那行)、`notes` 恒为 `'Thank You Ka~'`,`doc_date._SESSION_LABEL` 从未命中。**`56d7a4b2` 把两个键换成解析票面文本,只是把一条死分支换成了另一条死分支。**
- **真值锚定**:MR.ERP 销项税登记簿把 `SW02000139` 记在 `vat_date=28/05/2569`(税前 439.25 / VAT 30.75,与票面一致),即会计口径确实取 Time out。**但同批另外 5 张(进出都在 27 日)也全记 28/05**,说明那个日期是批量键入日 —— **这份语料只有这一张能当日期口径的真值,别拿另外 5 张反推口径。**
- **🛑 为什么判不做(这是本条的核心)**:提示词那一步**有效但代价不可接受**。两种放法各自把一张 A4 采购税票的税号稳定读错 —— 规则放进 CRITICAL RULES 编号表:`IMG_2640` 的 `buyer_tax` 改前 4/4 读对 `0105567178203`,改后**稳定读空 7/7**(人工核对原件裁切图确认票面确实印着那串,即 `''` 是错的);把同一句挪到提示词末尾:`IMG_2640` 回正 3/3,却换成 `IMG_2643` 的 `seller_tax` 被读成买方自己的税号,4 次错 3 次(改前 4/4 全对)。**税号是进项能不能抵的凭据,拿它换「104 张里 1 张的入账日差一天、且申报期不变」不划算。**
- **病灶隔离过,不是甩锅给 LLM 抖动**:只留两个 schema 键、删掉规则 → 两张税票全部恢复,但 `IMG_2574` 的两格要么空、要么只给裸时钟(无日期,`parse_invoice_date` 解不出)→ **只补字段等于零收益**。抖动对照:`IMG_2568` 的 `invoice_number` 在两种写法间来回 = 真抖动;`IMG_2640` 的 `buyer_tax` 在两臂内各自 3/3 一致、跨臂完全分离 = 真回归。链路上 `direct_read.py` 直接 `from .layer2_prompts import _SYSTEM_PROMPT`,**直读主路与 Vision/L2 路共用同一份提示词,改一处即全量生效,回归也就全量生效。**
- **前置条件(真要闭合先解这个)**:提示词里关于税号只有 `5. TAX IDs: Exactly 13 digits` 一句,**没有任何「同一张票上两行同名 `เลขประจำตัวผู้เสียภาษี` 怎么分买卖方」的消歧规则**。先解掉它,再连场次时刻规则一起在更大的采购票样本上评测。
- **本轮只交付无回归、可验证的部分(`c50a931c`)**:① `services/ledger/doc_date.py` 文件头写明实测结论 + 后果边界(`02000139` 现按 27 入账 vs ERP 的 28,同属 5 月申报期故未错期)+ 两种修法各自的回归数据,**零逻辑改动**;② **金标去伪** —— `_ledger_golden.py` 里 `GOLDEN_SESSION_RAW` 原值是编的(大写、空格、斜杠日期),换成真件原文(小写 + 冒号 + 连字符)并显式标注它是合成夹具;③ 新增**现状诚实闸** `test_ocr_shape_today_cannot_reach_the_session_rule`,喂线上真拿到的形状断言结果就是 `2026-05-27/printed` —— **它红了就等于上游终于把场次时刻送进来了**,那时要连着 doc_date 文件头一起更新。
- 花掉的钱:全程约 112 次真调 Gemini(economy 档),约 **฿11-17**。

### 3.15 ✅ STCRD 明细行不写 REFNUM 已修 —— 而且真正的缺口在在产的那一份(2026-07-31 闭环)
- **两问都有实测答案**。① **填充率**:镜像 17 套(EXP67/68/69)STCRD 活行 59799 —— 采购单别 `RR` 18168/18533 = **98.0%**、`HP` 147/147 = **100%**,且与 APTRN 单头 `REFNUM` 同值;销项 `IV` 0/9266、`HS` 0/5492、`HL` 0/3076 **全空**(所以「采购行写、销项行留空」是照真账的形状,不是拍的)。② **有没有报表按它取数**:有,而且是**随程序发的标配库存卡** —— 全镜像报表模板/程序二进制里 `STCRD->REFNUM` 落在 39 个文件、12 种模板(`STCRDL.RWT` / `STK-CARD.RWT` / `STOCK001.RWT` …),其中 eng 21 份、thai 18 份是标配不是客户自定义。模板原文 `DOC=IIF(STCRD->REFNUM # '  ', STCRD->REFNUM, STCRD->DOCNUM-'-'-STCRD->SEQNUM)` —— **我们留空,那一行在库存卡上就只剩内部单号,同页所有原生行显示的却是供应商票号。**
- **真正的缺口不在桥**:桥那侧(`bridge/writepath`)2026-07-27 就写了 `REFNUM`,**而跑在会计电脑上的 `src/companion` 这份连这个键都没有**,连合成表规格里都没这一列。已把整套(4 个行构造器 + `write_stcrd_detail` + 采购两处调用点 + 库存入库路)从桥抄进 `src/companion`,合成 STCRD 补 `REFNUM C(15)`。
- **表结构先验过再写**:17/17 套 STCRD.DBF 均为 45 列且含 `REFNUM C(15)`,缺列账套 0 个 —— 写这一列不会在任何一套真账上炸。
- **反证先红后绿**:改动前 `pytest tests/test_purchase_detail_refnum.py -q` **4 failed**(3 条采购断言 `KeyError: 'REFNUM'`、销项那条同因),改完 4 passed;每条先断言明细行数非零,免得「一行没写」也算通过。companion 全量 420 passed / 23 skipped,中途逮到一条真回归(期初行 gold 少一格 `REFNUM=''`)已按「期初无对手方票、恒空串、落盘等价」补上。
- **⚠️ 但它在会计电脑上还没生效**:这笔在 companion 仓工作树 `16c2236`,**未推、未 bump、未发版**(§二.10)。按铁律「不 bump = 改了白改」。

### 3.16 ✅ 交付物下载链已验到真下到文件(2026-07-31 闭环)
- **原问题**:库里 `artifact_path` 是手工种的、指的文件本机不存在(而且路径形如 `.../b2000000/c6c7144a/pkg/`,连工单目录都不在里头),端点必然 404 —— 这条链此前只证明了「路由在、鉴权在」。
- **真跑了什么**:在真栈上走产品自己的路把 `c6c7144a`(Sister Makeup Steward · 2569-06)推到 review —— 补一笔人工销项(数字取语料答案 `MR.ERP SM 2569-06 现金销售 609 单`:税前 953,509.68 / VAT 66,745.59)→ 引擎跑到 package → `deliverables/v2` 出 8 个真文件;再走「驳回重做」重跑一版验版本递增,读侧确认取的是最新版 `v3`。
- **真下到什么**:7 件交付物逐件走 `GET /deliverables/{kind}` 全 200,响应字节的 sha256 与盘上那份**逐件相等**;`Content-Disposition` 带 RFC 5987 名(`Sister Makeup Steward_2569-06_แบบร่าง ภ.พ.30.md`);月度报表 xlsx 用 openpyxl 真打开(5 个工作表、A1=`หมวด`)、PDF 有 `%PDF-` 头与 `%%EOF` 尾。真浏览器在交付包页点「下载」也真触发 download 事件、文件真落盘 2175 字节且与磁盘 sha256 一致(截图 `tests/e2e/_artifacts/deliverable_download/`)。
- **反证**:匿名 401 / 篡改 token 401 / 未知 kind 404 / 别的租户的工单 404(对照文件确实在盘上)/ `artifact_path` 指到工单目录之外 404。
- **钉成测试**:`tests/integration/test_workorder_deliverable_download_real.py`(6 条,自建夹具真跑整条流水线出真文件,`require_db` 自我关闭)+ `tests/e2e/_deliverable_download_real_verify.spec.js`(真浏览器点下载,`PEARNLY_E2E_LOCAL` 守卫)。两者都喂过毒样本确认会红:路由改吐 200 HTML 错误页 → 红;关掉取盘目录内含校验 → 红;同时抹掉「租户前缀入路径」与 store 的 tenant 谓词 → 跨租户那条才红(说明隔离是两道独立防线)。
- **顺带发现(未修)**:本机开发库 `work_order_{events,items,deliverables}` 三条外键实际是 `RESTRICT`,而 0059 迁移与 `schema.py` 写的都是 `ON DELETE CASCADE` —— 本机建库路径与迁移有漂移。**生产是哪一种没查**(本机连不上生产库)。

### 3.17 ✅ 三处棘轮基线:收紧两处,第三处实测没得收(2026-07-31 闭环 · `7586c296`)
- **`check_ui_consistency` 480 → 219**、**`check_theme_responsive`「暗夜:3 位 hex」246 → 242**。
- **`ui_design_lint`「小固定 max-width」没收:实测 153 = 基线 153,没有可收的格子。** 本单原来记的 152 是 07-27 那一刻的读数 —— `git log -S` 定位:152 只在 `77d8b2d8` 存在过,同日 `4f7794b6` 起就是 153,多出来的那处是 `static/ai/ai-steward-attach.css:172` 的 `max-width: 220px`,此后五笔提交一直是 153。**要收得先还掉那处,而那是用户可见版面,属 UI 改动要配真浏览器验收,不在本条范围。**
- **「会不会撞设计系统那条线」是量出来的,不是猜的**:设计系统最后一笔停在 07-24 且已在 master;当前唯一未合并又碰这两道闸扫描面的是 `feat/pos-barcode-scan`(今天的 `167c1c16`,改了 `ui_design_lint.mjs` + 60 个 static/src 文件)。`git archive` 把它的扫描面导出来跑同样两道闸,读数与 master **逐项相同**(219 / 242)—— 收到实测值不会误伤它。
- **反证证明那 261 格与 4 格松弛等于没装闸**:同一份毒样本(1 个 3 位 hex + 3 个裸 6 位 hex 塞进被扫 CSS),**收紧前两道闸都 exit 0 一声不吭**,收紧后双双 exit 1;还原后 sha256 逐字节一致、两闸回绿。

### 3.18 ✅ 那个对不上的 APBAL 组合追到底了 —— 是客户数据残骸,不是我们算错(2026-07-31 闭环 · `8dc5cfb0`)
- **结论不是「疑似」**:对不上的原因是客户账套里**一行没有主档的 APBAL 残骸**,既不是付款冲销、也不是跨年结转。该行(`69ASIAMI` 与 `68ASIAMI` 的同一个 `ซ007`)**APMAS 一行都没有(含软删)、APTRN 一张单都没有**,19 个桶却有钱。
- **两条排除都是手算的**:RR 本年合计 66189.52 **恰好等于** PS(付款)合计 → 那家供应商往来完整且已结清,付款根本没丢;跨年结转会落 `BEGBAL` 而不会散进 12 个月桶。
- **成因定住**:该账套 `ซ001..ซ010` 里**独缺 `ซ007`**,前后两档建档日期 2012 与 2023、`CREBY` 是 Express 自己的 `BIT9`/`MEAW`/`BIT5` —— 一个真实存在过的供应商,主档与单据被删掉、余额行留下。全镜像 17 套 8746 个 APBAL 主键码里**只有这 1 个**;它的金额在本账套的 APBAL/APTRN 里再无第二处(排除了改码后的孪生行)。
- **判为不改代码,而且这个判断是跑出来的**:先怀疑「发号只查 APMAS、够不到孤儿余额行 → 新供应商会白捡一屁股余额」,于是把 `master_create._next_code` 原样跑在真码表上 —— 它取同前缀 max+1(该前缀 max=`ซ010` → 下一个 `ซ011`),**200 次发号一次都够不到卡在洞里的 `ซ007`**。只有当孤儿码**大于**当前 max 时才可能撞上,17 套里没有这种。**所以不加闸、不改发号。**
- **落成两件可复用的东西**:① 交叉验证脚本加「先剔掉没有 APMAS 主档的 SUPCOD」,剔完 **HP 桶 36 个组合 0 个对不上**;② 这个坑写进 docs 30 号文的「坑/纪律」,下一个人拿 APBAL 反推单据时不会再把客户的数据残骸报成「我们对不上」。
- **离线分不出的那一半,如实写**:`ซ007` 是「会计删档」还是「从别的账套导入」留下的,两种路径痕迹一模一样,要分辨得开 Express 看操作日志 —— Accserver 不通,做不了。
- **⚠️ 顺带发现、没追(别当已查)**:`68ASC` / `6902ASC` 两套上 **RR 桶 1860/2160 个组合对不上**(同一供应商有 2–3 行 APBAL、且存在 `NETAMT=0` 的单)。这不属本条(本条明确是 HP 桶),看数据像是 APBAL 主键不止 `SUPCOD` 一列、或那套账有历史迁移残留 —— **没查,不给结论。要追得先把 APBAL 的真实主键搞清楚。**

### 3.19 🟡 Express 界面在 `LOCK='Y'` 时到底拦不拦 —— 离线证不了
已在 `fiscal_period.py` 模块 docstring 里显式写明这条证不了、以及为什么不影响实现(桥无条件自己拦)。等 Accserver 开机后可实证。同批未验的还有:「Express 界面认不认这张单」「账套锁 waiting_lock 与软删残骸」。

### 3.20 🟡 靶场留了脏数据没清
`\\Accserver\d$\ACCOUNT\69EXP\test` 现有 4 张 690727 采购单、S001 的 HP7=4280 / RR7=2140、T001 新活行 HP7=535;另有软删护栏测试把 T001 原 APBAL 行标成软删(total 692 / live 691)。
**还原法**:直接用 scratchpad `accserver/test_backup_before/` 覆盖回去。

### 3.21 ✅ 未指定过账去向的票能就地补选重推(2026-07-31 闭环)
- **原判断有两处不准**:① 「只差前端加个按钮」偏乐观 —— escalate 落的是 `status=manual`,而裸重试按钮的条件写的是 `status==='failed'`,那张卡上**一个可点的东西都没有**(真浏览器实测按钮列表 `["查看详情"]`);② 「retry 已能收 posting_kind」不成立 —— `/api/erp/logs/{id}/retry` 没有请求体,只能从 `history` 那一列读。
- **改法**:mapper/sales_mapper 的 escalate 带上票面商品行 → `classify_push_exception` 开桶 `posting_kind_needed` → `derive_posting_fix` 出 usage+items → 失败卡渲第四张修复面板(商品行 / 选了会怎样 / 推成功后改不了)→ `POST /api/erp/logs/{id}/express-posting-kind` 归一、写回 `ocr_history.posting_kind`、重推。写回票而不是只当本次入参:声明跟着票走,否则这次点通了、轮到自动重试仍会 escalate。
- **验到什么程度**:真浏览器跑 `static/dist` 真产物,`tests/e2e/_postkind_fix_local.spec.js` 4 条绿;换回旧 bundle 4 条全红。POST 请求体逐字 `{"posting_kind":"stock"}`。后端反证:摘掉 classify 分支红 2 条、摘掉 mapper 的 `line_item_names` 红 3 条、四种脏值全 400 且不写库不重推。E2E 夹具由 `scripts/gen_postkind_fixture.py` 跑真 mapper+真分类器生成,单测每次重算比对。
- **顺带修**:卡上多一个按钮把手机端「买方/目标系统」两列从 132px 挤到 40px(名字只剩两个字)· 760px 下带修复面板的卡把操作区放到自己一行 → 206px。这条对已有三张修复卡同样生效。
- **上游那件事(判断了,没动)**:过账去向控件留在上传第 1 步是对的,不该挪到第 4 步 —— 开了自动推的客户在识别完成那一刻就已经推出去了,走不到第 4 步的传参(`dms-intake-invoice-recognize.ts:60` 写着)。真正的缺口是第 1 步不提示:账套指纹此时已经知道这家是永续账套、不选就必然全批转人工,却一声不吭;而第 4 步那个 `posting-preview` 的 escalate 提示来得太晚(OCR 费已经花了),自动推的客户还根本走不到。**留作单独批次**:第 1 步按画像给一句就地提示(仍不预选 —— 预选等于替用户声明,会绕过安全网)。

### 3.22 ✅/🟠 任务板第 6、7 条(三轮各做掉一部分)

**第 6 条「全功能真浏览器视觉验收」—— 🟠 三轮做完仍不是全覆盖。**
- 三轮合计 **383 张**入库截图(`git ls-files` 数的:`_artifacts/closeout_visual/` 107 + `closeout_visual_2/` 171 + `closeout_visual_3/` 105),四语 × 桌面 1280 / 移动 390,真语料。
  - 第一轮 107 张:只覆盖 `/ai` 的 10 个面(定点 16 / 走查 83 / 402 复验 8),揪出 7 条存量缺陷(§八.3)。
  - 第二轮 171 张 / 211 个「面×语×端」组合:`/ai` 补齐剩下 8 个面、`/home` 9 个面、`/dms` 4 个视图、`/pos` 公开页 + `/cashier` 未绑店、`/admin` 非超管落地态、主动构造的「载入中/失败」27 张 + 启动探针 500 各 1 组。揪出 8 条存量缺陷(§九.3)。
  - 第三轮 105 张:**「载入中/失败」两态系统跑了一遍** —— 16 个面 × 两态 × zh/th 桌面(失败态另加 390 手机)= 88 组合;外加从没验过的两屏(零账套主体新用户硬门四语双端 8 张 + 建主体表单 3 张)。**并把第一批 83 张逐张肉眼看完**(上一轮只看过 8 张)。揪出 11 条(§十.3)。
- **第三轮把两条方法论落到实处**:① **拦到 0 次一律判红并删图** —— `/home` 首页第一版打 `/api/erp/logs` 拦到 0 次(本机没配 ERP 端点,那接口根本不被请求),5 张作废重打;② **自己揪出一条自己的假红** —— `home-clients` 第一版停在「账套主体」页签,而 500 打的是「买方客户」那一栏的 `/api/clients`,失败态根本没露面,补页签点击重打之后才是真结论。
- **没做到的,逐条(第三轮口径)**:
  - **`/pos` 老板后台与收银台、`/admin` 超管 SPA 仍只走到公开页与未授权落地态 —— 但这一轮查清了为什么。** 不是「密码不知道」:本机 `select count(*) from users where is_super_admin` = **0**,产品里现在一个超管都不存在;`stw_e2e` 租户 `tenant_modules` **0 行** → `POST /api/login {entry:"pos"}` 实测 **401**(main/ai/dms 都是 200)。而给租户开 POS 的正路是超管走 `admin_pos_entitlement_routes` 发放 —— **两个缺口是同一把锁,先解开超管才谈得上 `/pos`**。按「别硬造账号绕过入口授权」没去 `UPDATE users SET is_super_admin=true`。所以 `adm-*` 只 zh+th 那条口径也仍然没验到。
  - **`/home` 报税中心本机验不到**:该租户没开 accounting 模块 → `nav-presets.ts:81` 的 `redirectOffHidden` 把 `#/tax-center` 打回 `#/dashboard`。URL 与界面是一致的(不算不诚实),但这个面看不到。
  - **肉眼覆盖**:第一批 83/83 已逐张看完、第二批 171 张里看了 66 张、第三批新图另看了 12 张关键态 —— **合计约 161/383**。en/ja 移动端仍多为机器实测。**这件事非做不可**:第三轮 4 条新缺陷里有 **3 条机器实测是绿的**。
  - **`/ai` 事务所矩阵末列被切那条仍没补上 DOM 实测**:本机开发库被清空后 `/ai` 客户数为 0、矩阵表根本不渲染(`measure.js` 输出 `{"err":"no table"}`),量不到。**留给下一轮在有客户的库上量**:滚动容器的 `overflowX`、`scrollWidth` vs `clientWidth`、最后一个 `th` 的 right 减容器 right。
  - 识别记录页仍没查表格列对齐 / 数字右对齐 / 分页。
- **走查脚本自己的已知盲点(写给下一轮别当真缺陷)**:① 「子元素越界」判据会把整体位移到屏外的关着的抽屉算进来(第三轮已排掉);② 「en 页出现汉字」会被语言切换器自己的「中文 / 日本」按钮触发;③ 「฿ 后必须窄空格」是 `/ai` `AI.format.money` 的口径,套到 `/home` `/dms` 会把合法的「฿/月」单位写法一起报红;④ **第三轮自己又栽了一条**:`LOADING_RE` 收了「正在加载」没收「正在查询」,于是把 `/dms` 录入页判成「载入中零指示」,实际页面写着「正在查询状态...」—— **判据缺口,不是产品问题**。
- **复核后判定不是缺陷、写下来免得下一轮重查**:① 第一批 4 张移动端设置图看着像「页头出现两遍」,同路由同语言同视口现测 fullPage 不复现 → 整页截图的拼接假象;② 管家卡片在 th/ja 界面下正文是中文 —— 管家文案是建任务当时按会话语言服务端渲染并落库的,夹具是 zh 会话,真泰文用户建的任务会是泰文 → **夹具产物,不是产品缺陷**。
- **本机配置改动都还原了**:第二轮为验 `/ai` 总台插过 `platform_settings` 的 `pearnly_ai_front_desk` rollout=all,验完删回(要复验先重新插)。第三轮**按产品自己的路子**(点「新建主体」→ 手填 → 确定,不是 curl)建了一个账套主体才进得去 `/home`:`workspace_clients id=14`,名字 `CLOSEOUT-VISUAL3 บริษัท ทดสอบ จำกัด`(必须含泰文才过 `thai_name_gate`)—— **这行留在库里**,不需要就 `DELETE FROM workspace_clients WHERE id=14;`。

**第 7 条「收官:`/simplify` → push → 盯 CI → STATE 卡」—— ✅ 三轮都走完。**
- 第一轮:`8f100473` 的 run 判 failure → 查下来是桩不是产品 → `eaec02c4` 只改测试文件收口,run `30557706501` 9/9 全绿(§3.0)。
- 第二轮:`22d2699f` 的 run `30576549338` failure → `9766e611` 修 → run `30577191972` success;`/simplify` 又落两笔,最终 run `30579711972` = success · 10/10(§九.2)。
- 第三轮:`6e209b78` 的 run `30625637803` failure(两条红)→ `323d559b` + `62c5c8d5` 分别修掉 → 最终 run **`30627117412` = success · 10/10**(§十.2)。**两条红都修在环境/夹具那一侧,没有一条是把闸改回放行换来的。**

---

## 四、已知但决定不做的(别再做一遍)

1. **`PAYLOAD_VERSION` 不 bump,保持 1。** 理由:`preflight.py:349` 的版本协商是**端点级一刀切**,bump 到 2 会让所有还报 1 的小助手连采购/销项一起停摆,而那两条路一个字节没变。老载荷键集零变化;老小助手收到新 doctype 会被桥端白名单拒成 `bad_payload`(响亮失败)。
   **该做的是另一件事(留给下一窗口)**:让桥在 hello 里上报 `write_doctypes` 能力清单,按能力分派,而不是拿一个单调递增的数字把不相关的车道绑一起。

2. **四类新单据不塞进 `orchestrator.run`。** 那条流水线整条长在「一张带对手方的发票」上(`ref_no` 幂等锚 / base·vat·total / items_status / party 建档),手工凭证没有对手方、库存调整没税额、收付款的幂等锚是被冲单据集合。做法是**闸与事务原语单份、流水线按单据形状分两条、四类之间走 `DocRecipe` 注入**。若要「必须物理上同一个 `run()`」,可以推翻。

3. **`duplicate_gate.py` 两侧各存一份逐字节相同的副本,不 import。** 两棵树不共享包根,import 会违反桥↔小助手隔离闸(桥不发版、小助手要发版,连起来两边行为永远对不上账)。靠字节比对闸防漂。

4. **不引入 `RISK_SPEND` 风险档。** 万能口两个工具不动会计钱包(逐行核实过 `services/fileconv` 与 `services/vat/vat_report_parser` 全程没有 `charge_ocr`),真要拦的是「会不会为这份料调一次模型」,已做成确定性回执卡上的一次点击。加一个没有工具使用的枚举值就是死代码。口径统一成 `cost.wallet_charge=false / cost.model_call=true|false`,不说「免费」。

5. **小助手不搬关账锁闸的「拒单」那一半**(只搬期号判定)—— 见 §二.4,搬半截进来又不拦就是纯死代码 + AI 味。**但这是记账不是决定**,拍板前别当已定案。

6. **管家 i18n 的 `ai-i18n-steward.js` 分片只写 zh+th**(en/ja 由 `at()` 回落 zh)。这是 B2-M1 落地时拍的口径,有守门测试锁 zh/th 集合一致。任务书写「四语齐」时未破例。
   **⚠️ 07-30 起这条不再是「决定不做」,已升为待拍板(§二.9)。** 理由:真浏览器实测量出了代价 —— en 桌面管家页 145 个汉字,余额不足卡 7 句里 5 句中文,连主 CTA 都是「去充值」。`ai-i18n-fail.js` 也是同一个口径(只 zh+th),而那张卡上写的正是「至少充多少钱」。**在 Zihao 拍板前别再拿本条当「已决定不做」引用。**

7. **前门 `pearnly_ai_front_desk` 保持封存**(`enabled=False` 带 sealed 标记,代码留仓,`interpret` 被管家真复用)。但「五闸全关」的表述已过时 —— 另外四闸 2026-07-17 经 J-gate 拍板全部 `rollout=all`。

8. **8 份 E2E spec 不入库**(10 份里只入 2 份)。逐个理由:一次性探针 / 不可原样重跑(自己会把 fixture 用掉)/ 纯出图脚本 / 要特定端口 + 直改 docker 库。判据是「换个人换台机器还跑不跑得起来 + 会不会被下一次改动碰坏」。

9. **(07-31 第三轮)`tests/integration` 那 28 个破坏性模块不进 CI。三条理由按分量排。**
   ① **它们会把目标库拆掉** —— `setUpClass/tearDownClass` 里 `DROP TABLE IF EXISTS users, clients, ocr_history, erp_push_logs … CASCADE`,打的是 `DATABASE_URL` 指到哪就是哪(无临时 schema、无回滚、无判断),而 `require_db()` 只检查 env 有没有值。**本机已中两次。** CI 里给一次性 Postgres 掩盖不了这一层,所以本轮先做的是防护(哨兵 + 静态闸,§十.1 D 组),那是搬 CI 的**前置不是替代**。
   ② **它们互相不隔离** —— 同一个库连跑两遍,红从 28 条涨到 32 条,库从 175 张表掉到 114 张(靠前的模块 tearDown 掉 `users`,靠后的全部 `UndefinedTable`)。**红的条数取决于库被拆到第几遍。** 要进 CI 得先做到每模块一个库(`CREATE DATABASE x TEMPLATE tpl` 可行,triage 时就是这么跑的),那是改 28 个 `setUpClass` + 一个 conftest 级编排,是一单独立的活。
   ③ **CI 里没有能建出这套 schema 的东西** —— 空库跑 `alembic upgrade head` 在第一条碰 `ocr_history` 的迁移就 `UndefinedTable`;`import app` 在空库上只建得出 5 张表;**基础表的 `CREATE` 语句全仓只存在于测试自己的桩里**。这条同时是本仓真正的欠账(见 §十.3 第 8 条),值得单独立项。
   **顺带更正一个被反复引用的前提**:此前(含 GATES.md)写的是「那 70 份 `require_db` 用例从没在 CI 跑过」。实测是**整个 `tests/integration` 目录**从没在 CI 跑过 —— `ci.yml` 的 test job 只 `discover -s tests/unit`。也就是说 README 里写着「CI 每次都跑」的那 **11 份纯函数安全网(157 条,扣费/对账/OCR 归一/DAL 名册那批)同样一次没跑过**。这批**无条件跑、不连 DB、3.5 秒**,加进 CI 几乎零成本零风险 —— 建议单排一条,只收 `test_*_safety_net.py` 那批。

10. **(07-31 第三轮)触控目标不按 44px 全站一刀切。** 按「手机上按空了会怎样」筛,只抬四类:`/home` 汉堡(手机端唯一主导航键)、`/home` 切账套(按错 = 录进另一家公司的账)、`/home` 账套门退出(成员零主体空态下屏上唯一控件)、`/dms` 四条 nav(车行操作员手机端的全部导航);外加上一批已抬的 `/pool` 三个签批钮。
    **没抬的与理由**:`/dms` 语言键 40(设一次就不碰)、`/dms` 退出 36 与连接 ERP 32(一次性动作,旁边没别的东西,误触成本低)、`/ai` 客户页 tab 条 32 与总台快捷 chip 22(横滑条与二级入口,抬完布局要重排)、`/ai` payroll 页 select/input 33~36 与各处文字链 16~18(表单与说明链,抬起来只把界面撑肿)。**要不要按 44 全站收口归 Zihao 定**;在他定之前,这是「按后果筛」不是「做不完」。

---

## 五、现场事实与环境

**Express 真实拓扑**(记忆 `express-topology-and-bridge-deploy`)
- **Accserver = 192.168.0.11**,跑 Express 程序,**Windows Server 2003**(连 .NET 都没装)→ 桥装不上去也不需要。
- **192.168.0.212**(S 盘)= 生产账套数据,只开 445,无管理通道 → 同样装不了。
- **桥要装第三台常开 Win 机**,经 SMB 读写。跨网写 DBF 已被现役小助手生产实证。
- **TEST 靶场**(Zihao 授权可读写删):`\\Accserver\d$\ACCOUNT\69EXP\test`,公司 `บริษัท ซินเซียร์ไอซ์ จำกัด` / 0105546015062,规模小,1 月起账、CY=2026、24 期 LOCK 全 'N'。
- **唯一需要 Zihao 拍板的关卡:第一次往生产账套真写。** TEST 靶场验证不用问。
- ⚠️ Accserver 会掉线(本机在 172.20.10.3 热点网段时不在 192.168.0.x)。断网期靠本地镜像。

**本地资产**
- 真账镜像 `D:\pearnly-erp-lab\_mirror_20260726\`(EXP69 全 7 套 + BANKREC 测试套 + EXP68 若干,约 285MB)。**只读**,`find -newermt` 可自证零改动。
- 真语料库 `C:\Users\skin3\Desktop\Pearnly-产品语料测试数据`。金标:`SM/A_客户给的原料/IMG_2640–2650` 11 张进项票 → `input_vat 29,263.28 / purchase 418,046.86`;销项锚 60,114.61。
- 选料单与 E2E 跑法/坑清单:scratchpad `visual_prep/ACCEPTANCE-BRIEF.md`。
- 靶场写前备份:scratchpad `accserver/test_backup_before/`;ISPRD 靶场备份:scratchpad `companion_fiscal/test_backup`(165 文件 / 4.3MB)。
- 本轮全部探针/报告在 session scratchpad:
  `C:\Users\skin3\AppData\Local\Temp\claude\C--Users-skin3-Desktop-pearnly-app\0242d738-f7e7-4f6c-bd67-12995cf87a8e\scratchpad\`
  (重点:`p2a-summary.md` / `artifact_download_design.md` / `ai_legacy_audit/shallow-review.md` / `erp_loose/detail-lines-diagnosis.md` / `periods/` 期间取证 / `ledger_golden/`)

**本地真栈**
- FastAPI `127.0.0.1:7860` + docker `pearnly-db`(healthy)。起法:scratchpad `start.sh`。
- 测试号 `stw_e2e` / `StwVerify#2026`(entry=ai)。
- ⚠️ **`steward` worker 是 DB 全局抢单**(`store.claim_next_task`)—— 旧进程会把新任务抢去用旧码跑。改完代码必须 kill 重启,否则会误判成「大脑不选新工具」。
- ⚠️ 起服务要显式 `export OPENAI_BASE_URL=https://openrouter.ai/api/v1`;本轮把 `JWT_SECRET` 换成 `steward-skills-verify-jwt-secret-2026`(别的窗口旧 token 全失效,重登即可)。

**发版方式**
- 主站:`git push origin master` → webhook 约 20 秒自动上线。必写 4 语 `release_notes`。
- 小助手:bump `src/companion/version.py` → `packaging/release.ps1` → **验在用小助手真更新**(托盘关于里看版本号)。三步缺一不算完。
- 桥:`build_bridge.ps1`,尚未打包发布过。

**已知本机假红**(不用追,CI Linux 绿):`test_agent_capability_audit`(cp874)、`test_ops_backup`×9(本机 WSL 缺 `/bin/bash`)。跑测试一律 `PYTHONIOENCODING=utf-8 PYTHONUTF8=1`。
⚠️ **`test_file_crypto` 07-31 已从这份名单里除名(`9f282bf5`)** —— 它当时不是「本机编码假红」,是那条断言**从来没真验过 `stderr` 内容**(子进程按 utf-8 写、父进程按 cp874 解 → `proc.stderr` 变 `None`,崩在 `TypeError` 上)。**被写进「已知假红」名单的东西没人再去看它为什么红,这条名单本身是有代价的。**
⚠️ **跑全量单测前先 `rm -rf test-results`**:Playwright 失败留下的 trace 资源会被破缓存闸扫到,造一条与代码无关的红。

---

## 六、这一轮学到的教训

1. **假绿第四型:两仓各照各的镜子。** P2-B 云端产物喂进桥端 validate,**四类里只有 2 类过**,而两侧的单测都是绿的。根治不是「再加一个测试」,是**把交接物落到磁盘上,一份,两仓共用**,再配三层闸(fixture 新鲜度 / 云端镜像 / 真桥 validate)。缺任何一层就退回假绿。
   —— 而这道镜像闸**今天就抓到了第一条真漂移**(§3.1),证明它是活的。

2. **闸在但扫不到,长期报绿。** 本轮抓到三例:① 老链路跨仓契约闸把桥仓路径写死 D 盘,搬家后一直静默 skip;② 隔离闸只认 `src.` 一种拼法,`companion.x` 整条穿过去;③ py38 扫描器与写打开单口闸配了反证,隔离闸没配 —— **清单式闸没有反证测试,长期报绿无法区分「代码干净」与「闸根本没看」**。三条都补了「喂已知有毒样本」的反证。

3. **验证脚本自己就是缺陷源。** 靶场那次「HP/RR 写路失败」查下来是**验证脚本的锅**:`payload()` 完全没带 `items` / `items_status`,`purchase_adapter` 对缺失取默认 `"empty"`,写路整段跳过明细。同一份报告里还有两处断言对不上真列名(`YOUREF` vs `REFNUM`、`RECTYP` 期望值写错)。
   **教训**:验证脚本没跟真表核对,就能拿空载荷跑绿。已加负向断言把「表头模式」的输出形状钉死。

4. **考卷绕开唯一会炸的地方,就等于没考。** 见 §3.8。串两步那题挑了个参数选填的工具,成本闸那几题靠注入现成 gate —— 两条断掉的能力被验成绿。**出题时要专门挑「必填 / 会累加 / 会炸」的那条路。**

5. **进程在跑的是提交前的旧码。** 7860 上 PID 起于 06:59,批次提交在 13:48,最初问「今天先干哪个」拿到 `out_of_scope` 拒答,差点误报成路由缺陷。**验收前先确认进程起动时间晚于提交时间。**

6. **`RATCHET-EXEMPT` 必须写在 commit message 里,而共享工作树禁 amend** → 只能单开一笔空 chore 提交声明豁免(本轮开了 11 笔:`852b6cff` `0a5db2ba` `079f0e80` `52fc2e42` `3c9b8451` `c058de49` `65580b38` `e178f1d6` `6ff89cfb` `7cd3bae6` `40cf63bb`)。这是现有工具链的必然产物,不是懒。
   **另**:本轮有代理为改 commit message 而 amend(PowerShell here-string `@'…'@` 喂 Git Bash,`@` 被当字面量,标题被污染成 `@ fix(...)`)—— 污染痕迹现在还留在 git log 里:app 5 笔(`2801793c` `c2b048e6` `d6158a49` `873ada59` `77acfb50`)+ companion 1 笔(`5ac14f5`)。**多行提交信息一律 `git commit -F <文件>`。**

7. **CSS 注释里写 `*/` 会提前闭合注释**,后面整段变成非法选择器被浏览器整条丢弃(`ai-dashboard.css` 的 `.kb-tools` 就这么没的)。prettier 还会把它重排成更乱的样子。

8. **E2E 断言点完立刻取 computed 值会撞过渡中途值**(120ms transition)—— 改 `expect.poll`。移动端量 `min-height` 必须开 `hasTouch+isMobile`(规则挂在 `@media (pointer: coarse)`),否则量到桌面值是**脚本假红不是产品缺陷**。

9. **「文档漂移」这次是反方向的:实际比文档更绿。** 两条被记成「没做」的(IN-0a/b、建档税号警示)其实当天或次日就做完了,只是 STATE/记忆从未回填。**盘点时别只信文档说「没做」,要去代码里核。**

10. **产品哲学纠偏(Zihao 07-27,已进长期记忆)**:红线不是「模型能不能算」,是「**错了能不能被发现、能不能改**」。管家 discovery 曾因「对话框没有上传口」砍掉三项能力 —— 把当前缺失当成固定约束,正确反应是**把上传口建起来**(已建,见 §一.I)。见记忆 `assist-not-replace-loosen-the-model` / `steward-universal-intake-is-the-product`。

11. **(07-31 · 本窗口最值钱的一条)「桩比真后端少给键」不是偶发,两轮一共抓到四处 —— 逐个修是修不完的,要做成登记表式的闸。**
    四处逐条:① `_f1_steward_attach_local` 的 `GET /sessions/{id}` 回**空数组**,而 `syncSession` 是整份替换 → 对话流当场清空退空态(§3.0,CI 红);② `_b2m1_steward_local` 的 `/status` 桩**漏 `attachments`** → 回形针按钮与「文件可拖入或粘贴」注脚**从来没渲染过**;③ `_b2m1_copy_rework_verify` 与 `_copy_pro_shots` 把 `attachments` **写成 `null`**(键在值不在,前端 `resp && resp.attachments` 照样拿不到,与漏键同罪);④ `copy_rework` / `copy_pro_shots` 的 `GET /sessions/{id}` **只回最后一轮 2 条消息**,而真后端回全部 —— 任务一落终态 `syncSession` 整份重建,前六轮当场被抹掉。
    **其中第 ② 处让已经入库当验收证据的截图失真**(`_artifacts/b2m1_steward/` 那批,`46688aa6` 收掉);第 ③ ④ 处同样污染了 `_artifacts/steward_copy_rework/` 的 10 张,第二轮全部重生成并逐张肉眼复看。**假绿会落盘成「证据」,而截图一旦入库就没人再质疑它。**
    **根治不是逐个修,是 `scripts/check_e2e_stub_contracts.py`(第 22 道闸)**:按登记表比对桩的顶层键集合,登记为 `not_null` 的键不许写成 `null`/`undefined`;配 `tests/unit/test_e2e_stub_contract_gate.py` 的两层 —— **防漂**(用 `ast` 读 `routes/steward_routes.py` 三个处理函数的无条件返回字面量,与登记表逐键相等,**后端加键先红**)+ **覆盖率**(每个登记端点必须真被 ≥1 个 spec 桩过,且那处桩要读得出回包形状)。
    **闸的口径也吃过一次教训**:覆盖率断言第一版写的是「读得出形状的桩点 ≥ 8」,那个 8 是在开发机上现数的 —— 本机躺着两个未跟踪脚本贡献 4 处,CI 上只有跟踪文件,当场红(run `30576549338`)。**没把 8 改成 7 交差**:改成先按 `git ls-files` 过滤到跟踪文件(`git` 列不出就直接炸,不静默退化),门槛从「总数 ≥ N」换成「每个登记端点至少一处」。**任何写死的计数门槛都要问一句「这个数在别人机器上还成立吗」。**
    **反过来的一半也要记**:判一条 E2E 红「是产品回归」之前,先拿真后端的投影核一遍桩(§3.0 教训二)。第二轮修 `_b3_closeout_verify` 的 T2 时又撞到同一形状 —— 桩只桩了 `POST /messages`,压根没桩 `GET /sessions/{id}`。

12. **🛑(07-31 第三轮 · 本轮最值钱的一条)单元测试把整个仓库删空了。差一步就把「删光仓库」推上生产。**
    **根因是一条优先级**:git 调用钩子时会往环境里注入 `GIT_DIR` / `GIT_INDEX_FILE`,**这两个的优先级高于 `subprocess` 的 `cwd=`**。`test_check_cachebust` 里那几条「造真 commit」的用例,防护只有 `cwd=tmp` 一层 —— 平时直接跑单测没有 `GIT_DIR`,所以它们十几个版本一直是绿的、也确实跑在临时目录里。**这个洞潜伏很久,是当天装 pre-push 钩子把它引爆的**:钩子里有一步跑全量单测,于是 `git init / add / commit` 全落进宿主仓,`git add -A` 从临时目录看过去把 **4905 个跟踪文件全记成删除(4838 files / -794172 行)**,分支上还多出四笔 base/head。本仓 **push 即上线**,下一步就是把「删掉整个仓库」推上生产。
    **连带残留**:同一批用例里的 `git config user.email t@t` 也被劫持,写进了真仓库的 `.git/config` —— 结果是本轮 `95e9c775`…`5d9be77c` 共 **11 笔提交的作者是 `t <t@t>`**(`git log --format='%an <%ae>'` 一眼可见)。**禁 amend,所以这 11 笔的作者改不回来了,留在 git log 里当标记。** 配置本身已改回。
    **修法不是「再加一层 cwd 防护」**:临时仓库统一走 `tests/unit/_git_sandbox.py`,它把宿主的 `GIT_*` 从 `os.environ` 里**摘掉**,被测脚本自己再 fork 出去的 git 也一并干净(`check_cachebust.main()` 内部就有 `git diff`);身份改用 `GIT_AUTHOR_*` 环境变量给,**不再 `git config`** —— 那条正是改坏宿主配置的路。要验的那层意思一个字没动:仍然造真 commit、走真 `git diff`。
    **两处更该记的**:① `test_anti_bigfile` **两个月前为同一个坑写过一份 scrubbed env,没人复用它** —— 同一个坑在同一个仓库里被独立解决过一次,然后被第二个人重新踩;这次收进沙箱,两处共用一份实现。② **建了第 26 道闸 `check_test_git_writes`**(AST 判据:`tests/` 里起 git 的子命令必须在只读白名单里,写操作、或看不出是什么的 `["git", *args]` 转发一律红;造真 commit 只有 `_git_sandbox.py` 一条路,**换个文件名放同样的代码照样红**),配 6 份有毒样本反证(直接 commit / init+add / `*args` 转发 / `os.system reset --hard` / `-c` 夹带 / JS `execSync checkout`)+ 3 份干净样本。**闸在钩子里排第一道,位置是死的** —— 排在「全量 unittest」之后等于事后收尸,仓库那时已经被写坏了。沙箱本身另配运行时反证:先证明 `GIT_DIR` 在时 `cwd=` 确实说了不算(否则下一条是空转),再拿诱饵仓验沙箱造的 commit 一笔都没落到它头上。
    **同一天里同一形态又抓到第二例**:`tests/integration` 那 28 个模块会把 `DATABASE_URL` 指到的库 `DROP TABLE … CASCADE`,而 `require_db()` 只看 env 有没有值 —— 照它们自己 docstring 跑一遍,**本机开发库当场被拆掉 62 张表、`users` 整张消失**。修法同一个思路:判据从「env 里有没有某个变量」换成「**目标库里有没有哨兵表 `_pearnly_disposable_test_db`**」—— env 是一句承诺、误 export 一次就没了,哨兵表是操作者对着这个具体的库亲手做过的一件事。配第 27 道闸 `check_destructive_db_tests`。
    **一句话**:**「测试只在临时目录里跑」如果只靠 `cwd=` 或一个环境变量撑着,那它不是隔离,是运气。** 隔离要断机制(摘变量 / 让目标自证可丢弃),并且要有一道闸盯着别人不会绕过去。

13. **🛑(07-31 第三轮)多个并行代理各自抓的「现状快照」会互相矛盾,不能当判据 —— 主控必须当场自己跑一遍。**
    本轮三路 agent **各自独立**报了同一条:「`check_file_size` 红:`services/erp/bridge/store.py` 507 行 / `services/ocr_history/mutations.py` 514 行,不是我的,请主控找那一路收口」。三份报告互相印证,看起来铁证如山。
    **实测:两者是 462 / 490,`check_file_size` EXIT=0,1334 个受监控文件 0 FAIL。** 那三个数字是它们**飞行途中**抓的快照 —— 写 `store.py` 的那一路当时确实一度到 507,随后自己把 `close_unacked_confirmed_writes` 搬去 `write_gate.py`(按职责边界切,不是从中间切一刀)降到 462;而 `mutations.py` 的 514 **从来没发生过**,那一路没把「写回 posting_kind 这一列」塞进去,而是单开了 `posting_kind_store.py`(58 行)。**主控照抄快照下判断,会派出一次「把已经合理的模块再切碎」的返工。**
    **而真正红着的是另一件事,三份报告里一个字都没提**:`check_tracked_imports` EXIT=1 —— `services/ocr_history/store.py` 的 re-export 已指向 `posting_kind_store`,但该文件还是 `??` 未跟踪。工作树里 import 得到、`check_imports` 也绿,**只有 clean clone(= prod checkout)才 `ModuleNotFoundError`** —— 正是 2026-06-11 那次全 worker 起不来的 502 形态。
    **两条操作口径**:① **并行代理的 not_done 里凡是「别路的红」,一律当线索不当结论,收口时逐条重跑**(它们看到的是一个正在变的工作树,而快照没有时间戳);② **闸要按「clean clone 会怎样」判,不是按「我这棵树能不能跑」判** —— 工作树里能 import 的东西,`git ls-tree HEAD` 里可能压根不存在。

## 七、接手第一步该做什么

> 2026-07-31 第三轮重排。原第 2、3 步(修红、推 companion)早已作废;push / 盯 CI / STATE 卡三轮都做完了。
> **主仓现在是绿的、推平了,接手不用先擦屁股;要擦的只有 companion 那两笔。**

**按顺序,别跳。**

1. `git log --oneline -20` 两仓,以 git 为准(本单必然会落后)。
   - **主仓**:`origin/master` = `62c5c8d5`,0/0。
   - **companion**:主树可能还停在 `5d90b9a`(VERSION 读 1.1.57),先 `git pull`。⚠️ **`.claude/worktrees/steward-closeout` 里有两笔已提交、未推、未发版的改动**(`6c93b3d` `16c2236`)—— 别当它们已经在会计电脑上跑着。
2. 拿 §二 剩下的 **6 条**(4、5、6、8、9、**10**)去问 Zihao,一次问完,别分批打断他。
   - §二.5 要连着 §3.1 那**四个**前置一起说,别只报「加个确认出口」;重点是第 ④ 条 —— **这个出口只能对采购方向放开**。
   - §二.9 要按第三轮量出来的规模说(**管家整片 + 看板问答条 + 余额不足卡在 en/ja 下 100% 中文**),不是「一个导航项」。
   - §二.10(发不发 1.1.59)和「谁去通知会计点更新」是**同一件事**,合起来问。
3. 拍板回来后:
   - 小助手 → **1.1.58 已在线上、现网 0 台装**。要么去通知会计逐台点托盘,要么按 §3.2 另开单改静默自动装;两条路都以「**托盘/关于里显示的版本号**」为判据,不是看安装目录文件时间。真要发 1.1.59,先把 companion 那两笔推上去再 `release.ps1`。
   - 同意补四语 → 给 `ai-i18n-fail.js` / `ai-i18n-steward.js` / `ai-i18n-states.js` 补 en/ja,补完**必须真浏览器四语复看**,别信 `check_i18n --strict`(它只管 `static/i18n-data.js`,看不见 `/ai` 这片)。
4. **挑 §十.3 那批存量缺陷动手,前三条按「用户能不能被误导」排**:
   ① **账套硬门上的错误提示物理不可见** —— 新用户填不含泰文的名字点「创建并进入」,后端 422、前端 `showToast` 也调了,但 `visibility` 全程 `hidden`,**用户看到的是「点了确定什么也没发生」**;
   ② **「后端 500 显示成空态」是系统性的** —— `/home` 对账中心与客户管理仍是这样(识别记录那个面本轮已修);
   ③ **OCR 读失败把 Python 异常类名原样印给会计**(四语都印「… `Layer1AuthError`」)。
   三条都在四态诚实红线上,不是美观问题。**②③ 各自都有现成正解在同仓里**(`/ai` `/dms` 的失败态、`error_format.py` 的消息回落),照抄不用重新设计。
5. **把任务板第 6 条剩下的补完**(§3.22):`/pos` 老板后台 + `/admin` 超管 SPA + `/home` 报税中心三处覆盖缺口 —— **先解账号权限**(本机 `is_super_admin` = 0 行,得要一个真超管口令,或由超管给测试租户开 pos/accounting 模块),**别硬造账号绕过入口授权**;肉眼覆盖还差约 220 张。
6. **回公司网段的那天,一次性做掉 Accserver 那一族**(§3.4~3.7、3.19、3.20):桥装第三台机 → 四类新单据上真靶场 → 还原靶场脏数据 → 验 Express 界面认不认 `LOCK='Y'`。
7. **本机环境有三样要知道**(不处理会浪费半天):① 开发库 `pearnly` 是从 prod schema-only dump 重灌 + 手工接回身份的,`stw_e2e` 已按原 uuid 建回原租户 `b2000000-…-0001`,误建的号改名 `stw_e2e_orphan` 保留;② `knowledge_embeddings` 本机建不出来(容器是 `postgres:16-alpine`,没有 pgvector),只影响 RAG,不挡任何一条真栈路径;③ 库里留着两行合成数据(`erp_push_logs` 的 `IV69/09901` 与 `workspace_clients id=14`),不要就按 §十.4 的命令删。

**动手前必读的记忆**:`assist-not-replace-loosen-the-model`(产品哲学)、`steward-universal-intake-is-the-product`(万能口)、`visual-acceptance-with-real-corpus`(闭环判据)、`express-topology-and-bridge-deploy`(现场拓扑)、`verify-target-must-be-real-content`(假绿第三型)、`pipe-swallows-gate-exit-code`(判绿口径)。

**纪律**:共享工作树 —— `git commit --only <逐个文件>`,新文件先 `git add`,禁 `add -A`/reset/rebase/stash/amend。别的窗口同时在动 `services/workspace/purge*` 与 `static/ai/ai-purge.js`,不要捎带。

---

## 八、2026-07-30 第一轮

范围 `7264eaf3..eaec02c4` = 20 笔,已 push(生产上跑着)。工作树 `.claude/worktrees/steward-closeout`。
这一轮的目标是清 §三 的欠账 + 补任务板第 6 条,不是加新功能 —— 零新 feature flag、零新路由、零新迁移。

### 8.1 做了什么(20 笔分七组)

**A 组 · 先把上一轮留在工作区的两处破面收掉(2 笔)**
`3a915f16` `004aa51c`
- `erp-integration.ts` 的悬空引用;5 份 `src/home/*.ts` 补顶层 `export {}` 消 19 条 TS2393(无顶层 import/export 时 tsc 当全局脚本,各自的同名 `_t`/`_auth` 互撞)。
- 采购设置页删掉没有宿主的死 CSS,**视觉照搬闸从 2026-06-28 起就烂着的那条选择器回绿**(为什么没人发现,见 8.3 第 7 条)。

**B 组 · §三 里四条产品欠账(4 笔)**
`a5116faf`(3.9 授权卡)`750b5487`(3.10 402)`f82c5aea`(3.11 预留额 + 3.8 考卷盲区)`acf177ab`(3.12 时序缝)
逐条改法与验证程度写在 §三 对应条目里,不在这里抄第二遍。

**C 组 · 生 key 根治(1 笔)**
`ed7cf7bf` —— `intake_failed_batch_n` 四语补齐。这个键**一直原样印在 402 失败卡上**(`ai-intake-manifest.js:338` 调它,四语词典里都没有),是走查在本批改动的靶心上抓到的。

**D 组 · 把「这类洞」做成机械闸(3 笔)**
`86cb4027` `954ed1c1` `9a58a3a1`
- 新闸 `check_ai_i18n_refs`:`/ai` 前端 `at()`/`t()`/`data-at` 引的键必须在某份 `static/ai/ai-i18n*.js` 里有定义。配 19 条反证 + 定义侧 1377 键交叉验证 + 969 处取词全解析。**填的是 `check_i18n --strict` 的盲区** —— 它只管 `static/i18n-data.js` 的 `window.I18N`,`/ai` 那套分片它一眼都没看过,所以上面那个生 key 在「4 语 × 4969 键 · 0 missing」的绿报告下活了很久。
- `tsc --noEmit` 进 CI `lint` job(在此之前 eslint 的 flat config 根本不收 `src/**/*.ts`,TS 源零机械闸)。
- 两个自己的毛病:闸在非 UTF-8 控制台崩在第一行 `print`;闸把 `/ai` 整棵树扫了两遍。

**E 组 · 文档与证据入库(4 笔)**
`da15e070` `7d8b5870` `384b7b09` `8f100473`
- `docs/ai/STEWARD-MAP.md` 管家架构地图(29 条路径 + 97 条行号锚点 + 工具表 20 == registry 20 对拍)。**落地当天自己复核就发现九个数是错的**(`8f100473` 改回来)—— 手写数字必过期这条又验证一次。
- 107 张视觉验收截图入库 + 7 条走查缺陷写进 commit 正文。

**F 组 · `/simplify` 收口(4 笔)**
`56cfdd19`(「哪类失败该补钱数」判在两处 → 收成 `factsHtml` 一个口)`b5da2c37`(两道按钮接线闸并存,弱的会先绿把强的盖掉 → 删弱留强)`5f41fdee`(台账假件搬家搬了一半)`afb88f61`(上一批抽了 `open402Card` 却把 401 那条的原样复制留在原地)。

**G 组 · 收尾:复判交接单 + 把 CI 弄绿(2 笔)**
`560abfde`(按实测逐条复判本单,推翻它落笔时的头号结论)`eaec02c4`(§3.0 那道红 —— 只改 F1 的桩,**产品代码零改动**,CI 回绿)。
⚠️ `560abfde` 里把 §3.0 的根因写成了「产品回归」,**判错了**,由 `eaec02c4` 实测推翻并在 §3.0 改正。要看的是改正后的那版。

### 8.2 验到什么程度

| 层 | 实跑 |
|---|---|
| 机械闸 | 21 道全量复跑,全部 EXIT=0(含 ruff / black / `check_i18n --strict` / `check_ai_smell` / `check_asset_bundling` / `ui_design_lint --gate` / `check_file_size` / `check_ui_consistency` / `check_theme_responsive` / `check_authz_coverage` / tsc / build+dist 逐字节一致);另跑 `ci.yml` 的 YAML 解析 + 结构断言 + `bandit` |
| diff 类闸 | `check_line_ratchet --base origin/master --head HEAD` **在 commit 之后**才跑(commit 前跑等于扫上一笔的 diff,是已知假绿);首跑真红 4 条,`954ed1c1` 补录四份 `.ts` 的 `RATCHET-EXEMPT` 后绿 |
| 单测 | `Ran 11376 tests` · **0 FAIL** · skipped=10 |
| 视觉照搬闸 | `node tests/visual/test_design_fidelity.spec.js` 194 条 ✅ / 0 ❌ / 19 个照搬页,真 chromium + `getComputedStyle` 逐令牌比对 |
| 真浏览器 | 107 张截图(定点 16 + 走查 83 + 402 复验 8),四语 × 桌面 1280 / 移动 390 |
| 既有 E2E 套件 | 查 §3.0 时补跑 11 份 spec(`_f1_steward_attach_local` 11/11 · `_b2m1_steward_local` 14/14 · `_fail_ways_out_local` / `_b1_states_local` / `_empty_states_local` / `_b2m1_copy_rework_verify` / `_b2m1_rework_verify` / `_ai_gaps_verify` / `_steward_loop_ui_verify` / `_b3_closeout_verify` / `_ai_blocked_reasons_verify` / `_b5_billing_local`),**0 红** |
| CI | `eaec02c4` run `30557706501` = **success**,9/9 job 全绿。中途 `8f100473` 的 run `30554691792` 曾 failure(§3.0) |

**这一轮最该记住的一条:上面那一整列绿,没能拦住那道红。**
21 道机械闸全 EXIT=0、单测 11376 例 0 FAIL、视觉照搬闸 194/0 —— 然后 CI 红了,因为挂的是
**Playwright E2E**,而本地改完 bundle 之后一次都没跑过仓库里已有的 E2E 套件。
两条教训(「改共享入口要按依赖挑 spec 重跑」「红了先核桩再判产品」)写在 §3.0 尾部,不在这里抄第二遍。

**验证方法上这轮自己踩到的两件事,记下来**:
- **402 复验第一版是自造假红**:量 `฿` 后面那个字符前先跑了 `/\s+/` 归一,把 U+2009 窄空格洗成了 U+0020,然后报「窄空格没生效」。重做时用 `codePointAt` 直读、全程不归一,并把「先归一」这个手法**做成第 4 条反证**钉住。
- **走查第一版计数虚高**:「顶破容器 3~8 处」是把 `overflow-x:auto` 里正常横滚的矩阵表和绝对定位元素算进去了;「触控目标 139 个」是用 `cursor:pointer` 兜底把大量非交互元素算了进来。两处都重测过,8.3 里的数字以重测为准。**报缺陷前先证明脚本没错。**

### 8.3 新发现、没修(这是本节最重要的部分)

真浏览器走查揪出来的存量缺陷,**按派单要求一条都没动手修**,原样留给下一个窗口:

1. **🔴 en/ja 的 402 卡是语言沙拉。** 7 句里 5 句渲染成中文,连主 CTA 都是「去充值」;只有 `intake_failed_batch_n` / `intake_retry_failed` 两条真翻了。根因 `static/ai/ai-i18n-fail.js` 只 `Object.assign` 了 ZH/TH,`at()` 回落链是「当前语言 → zh → 原 key」。会计最关心的「至少充 ฿4.50 才够」那句,英日用户读不了。**这是 §四.6 定的口径,要 Zihao 拍板补不补(§二.9)。**
   顺带一条方法论:**这个洞机器闸看不见** —— 生 key 闸报绿正是因为回落到了 zh 词典而不是回落到原 key,回落把闸掩护了。
2. **🟠 `/pool`(待我处理)移动端 390 真横向溢出 18px**,四语四端全部 `scrollWidth=408 vs clientWidth=390`。顶出去的是文件名。根因 `.riq-item-file`(`static/ai/ai-review-inbox.css:163`)只设了 `font-size/weight/color`,在 `.riq-item-hd` 这个 flex 行里既没 `min-width:0` 也没 `overflow-wrap`。
3. **🟠 同一处,文件名显示的是存储键不是人看的名字**:`<32位hex>__IMG_2485.jpg`。`static/ai/ai-review-queue.js` 的 `fileName()` 只按 `[\\/]` 切目录,没剥 `<hash>__` 前缀。它既是上一条溢出的直接原因,本身也是 UX 缺陷(会计看到一串 hash)。**和第 2 条一起修最划算。**
4. **🟠 看板页 ja 语桌面 1280,「ワークオーダー作成」按钮越出卡片右边界 18px**(self=300..436 / parent=284..418,父元素 `overflow-x=visible` = 真漏在卡外)。zh/th/en 标签短没触发。
5. **🟡 移动端 390 触控目标普遍 15~38px,低于 44。** `/pool` 上 43 个(**「复核通过 / 签批冻结 / 驳回」全是 30px 高** —— 会计队列上的批准和驳回在手机上只有 30px)、工作台 37 个、设置页 2 个、管家失败卡 2 个。项目里 ≥44 的闸只挂在两个 spec 的附件夹/送出两个控件上,全站没覆盖。
6. **🟡 看板卡片客户名硬切**(scrollW 137 vs clientW 134),四语桌面端都复现;移动端 clients 页 `.cl-name` 有 3~6 处同类。属 ellipsis 收口范围。
7. **🟠 `.cat-goto`(采购设置页「去费用数据 →」)全站没有任何 CSS**,真页面 `getComputedStyle` 实测 `color=rgb(0,0,238)` `text-decoration=underline` —— 浏览器默认蓝色下划线,和这个 SPA 的紫色 accent 完全不搭。
8. **🛑 视觉照搬闸的触发面与覆盖面对不上。** `scripts/git-hooks/pre-push:54` 只在改 `static/pos/` / `src/home/(pos|inventory|purchase)-` / `tests/visual/design/` 时触发,**CI 里 `design_fidelity` 零命中**;而它的 `MAPPINGS` 覆盖 dashboard / history / clients / sales-report / vouchers / acct-* 等 20 个 route。**所以 dashboard 那条选择器从 2026-06-28 起就烂着,没有任何东西会告诉你。** 且 `tests/visual/design/pur-settings.html` 的基准里还留着生产已经删掉的 chip 墙 —— **没有任何东西检测基准过期**。这是「闸在但扫不到」(§六.2)的第四例,而且是覆盖面/触发面错配这个新形态。
9. ~~**🛑 `/home` 那棵树有 `/ai` 刚堵上的同一个洞。**~~ **✅ 07-31 关掉(`98eb6685`)** —— `check_home_i18n_refs` 上线并进 CI + pre-push。摸底结果与预期相反:全树 **3354 处**字面量取词(比 `/ai` 大 3 倍),存量落空只有 **1 处**,不是「一堆」,所以基线只记 1 条。那 1 处已于 07-31 补齐(`ff7e3ad4`,§九.3 第 17 条),基线文件随之删除,**本闸现在是 0 容忍**。
10. ~~**🟡 成本台账在单测里仍是假游标。**~~ **✅ 07-31 关掉(`f41db5be` + `b4965755`)** —— 改在真 Postgres 上验,并且已经在 CI 的 `pg-smoke` job 里跑着,详见 §3.8。
11. **🟡 `copyCode` 连点两次会把按钮卡在「已复制」**(`ai-steward-actions.js:77`,`var prev = btn.textContent` 在点击当下取);`ai-billing.js:293` 的 `copyAcct` 同款缺陷。**本轮新加的那个复制按钮就带着这个毛病上线了。**

**本轮自己引入、也没改的**:`ai-i18n-fail.js` 新加的两条 zh 词条用了全角逗号,和同文件既有口径(半角 20 : 全角 0)相反,在 402 卡上肉眼可见。

**遗留的两处工程债**:`static/ai/ai-steward.js` 511 行仍越 500 行标准;`ci.yml` 的 step 清单还没有覆盖率测试兜「被人悄悄删掉」(清单式闸必须配反证,§六.2 的老账)。

### 8.4 明确没做的(别当已做)

- **Accserver 一族全部没做**:桥装真机、四类新单据上真靶场、靶场脏数据还原、Express 界面 `LOCK='Y'` 验证。实测本机在 `172.20.10.3`(手机热点网段),`192.168.0.11:445` 不可达 —— **物理做不了,不是没排期**。
- ~~**companion 1.1.58 没发版**~~ **(07-31 已作废:拍板后已发,见 §3.3)**。当时状态:三个文件已提交(`515d48c` `7e43877`)并推 GitHub,单测按基线复跑过(1001/26/EXIT=0),但 `release.ps1` 一次没跑 —— 里面的 `scp` 覆盖生产 exe 与 `latest.json` 是不可逆对外动作,等拍板。
- **任务板第 6 条只做了一部分**:覆盖面、目检张数、四态覆盖三个缺口逐条写在 §3.22。
- **既有 E2E 套件是事后才补跑的**:本轮改完 bundle 时一次没跑,11 份是 CI 红了之后查 §3.0 才连着跑的。顺序反了 —— 该在 push 前跑。
- 两处提交纪律瑕疵(禁 amend,修不了,记在这里免得下一个人踩):
  - `3a915f16` 正文末尾**没有 `Co-Authored-By` trailer**(信息文件里就没写)。
  - `acf177ab` 里带了一行属于上一笔 `a5116faf` 的动作分派 `stw-copy-code`。结果是**单独 checkout 到 `a5116faf` 时,那道「render 吐的每个 `data-action` 必须有分支」的跨文件接线闸是红的**,到 `acf177ab` 才绿。bisect 到那一笔的人会看见一道与他无关的红,已在 `acf177ab` 正文里写明。

---

## 九、2026-07-31 第二轮

范围 `47d6e095..5a1097f1` = **13 笔**,已 push(生产上跑着)。同一个工作树 `.claude/worktrees/steward-closeout`。
目标是继续清 §三 的欠账 + 把「这类洞」做成闸,**不是加新功能** —— 零新 feature flag、零新路由、零新迁移;
唯一一处产品面改动是 `81282c41` 的失败卡文案与差异行(§3.1 前半)。

### 9.1 做了什么(13 笔)

**A · 产品:查重差异从看不懂的中文改成会计能动手的差异(1 笔)**
`81282c41` —— §3.1 前半,改法与验证程度写在 §3.1,不在这里抄第二遍。**后半的按钮判为暂不做,三条理由同条目。**

**B · 「桩比真后端少给键」根治(2 笔)**
`e7e15763`(修第三、四处:`attachments` 写成 `null` + `GET /sessions/{id}` 只回最后一轮)
`0e734593`(新闸 `check_e2e_stub_contracts` + 19 条反证测试 + 限额值改由 `_fixtures_steward_limits.json` 单份提供,不再各 spec 手抄)
- 顺带把 `_artifacts/steward_copy_rework/` 那 10 张失真的跟踪截图全部重生成并逐张肉眼复看。教训写在 §六.11。

**C · `/home` 词典引用闸(1 笔)**
`98eb6685` —— 扩自 `/ai` 同款,但 `/home` 的取词入口有 **5 个**不是 1 个(全局 `t()` / 各写各的 `_t` `_bt` `T` / 共享 `kbT()` / `data-i18n` 属性 702 处 / `I18N[lang]['字面量']` 下标),键的字符集也不同(`/home` 带连字符和点,直接抄 `/ai` 的正则会让 2466 处 `t()` 只认出 1 处)。把括号/字符串/注释扫描抽成共用的 `scripts/js_key_scan.py`,两道闸共一个底座(`check_ai_i18n_refs` 顺带 257→147 行)。
- **建闸当天自己栽了一次,被交叉验证抓住**:第一版定义侧按缩进认键,`i18n-data.js:1073` 那种**一行挤两条词条**的后一条会漏,于是把 8 个明明有定义的键报成落空。是「定义侧 node eval 交叉验证」当场戳破的 —— 现在 0 多认 0 漏认(每块 4989 键),这条已固化成测试。

**D · 台账真库闸 + 交付物下载真闭环(2 笔)**
`f41db5be`(§3.8 尾巴:精度/串行/RLS 改在真 Postgres 上验,3 条自带反证)
`84232e68`(§3.16:真跑 package 出真产物 → 真下载 → 逐字节比对 → 真浏览器点下载)

**E · 三道新闸进 CI 与 pre-push(1 笔)**
`b4965755` —— `pg-smoke` 是本仓第一个带 Postgres service 的 job。合的时候发现一件事值得记:**派单说「三路都按令没动共享文件」不成立**,桩契约闸那一路已经把自己那份合进去了,差点重复插入。GATES 道数最后是**数出来的**(表格数据行 25 条)不是算出来的。

**F · 修三条自己坏了的测试(1 笔)**
`9f282bf5` —— ① `_b2m1_rework_verify:88` 的 `.first()` 解析到隐藏的 `#stwAttFile`(F1 附件口上线后没跟着改)② `_b3_closeout_verify:207` 断言页头写「只查不改数」,而写工具上线后产品改成了「会改数据的操作需你先批准」③ `test_file_crypto` 在设了 `PYTHONIOENCODING=utf-8` 的环境里必崩(而 pre-push 第 32 行正好 export 它)。**三条都判为断言/测试过时,没有为了让它绿去改产品**,并各配了「把产品临时改回旧样子」的反证。

**G · 视觉走查第二批入库(1 笔)**
`8438349a` —— 171 张,详见 §3.22。

**H · CI 红 + `/simplify` 收口(4 笔)**
`22d2699f`(证据 JSON 少文件末换行,CI 的 `format:check` 是全仓扫)`9766e611`(修 CI 红,§九.2)
`8f790911`(桩契约闸不再自带一份引号/注释扫描,三道闸共用词法底座)`5a1097f1`(两份真库冒烟共用一个连库口,「绝不回落 `DATABASE_URL`」只写一处)

### 9.2 验到什么程度

| 层 | 实跑 |
|---|---|
| 机械闸 | 逐条单独取退出码、**不接管道**:`check_e2e_stub_contracts` / `check_home_i18n_refs`(3354 处)/ `check_ai_i18n_refs`(969 处)/ `check_i18n --strict`(0 missing · 0 extra)/ `check_ai_smell` / `check_file_size` / `check_authz_coverage`(643 routes)/ `ruff` / `black` / `prettier` / `eslint`(0 error)/ `tsc --noEmit` / `check_asset_bundling` / `ui_design_lint --gate` / `check_theme_responsive --gate` / `check_ui_consistency --quiet` 全 EXIT=0 |
| diff 类闸 | **在 commit 之后**才跑(commit 前跑扫的是上一笔的 diff,报 PASS 是假绿):`check_line_ratchet`=0(输出 3 行 `[EXEMPT]` 逐条对上 `push_exception_classify.py +41` / `push_log_queries.py +9` / `erp-log-card.ts +26`,`home.html` 净 0)· `check_new_debt`=0 · `check_cachebust`=0 · `check_blob_size`=0(最大单图 252KB)· `git diff --exit-code static/dist/`=0 |
| pre-push | **整条真跑通了**(不是只单跑闸本身)。⚠️ 本分支与 `origin/master` 同点时 `CHANGED` 为空,钩子第 47 行直接「跳过守门 · 放行」什么都不跑 —— 只改 RANGE 那一行造两份副本才跑得到:一趟 Python 块(全量 11451 绿),一趟前端块 exit 0「✅ 全部守门绿 · 放行推送」,日志里逐行可见两道新闸真被调用到 |
| 单测 | `Ran 11451 tests` · **OK · 0 FAIL** · skipped=11。⚠️ 跑前必须 `rm -rf test-results` |
| E2E | `_b3_closeout_verify` 9 passed(**T2~T9 长期被 T1 挡着没验到,这轮全跑出来了**)· `_b2m1_rework_verify` 10 passed · `_b2m1_copy_rework_verify` 10 passed · `_steward_loop_ui_verify` 5 passed · 18 个 `*_local.spec.js` 分两趟 76 / 72 passed 6 skipped |
| 真浏览器 | 171 张截图 / 211 个组合(§3.22);`_dupdiff_card` 中泰双语 2 张;`_deliverable_download` 真点下载真落盘 |
| CI | 最终 run **`30579711972`** = **success** · 10/10 job(含首次上 CI 的 `pg-smoke`,日志 `Ran 13 tests / OK` 无 skip)。中途 run `30576549338` = failure |

**CI 那道红怎么修的,值得单记**:红点是 `test_e2e_stub_contract_gate` 的覆盖率断言 `assertGreaterEqual(len(readable), 8)` 实收 7。**那个 8 是在开发机上现数的** —— 本机 `tests/e2e/` 躺着两个未跟踪脚本贡献 4 处桩点,CI 上只有跟踪文件。**没把 8 改成 7 交差**:改成先按 `git ls-files` 过滤到跟踪文件(`git` 列不出直接炸,不静默退化),门槛从「总数 ≥ N」换成「每个登记端点至少有一处读得出回包形状的桩」,并跑了两条反证(把 `_tracked_specs` 打成空集 → 4 处全红;把所有桩点 keys 抹成 `None` → 新断言红)。

**这一轮验证方法上自己踩到的**:
- **`_b2m1_rework_verify` 修完第 88 行之后,又暴露两条被它挡住的老账**(不修的话这份 spec 照样是红的):① 必验1 名叫「两个不同工具」,可第二问写的是「上个月谁缺料」—— **还是工具 A 换个期间**,既没验第二个工具,又押注上月一定有缺料客户(本机库 2569-06 缺料 0 张,管家如实答「没有客户缺料」不画表,`rows>0` 当场落空);② readTable / 轮询回归两处都在**任务还没落终态时就往下走**,拿到的表格节点下一拍被摘走。抽了 `waitTaskTerminal(page, title)` 按标题认准是哪一条任务 + 终态徽章两层一起等。**「一个测试名字叫什么」和「它实际验了什么」要对拍一遍。**
- **`test_file_crypto` 那条断言从来没真验过 `stderr` 内容**:子进程按继承的 utf-8 写 stderr、父进程按 cp874 locale 解 → `proc.stderr` 变 `None`,断言崩在 `TypeError` 上而不是断言失败上。修成两侧都写死 utf-8 之后,手工复跑拿到 `rc=1` 且 stderr 真含 `ImportError: FILE_ENC_MODE=on 但 PEARNLY_FILE_KMS_KEY 未设置…`。**它被记在 §五「已知本机假红」名单里,于是从没人去看它为什么红。**

### 9.3 新发现、没修(这是本节最重要的部分)

> **⚠️ 2026-07-31 第三轮结算(原文一条不改,只在这里标去向,免得下一个人照旧文重做一遍)**:
> **已修**:1(`95e9c775` 改窄屏三行卡片)· 2(`b14059e7` 补错误态)· 3(`b14059e7` 补 `.pu-skeleton` 骨架)· 4(`6666799a` 两壳同一张状态码分档表 + 第三张门面)· 5(`3d78844e` 当前 tab 滚进可视区)· 6(`2b50910c` 空态卡移出表格)· 7(`6f94a739` ฿ 收进 `src/home/money.ts` 单出口)· 8(`c75dffc1` 窄轨 padding 12→6 + 3 行封顶,**仍剩 th 最长那条收省略号**)· 9(`916e5bf3` 溢出归零 + 文件名剥 hash + 签批钮 44px)· 10(`c75dffc1` `.kopen` 折行)· 11(`c75dffc1` 改 `line-clamp:2`)· 12(`70c9cdbb` **只抬四类**,见 §四.10)· 13(`afc3f44c` 复用 `.pur .btn`)· 14(`0683c083` 闸进 CI + 基准过期检测 + 修 `pur-settings` 基准)· 16(`a70b4c6b` 抽 `ai-copy-flash.js`)· 19(`6d028127` 常量键表收进射程,实测是 **16 张表 117 个键**不是 6 张 56 个)。
> **仍欠着**:15(en/ja 402 卡 —— 等 §二.9 拍板)· 17/18/20 已在原文标闭环 · **8 的残余**(th「ตรวจสามด้านรายงานภาษีขาย」64px 轨道装不下,靠 CSS 到头了,要全露得改导航形态)· **16 的残余**(`static/dms/dms-billing-topup.js:244` 与 `src/home/billing.ts:264` 还各有一份逐字同形的私版 `flash()`,后者更糟:`if (!navigator.clipboard) return;` —— 非安全上下文下点了**一点反馈都没有**;要收得把模块挪到壳中立位置并接进两条打包链,单独一笔)。

**A · 视觉走查第二批揪出来的 8 条(全部既有缺陷,非本轮引入,一条没动手)**

1. **🔴 `/home` 客户管理手机端公司名列宽被压成 0px,名字完全看不见。** DOM 实测 `.cust-row` 的 `grid-template-columns` = `0px 90px 50px 165px`(zh)/ `0px 90px 50px 180.312px`(th),`.cust-cell-name` 宽 0(left==right),`.cust-name-text` scrollW=74 / clientW=0 —— 用户只看见 5 行截断的税号。th 行右边界 379.3 > 容器 375 → 整页横滚 14px。根因 `static/home-29-vat-recon-clients.css:179` 的 `@media(max-width:720px){.seller-grid{grid-template-columns:1fr 90px 50px auto}}`,`auto` 那列吃掉 165~180px 把 `1fr` 挤成 0。截图 `closeout_visual_2/home-clients-{zh,th,en,ja}-mobile390-data.png`。
2. **🔴 `/home` 识别记录后端 500 时显示的是空态不是错误态(状态诚实红线)。** 把 `/api/history` 全部 fulfill 500(拦到 3 次),页面出「还没有记录 / 识别的发票会自动出现在这里」,四张 KPI 显 `—`,**没有报错、没有重试** —— 会计会以为自己的记录没了。同场景 `/dms` 给「加载失败,请重试」+重试钮,`/ai` 给「โหลดไม่สำเร็จ…ลองใหม่」+重试钮。
3. **🟠 `/home` 识别记录没有载入中态。** 延迟 `/api/history` 到 12s、2.5s 处拍:KPI 显 `—`(这点诚实),但列表区整片空白,没有骨架屏、没有转圈、没有文字。三个壳三种做法:`/ai` 骨架屏、`/dms` 文字「加载中...」、`/home` 什么都没有。
4. **🔴 `/ai` 与 `/dms` 的启动探针拿到 5xx 一律当「未受邀」处理。** `static/ai/ai.js:505-514` 的 `boot().catch` 只分 401 与「无 status(网络)」,其余(含 500)走 `resolveGateClosed()`;`static/dms/dms-boot.js:160-170` 同款,注释还写着「探针非 401(404/403 = 闸关/非 dms 入口)」—— **作者心里只有 404/403,代码把 5xx 也吃进去了**。实测 fulfill 500 之后分别弹「Pearnly AI 工作台为邀请制 · 请联系 Pearnly 团队开通访问权限」,唯一出路是退出登录。**后果:后端抖一下,已受邀用户会来找客服要权限。**
5. **🟡 `/ai` 客户页 tab 条 390 下第 5 个 tab 默认在屏外**,且进页面不会把当前 tab 滚进可视区(`scrollWidth=324 > clientWidth=296`,「画像」`left=338.5` 已出容器右界 375)。更实的一处:th 下深链进「ชุดส่งมอบ(交付包)」,**当前 tab 自己就被右边切掉**。对照 `/dms` 的 nav 条会把当前项滚进来。
6. **🟡 `/home` 发票工作台手机端空态卡按表格滚动宽度居中而不是按视口** → 「暂无发票」/「ยังไม่มีใบกำกับ」被推到右边、一半在屏外。**机器实测报绿**(表格容器是合法的 `overflow-x:auto`,不触发截断/溢出判据),**只有肉眼看得出**。
7. **🟡 ฿ 的写法四个壳四套,同一屏并存三种。** `/home` 首页实测:「฿ 0.」×1、「฿1.5」×1、「฿0.7」×1、「฿/月」×3、「฿ 1.」×13,另有「数字在前 ฿ 在后」3 处。源码层面:`src/home/dashboard.ts:68` 注释写「฿ 与数字间垫窄空格」,**第 69 行写的却是 U+0020**;`billing.ts:100` 是 `฿100`、`inventory-common.ts:214` 是 `'฿'+fmtMoney` 全无空格;**全仓唯一真用 U+2009 的是 `static/ai/ai-format.js:25`**。视觉后果是「฿0」两个字形黏成一个。
8. **🟡 `/ai` 侧栏 390 窄轨(railW=64、标签容器 clientW=60)标签被省略号截断,泰/日最惨**:th「ตรวจสามด้านรายงานภาษีขาย」scrollW=119(截掉一半)、ja「売上税レポート三点検査」105、en「Sales tax report checks」98,zh 只有「销项税报告三查」67 超。**轨道显然是按 2~4 个汉字排的。** 触控高度本身没问题(47.4px ≥ 44)。
   - 另有两条证据不够硬的,写在这里免得下一轮当新发现:`/ai` 工作台矩阵表泰文桌面下最后一列「สปส.1-10」被右边切掉(**只有肉眼证据,没做 DOM 实测,也没确认那张表能不能横滚**);`/home` 识别记录 en 文案「1 pages」应为「1 page」。

**B · 第一轮走查那批,一条都没修,原样欠着(全文在 §八.3)**

9. `/pool` 移动端 390 真横向溢出 18px(四语四端全部 `scrollWidth=408 vs 390`),顶出去的是文件名;**同一处的文件名显示的是存储键**`<32位hex>__IMG_2485.jpg` 不是人看的名字 —— 这两条**一起修最划算**。
10. 看板页 ja 桌面「ワークオーダー作成」按钮越出卡片右边界 18px(父元素 `overflow-x=visible` = 真漏在卡外)。
11. 看板卡片客户名硬切(scrollW 137 vs clientW 134),四语桌面端都复现。
12. 移动端触控目标普遍 15~38px 低于 44:`/pool` 上 43 个(**「复核通过 / 签批冻结 / 驳回」全是 30px** —— 会计队列上的批准和驳回在手机上只有 30px)、工作台 37 个。第二轮把口径修偏后重数(上一版把 `<a>` 里的 span/svg/path 也当独立目标数,一页数出 45~201 个全是同一批链接的内脏),只数最外层可点元素:`/home` 的 `button.topbar-hamburger` 36.0(**手机端唯一的主导航键**)、账套切换 36.0、退出 34.0;`/dms` 的 nav item 35~37、退出 36;`/ai` 每页 9~18 个。**是否按 44 收口归 Zihao 定。**
13. `.cat-goto`(采购设置页「去费用数据 →」)全站没有任何 CSS,实测 `color=rgb(0,0,238)` + 下划线 = 浏览器默认蓝,和这个 SPA 的紫色 accent 完全不搭。
14. 视觉照搬闸的触发面与覆盖面对不上(`pre-push:54` 只在改三类路径时触发、**CI 里零命中**,而 `MAPPINGS` 管着 20 个 route);`tests/visual/design/pur-settings.html` 的基准里还留着生产已删的 chip 墙,**没有任何东西检测基准过期**。
15. en/ja 的 402 卡是语言沙拉(7 句里 5 句中文,主 CTA 都是「去充值」)—— 等 §二.9 拍板。
16. `copyCode` / `copyAcct` 连点两次会把按钮卡在「已复制」(`ai-steward-actions.js:77` / `ai-billing.js:293`,`var prev = btn.textContent` 在点击当下取)。

**C · 第二轮新记的工程债(不是 UI)**

17. ~~**`src/home/page-automation-panes-1.ts:124` 的 `data-i18n="auto-erp-subtab-connect-only"` 没有词典条目**~~ **✅ 07-31 关掉(`ff7e3ad4`)** —— 四语补齐 + 真浏览器四语验收(`entry=main` 能开 `/home`,下面「没顺手补的理由」那句是错的)。同时推翻两点:`static/erp-mrerp-connect.js` 一直用自带的影子词典改 `textContent`,**非中文用户看到的不是中文**(影子词典已删,改回单一事实源);且这颗按钮今天**没有用户走得到**(集成页 0 行 ERP 卡 · `#page-automation` 不可路由)。原文如下: —— 推送日志从集成页拆成独立 tab 后按钮改指了新键,词典没跟上,**今天泰/英/日用户在那个按钮上看到的是写死的中文「连接」,且不报任何错**。现记在 `scripts/home_i18n_refs_baseline.txt` 的存量基线里(只许降不许升)。补法不用猜文案:照旧键 `auto-erp-subtab-connect` 砍掉后半截 —— zh 连接 / en Connections / th การเชื่อมต่อ / ja 接続。**没顺手补的理由**:这是用户可见文案改动,按硬门得配真浏览器四语验收,而本机唯一测试号 `stw_e2e` 的 `entry=ai`,**开不了 `/home` 那棵树的集成页,验不了就不该改**。
18. ~~**`scripts/check_i18n.py` 有 14 个键从来没看过。**~~ **✅ 07-31 关掉(`48575acb`)** —— 判据换成 `js_key_scan.DICT_KEY_DEF` + 逐行 `finditer`,四块各 4989(== node eval),那 14 个键仍四语齐;配了「删 ja 的 `user-menu-logout` → 新闸红 / 旧判据仍报 0 missing」的一红一绿反证,并固化成 node eval 交叉验证测试。原文如下: 它的 `key_in_block_re` 用 `^\s*['"]…` + `.match()`,**一行只认一条**,所以「一行挤两条词条」的后一条它看不见。它今天自报「4 语各 4975 keys · 0 missing」,而 node eval 出的真值是每块 4989。漏掉的 14 个:`contact-line-label` / `contact-phone-label` / `dxi-st1s` / `dxi-st2s` / `dxi-st3s` / `dxi-st4s` / `help-modal-tip` / `set-group-about` / `set-group-company` / `set-group-system` / `set-group-workflow` / `set-page-sub` / `user-menu-help` / `user-menu-logout`。**今天这 14 个恰好四语齐,所以没有活缺陷,但闸是瞎的。** 修法是把那条正则换成 `finditer`;没顺手改的理由是它是另一道闸的契约(计数出现在文档和别窗口的基线口径里),改完要重跑 `--strict` 确认那 14 个键仍然齐。
19. **`/home` 词典闸的已知盲区:常量键表(全树 6 张表 56 个键)不在射程内。** 形状是 `const _DUP_FIELD_I18N = { doc_date: 'erp-dup-field-date', … }` 再 `t(表[变量] || '兜底键')` —— 兜底键那半截闸看得见,表里的键看不见(今天 0 落空)。没扩进来的理由:光靠命名(`*_I18N` / `*_KEYS`)会把 `const STORAGE_KEYS = {token:'mrpilot_token'}` 这类非 i18n 键表也当词条报,**误报会让人把闸静音,等于没有**。要扩得先有个准判据。
20. **`_b2m1_rework_verify` 那两条老账已修但要知道它欠了多久**:必验1 名叫「两个不同工具」其实一直在**同一个工具换期间**,readTable 与轮询回归两处**在任务没落终态时就往下走**。这份 spec 是 07-27 那轮当「可重复跑」入库的两份之一,一直没人跑到它。**入库标准里得加一条「它验的东西和它的名字对得上吗」。**
21. **未跟踪文件的格式债**:`npm run format:check` 全仓仍红 9 处,**全在未跟踪文件上**(4 份本机验收 spec + 5 份 `_artifacts/**/evidence.json`)。clean checkout 上没有这些文件,所以 CI 今天是绿的;**将来谁要把这些脚本入库,得先跑 prettier,否则 CI 的 format 步当场红**。
22. **`_dupdiff_card_local.spec.js` 缺一道自我关闭的守卫**:它 `import` 了 `_helpers/auth` 的 `hasCreds` 却没拿来 skip,没凭据的机器上会红在登录那一步,和 `test_e2e_local_stack_guard` 那道闸的口径对不上。它未跟踪,不进 CI,但下一个在本机跑全套 E2E 的人会撞。
23. **既有工程债原样在**:`static/ai/ai-steward.js` 511 行仍越 500 行标准;`ci.yml` 的 step 清单仍没有覆盖率测试兜「被人悄悄删掉」(清单式闸必须配反证,§六.2 的老账)。
24. ~~**`0e734593` 抽限额 fixture 时漏了第 5 份内联**~~ **✅ 07-31 关掉(`d453ef98`)** —— `_f1_steward_attach_local.spec.js:57` 那份手抄 accept 只有 8 个扩展名(真后端 20 个),且游离在 `test_limits_fixture_matches_backend` 之外;改 require fixture 后 11 passed,反证是往 fixture 注 `.fake` 那道锁当场红。

### 9.4 明确没做的(别当已做)

- **Accserver 一族全部没做**,两轮都是:桥装真机、四类新单据上真靶场、靶场脏数据还原、Express 界面 `LOCK='Y'` 验证。本机实测在 `172.20.10.3`(手机热点网段),`192.168.0.11:445` 物理不通 —— **不是没排期**。
- ~~**companion 1.1.58 只到源码,没发版**~~ **(07-31 已作废:拍板后已发,三处硬断言在真发版里全跑到并通过,`.bak` 命名修复也拿到真机取证,见 §3.3)**。仍然没做的是**装机** —— 现网 0 台是 1.1.58,升级要人点托盘(§3.2)。
- **「验在用小助手真更新」这一步仍然没做**:发版只证明「包在服务器上、能下、版本对」,没证明「会计那台机器上的小助手真的换了」。要真机,本机不能拿在跑的生产实例试。
- **任务板第 6 条不是全覆盖**:两轮 278 张图,但「载入中/失败」两态**只在 4 个面上主动构造过**;`/pos` 与 `/admin` **只走到公开页与未授权落地态**(账号权限进不去,按「别硬造账号绕过入口授权」没改库提权);`/home` 报税中心本机验不到;171 张里逐张肉眼过了 66 张。
- **§3.1 的「我知道,还是推」按钮没做,是判断不是没时间** —— 三条理由 + 三个前置写在 §3.1,别当遗漏重排一次。
- **`tests/integration` 那 70 份 `require_db` 用例仍未进 CI**;`pre-push` 里只有两道新闸(`pg-smoke` 按设计只在 CI,挂进钩子等于让每次 push 依赖本机 docker 起着)。
- **本机残留,交给下一个窗口**:本地开发库 `pearnly-db` 里留了一行合成的 `erp_push_logs`(`invoice_no='IV69/00473'`,`_dupdiff_card_local.spec.js` 靠它,不要就 `DELETE FROM erp_push_logs WHERE invoice_no='IV69/00473';`);集成测试跑完会留一个空目录 `C:\opt\mrpilot\storage\workorders\00000000`;`c6c7144a` 那笔人工销项的 `source_label` 在库里存成了 `MR.ERP ????`(Git Bash curl 打掉了中文字节,它只是去重槽位键,界面和交付物都不渲染)。
- **本机建库路径与迁移有漂移(未修,只记)**:`work_order_{events,items,deliverables}` 三条外键实际是 `RESTRICT`,而 `0059` 迁移与 `schema.py` 写的都是 `ON DELETE CASCADE`。**生产是哪一种没查**(本机只有生产库只读口,且这台机在热点网段)。

---

## 十、2026-07-31 第三轮(清欠账第二波)

范围 `dc4c684e..HEAD` = **40 笔**,已 push(生产上跑着)。同一个工作树 `.claude/worktrees/steward-closeout`。
目标是把 §三 里**本机做得了的欠账一次清光** + 把「闸自己的毛病」收掉 + 视觉走查第三批,**不是加新功能**
—— 零新 feature flag、零新迁移;唯一新增的路由是 `POST /api/erp/logs/{id}/express-posting-kind`(§3.21 那条修复卡的后端)。

### 10.1 做了什么(40 笔,按组)

**A · 🛑 P0:单元测试把整个仓库删空了(2 笔)**
`49c7cabc`(临时仓库统一走 `tests/unit/_git_sandbox.py`)`c8a6bebe`(第 26 道闸 `check_test_git_writes` + 6 份有毒样本反证)
- **完整根因、连带残留、两处更该记的,写在 §六.12,不在这里抄第二遍。** 一句话:`GIT_DIR` 的优先级高于 `cwd=`,pre-push 钩子里那趟全量单测把它引爆,4838 files / -794172 行,**差一步推上生产**。

**B · UX 四路(9 笔)**
`95e9c775` `b14059e7` `2b50910c` `afc3f44c`(`/home` 视觉走查五条)· `6666799a`(启动探针 5xx)· `916e5bf3` `3d78844e`(`/ai` 手机端够得着四条)· `3f06465d`(pre-push 钩子挂上)· `cea1e000` `dfcc4e3e`(统一构建 + 16 处 `?v` + 验收脚本入库)
- **`/home` 五条**:客户管理窄屏公司名列宽 0px → 照仓里已有的窄屏堆叠范式改三行卡片(根因不是「1fr 不够用」,是 `min-width:0` 允许 1fr 一路塌到 0,而 `auto` 那列光图标就吃掉 165~180px);识别记录 500 显空态 → 真因是 `loadHistoryPage` **从不看 `resp.ok`**,500 的响应体当正常载荷解出 `items=[]`;补载入中骨架用的是**共享层 `static/pearnly-ui.css` 的 `.pu-skeleton`**(全仓此前 0 处消费方,识别记录是第一个真消费方);发票工作台空态卡从 `<td colspan=8>` 挪成表格兄弟节点;采购设置那条链接复用 `.pur .btn`,`.cat-goto` 这个死类名删掉。
- **顺手拆掉一颗雷**:骨架行**刻意不带 `.history-row`** —— 那个类是「一条真记录」的语义,`history-drawer.ts` 的点击委托与 `tests/e2e/04-history.spec.js:36` 都按它计数;带上去会让那条 spec 在载入中窗口点到骨架行而偶发红。单独配了一条反证钉住。
- **启动探针**:定状态码分档表并落成两个壳**同一份纯函数** `classifyBootFailure` —— 401=expired / 403·404·其余 4xx=denied(**语义一个字没动**)/ 5xx·408·429=unavailable / 无 status=offline;新增第三张门面「服务不可用」(不复用邀请卡那把锁,正文明说「不是你的权限有问题,账号照常有效」,单起一行 HTTP 码给支持用,主动作「重试」原地重跑 boot)。**顺带修掉 `/dms` 断网时把令牌删掉的老行为**(网络抖一下就把人踢下线)。
- **`/ai` 手机端**:`/pool` 横滚 408→390(根因不是 flex 行没有 `min-width`,是 `.riq-item-file` 自己 `overflow:visible` —— **flex 子项的自动最小尺寸只有在 overflow 非 visible 时才归零**);文件名剥落盘 hash 前缀,判据与后端 `storage.py::original_name_of` 同款并配**前后端对拍单测**(后端改落盘名格式这条会红);三个签批钮 30→44px(射程只到 `/pool`,全站 `.btn.sm` 一个没动);客户页 tab 条当前项滚进可视区 + 两侧渐隐,**首次挂载接 `document.fonts.ready` 补算一次**(自托管 Sarabun 异步到,首帧按回落字体算「一个都没被切」—— 这条是被自己的 spec 偶发红逼出来的真缺陷,不是测试问题)。
- **pre-push 钩子装上**(`git config core.hooksPath scripts/git-hooks`,Zihao 已拍板)+ 把 `AGENTS.md` / `CLAUDE.md` / BUILD-SPEC 三处「钩子是关的」的假话改成实情 + 钉 `test_pre_push_hook_wiring.py`(20 条,查三类「装了也白装」的漂移)。**这一步也正是引爆 P0 的那一下。**

**C · 第二波十路(20 笔)**
`a0554033` `979a5308` `6d028127` `36e86dee`(四道闸自己的毛病)· `d4ac5aab` `5363d9ef`(共享文件合并:pre-push 按 merge-base + 视觉照搬闸与破坏性 DB 闸进 CI)· `ea110868`(§3.13)· `c50a931c`(§3.14)· `0683c083`(照搬闸进 CI + 基准过期检测)· `7586c296`(§3.17)· `8dc5cfb0`(§3.15/§3.18)· `6f94a739` `a70b4c6b` `c75dffc1` `70c9cdbb` `d0e08b88`(第三档 UI 六条 + 触控目标)· `bae4398c` `c64826ac`(§3.21)· `72521aa1`(§3.1 前置①)· `e7b2f51c`(视觉走查第三批 105 张)
- **四道闸自己的毛病(「闸在但扫不到」的第五、六例)**:① **`check_ai_smell` 装了两个月基本在空转** —— 它要求同时满足「在 `src/` 下」且「是 `.js/.mjs`」,而 `git ls-files src` 是 **231 个 `.ts` / 3 个 `.js`**,覆盖率 **3/234 ≈ 1.3%**;实测往 `.ts` 里塞「🚀 我注意到可以顺便优化一下」+ `console.log` 照样 exit 0。收进 `.ts` 与 `static/`(射程 3 → 406 个文件),存量走基线棘轮。② **`check_line_ratchet` 三处 fail-open 改 fail-closed** —— git 不可用 / base ref 不存在 / `git diff` 失败,原本一律 `return 0`,于是「PASS」同时意味着「真没净增长」和「压根没查」。③ `/home` 词典闸收进常量键表(**实测 16 张表 117 个键**,不是原记的 6 张 56 个;判据不靠命名靠**对拍词典** —— 具名对象字面量里至少两个像键的值、且至少一个真在 `window.I18N` 里才当键表,66 张具名表干净二分成 50 张全不对 / 16 张全对)。④ 破坏性 DB 测试哨兵(见 §六.12 末段)。
- **pre-push 两处改进**:① **范围从两点改 merge-base**。根因不是「三点比两点严/松」,是 `git diff A..B` 等价于 `git diff A B` —— **两棵树的差,不是「B 比 A 多的提交」**;master 每往前跑一笔都算到推送者头上,而且**方向是反的**(master 删掉的行在这个 diff 里成了本分支「新增」,正好顶在棘轮枪口上)。实测:`worktree-erp-posting-profile` 自己只改了 1 个 `.md`,两点报 1286 个改动文件、棘轮红 19 个它根本没碰过的文件,三点 PASS。全局量化:13 条有自有提交的分支,两点判红 12 条、三点只红 4 条 —— **8 条纯误伤(67%)**。**「不是放松闸」有凭据**:能真正 fast-forward 到 master 的 push,HEAD 必含 `origin/master`,此时 merge-base == `origin/master`,两口径输出**逐字节相同**。② **摘掉 git 注入的定位变量再跑单测**(`env -u GIT_DIR …`)—— 第 26 道闸拦的是「测试源码里出现 git 写操作」这种**写法**,拦不住间接绕道;摘变量断的是**机制**,两道并存。
- **那趟全量单测判定「留着」,而且选项 A 是验过不通不是嫌麻烦**:1038 个测试模块里 **213 个(2593 条 / 84.7s = 全程 57%)** 靠扫仓库文件、起子进程来验,与被测对象之间**根本没有 import 边**(它们守的多半是 `.ts/.js/.html/.md`,Python 的 import 图物理上看不见),必须无条件全跑;剩下 64s 里按反查闭包中位数(114/1047 ≈ 11%)最多再砍九成 —— 总账 149s → 约 92s,**换来一套会漏判的影响面分析,不划算**。真正的省来自 merge-base 那条修正:分支没动 `.py` 时整个 Python 闸块根本不进来,从「白跑 3 分钟」变成「40 秒文档路径」。
- **视觉照搬闸进 CI + 基准过期检测**:`tests/visual/design-freshness.js` 判「某个基准 class 只有在生产 DOM 里没有**且**所有样式表里也没有任何规则提到它」时才判过期 —— 两条同时中才红。射程是量出来的:19 份基准只按 DOM 判报 110 条(绝大多数假的),加样式表这条后 46 条,刨掉 12 页共有的水印 `.ptag` 剩 **34 条真差异**,其中 2 条(`.addc/.cats`)就是这次要修的 chip 墙,**其余 32 条逐条登记 + 写理由**。**闸自己不许瞎**:每次跑先往第一份基准里塞毒 class,必须「只逮住它」,逮不着直接判红。**登记表里已不再是孤儿的条目也报红**(防它烂成免死金牌)。
- **第三档 UI 六条 + 触控目标**:`฿` 写法四套收成一个出口(新增 `src/home/money.ts` 导出 `BAHT = ฿+U+2009`,25 个 `src/home/*.ts` 一律 import;词典里 82 处 `฿` 后全部改 U+2009;**真浏览器对拍 HEAD 版 bundle**,同一块余额卡改前是「一屏两种」改后 3 处全 U+2009,按码点直读不做空白归一)· `copyFlash` 抽成零依赖模块,状态挂按钮元素上(重渲染即失效,不留悬挂)· 看板按钮折行 / 卡片名 `line-clamp:2` / 客户目录 `.cl-name` / 侧栏窄轨 3 行 · 客户档案页 `#caTabs` **先量后接**(390 泰语深链落第三个 tab 时当前项右边被切 54.7px 且 `scrollLeft` 一直 0,确认真切才接)· 触控目标只抬四类(§四.10)。
- **⚠️ 顺带修了上一轮留下的一处假账**:`ai-shell.css` 的注释写「给两行(2×60=120 ≥ 最宽的 119)」—— `max-width:60px` 是死的,anchor 只有 39 宽,**真实每行只有 35px**,那条泰文标签当时仍被 clamp。已改成实测口径并写明「别按 3×48=144 反推 119 装得下」(泰/日在词边界断行、行填不满)。

**D · 清障与收口(9 笔)**
`8f790911` 之后的 `fa23287b`(统一构建 + 7 处 `?v`)`07c7937d`(补一条漏写的棘轮豁免)`6578cb33`(重生成 UI lint 报告)`8cf4f77b`(修一条真回归)`6e209b78`(black)`323d559b` `62c5c8d5`(CI 两条红)
- **`check_file_size` 那两条「红」是过期快照,真红在别处** —— 完整论述在 §六.13。
- **`?v` 汇总(7 处,四个入口 HTML 都是 CRLF,用 python 二进制改)**:`home.html` 的 `main.js` 12038805→12038806 / `home.css` 12022202→12022203 / `i18n-data.js` 12038605→12038606;`ai.html` 的 `ai.css` 75→76 / `ai.js` 113→114;`dms.html` 的 `dms.css` 13→14;**`admin.html` 的 `admin.css` 11991002→11991003 —— 这一处任何一路都没报,是跑完 build 逐个比出来的**(超管后台与主站共用 `home-06-responsive-rd.css` 与 `home-10-push-logs.css`,本轮的汉堡 44px 与失败卡操作区换行两条都打进了 `dist/admin.css`;漏了它超管后台吃旧样式)。
- **修掉一条本轮引入的真回归(`8cf4f77b`)**:全量单测第一趟红 4,其中三条 `test_ratelimit_limiter` 是真回归 —— `36e86dee` 新加的用例用 `importlib` 真 import `tests/integration/_helpers`,而那个模块**在 import 期** `os.environ.setdefault("RATE_LIMIT_ENABLED", "false")` 且没人还原,活到进程结束,**排在它后面的限流用例构造中间件时读到 `false` → 限流整个关掉**,断言「超限必须被拦」的三条变成请求直接打到应用层。修在**弄脏的那一侧**(setUp 取快照 + addCleanup 还原),并配反证:`import` 那个模块后再跑限流模块,打印 `RATE_LIMIT_ENABLED=false` 且同样三条当场红。

### 10.2 验到什么程度

| 层 | 实跑 |
|---|---|
| 机械闸 | 逐条单独取退出码、**不接管道**:`check_ai_smell --all`(406 个源)/ `check_line_ratchet` / `check_home_i18n_refs`(3491 处)/ `check_ai_i18n_refs`(975 处)/ `check_i18n --strict`(四语各 **5006** 键 · 0 missing · 0 extra)/ `check_e2e_stub_contracts` / `check_test_git_writes` / `check_destructive_db_tests`(79 处 DDL 全部已接哨兵)/ `check_file_size` / `check_imports` / `check_tracked_imports` / `check_ui_consistency` / `check_theme_responsive --gate` / `ui_design_lint --gate` / `tsc --noEmit` / `black` / `ruff` / `eslint`(0 error)全 EXIT=0 |
| diff 类闸 | **在 commit 之后**才跑(commit 前跑扫的是上一笔的 diff,报 PASS 是假绿):`check_line_ratchet --base origin/master --head HEAD` = 0,**44 条 `[EXEMPT]` 的净增数与提交信息里声明的 `+N` 逐条相等**(`agent_store +84` / `write_gate +49` / `erp-exc-actions +69` / `slots +41` / `posting_kind_store +58` / `money.ts +11` / 25 个 `src/home/*.ts` 的 `+1~+3` …)· `check_new_debt` = 0 · `check_cachebust` = 0 · `check_blob_size` = 0 · `check_new_routes_contract` = 0 · `git diff --exit-code static/dist/` = 0 |
| 视觉照搬闸 | `node tests/visual/test_design_fidelity.spec.js` = **233 条绿**(修前 194)· 0 ❌ · 末行「基准无过期结构」;`--list` 统计 19 页共判 401 个 class、命中登记豁免 44 次(32 条页内 + 12 页 `.ptag`) |
| 单测 | `Ran 11649 tests` · **OK · 0 FAIL** · skipped=11。⚠️ 跑前必须 `rm -rf test-results` |
| companion | `pytest tests bridge/tests -q` = **1013 passed / 26 skipped**(基线 1001/26)· 定向 `test_purchase_detail_refnum` 改前 **4 failed** 改后 4 passed |
| pre-push | **整条真跑通了**(不是只单跑闸本身):新 BASE 算出的 CHANGED 正好是那 4 个共享文件,`.py` 块因此进来,依次跑过 测试 git 写操作闸 → 破坏性 DB 闸 → ruff → black → `import app` → `check_imports` → `check_tracked_imports` → `check_i18n` → 全量 unittest(`env -u` 生效)。另拷副本把那趟单测换成 `true` 跑一遍,让后面的闸也有机会执行 → 尾段全绿 EXIT=0 |
| 真浏览器 | 视觉走查第三批 105 张 / 88 组合;六份可重复跑的新 spec(`_home_ux_fix5_local` 5 条 / `_gate_probe_honesty_local` 33 条 / `_ai_mobile_reach_local` 6 条 / `_ui_tier3_local` 8 条 / `_postkind_fix_local` 4 条 / `_confirmed_unacked_card_local` 1 条)—— **全部自带 python 静态服跑 `static/dist` 真产物,CI 收得到**,并各自过了 `test_e2e_local_stack_guard` 那道「本机验收脚本混进 CI」的闸 |
| 反证 | 每一路都在**改坏的真产物**上跑过:`/home` 五条 6 份毒样本 · 启动探针撤掉 unavailable 一档 → 33 条里红 13 条(且 404/403/401/断网 16 条全程绿 = 没顺手改坏权限那一档)· `/ai` 手机端 8 条 · 第三档 UI **8/8 在 HEAD 产物上全红** · 接地闸装闸当下 8 条红 · 照搬闸 4 条(含「把尺子弄瞎」那条)· 棘轮收紧前后同一份毒样本 exit 0 → exit 1 · 至多一次投递两侧各一组 |
| CI | 最终 run **`30627117412`** = **success · 10/10 job**。中途 run `30625637803`(`6e209b78`)= failure(两条红,见下) |

**CI 那两条红怎么修的,值得单记 —— 两条都修在环境/夹具那一侧,没有一条是把闸改回放行换来的**:
1. **`pg-smoke` job 起的是一次性空 Postgres,而那批用例踩的四张 legacy 表建不出来。** 桥那条队列的表由 `bridge_schema.ensure_tables()` 建,小助手这条队列踩的 `users` / `erp_endpoints` / `ocr_history` / `erp_push_logs` **建表 DDL 从来没进过版本控制**(96 条 alembic 迁移里这四张表只有 `ALTER`,一条 `CREATE` 都没有),于是 `setUpClass` 第一句 `INSERT INTO users` 就 `UndefinedTable`。修法(`323d559b`):列定义逐列抄自开发库的 `information_schema` / `pg_constraint`,只留本用例真读写的那些,`CREATE TABLE IF NOT EXISTS` 让开发机上整段 no-op —— **那边跑的仍是生产同款结构,列型真漂了会在本机先红**;`status` 的 CHECK 一并带上(这条用例断的就是状态落点,少了它 CI 上 `'manual'` 写得进去而 prod 写不进去)。反证:本机现开一个空库复刻 CI 条件,改前 2 个类整类 error,改后 `Ran 20 tests · OK · 0 skip`。
2. **棘轮改 fail-closed 之后,它的真仓用例在 CI 上翻红 —— 因为 `actions/checkout` 默认 `depth=1`,`HEAD~1` 在 runner 上压根不存在,棘轮据实报「判不了」。** **方向不动**:判不了就该红,fail-open 会让闸在最需要它的时候静默放行。要分开的是被混成一件事的两种情形 —— 「git 坏了」修环境,「这次 checkout 没有可比的基线」把历史拉全。修法(`62c5c8d5`):unit job 的 checkout 补 `fetch-depth: 0`(与 `lint-size`/`lint-debt`/`lint-routes` 同款 —— **真正把门的 `lint-size` 本来就是全历史,所以生产判据一直是对的,红的是测这件事的环境**);用例侧把前提单独断一条,并给「判不了」三条加 `assertNotIn("PASS")` —— **fail-closed 守的那层意思就是「PASS 只能意味着查过了」,这句话没被断住等于没守**。反证:真做一个 `--depth 1` 浅克隆 → exit 1 且打「浅克隆请把历史拉全」,`git fetch --unshallow` 后 exit 0;把 git 从 PATH 摘掉 / 在没有 `.git` 的目录里跑,两条仍红且输出里没有 PASS。

**这一轮验证方法上自己踩到的**:
- **反证脚本自己会造假绿,而且不止一次**:① 接地闸第一版 `_grounds()` 漏传 `spec=` 参数 → `check_slots` 回落去查不存在的工具 → 全部 `ok=False`,有毒样本「全绿」其实是全空转;② 同一路第一版削位样本 `840.00→840.0` 值没变(Decimal 相等),被记成 549 条「新判据仍放过」;③ `/home` 五条第一版反证用 `npx vite build` 单跑 —— 它会把 `static/dist` **整个清空**,之后所有用例都红在「产物不见了」,**毒样本的红不算数**。三条都在交付前自己抓出来并改掉。
- **`/ai` 那一路的反证还揪出一处自己的虚假**:第一版 CSS 写了 `min-width:0; max-width:100%`,把它们删掉断言照样绿 —— **说明这两条根本没在承重**(`overflow` 非 visible 已经让 flex 自动最小尺寸归零)。当场删掉、改成对 `overflow:hidden` 下毒才变红。**没这轮反证,这两行会当成「修好了的证据」交上去。**
- **差点报错一条「缺陷」**:`/ai` 侧栏截图上看起来搜索框压住了 KPI 药丸,肉眼像重叠。按截图同配置实测:三颗 `.sum-pill` 右缘最大 185.5、`#searchInput` 左缘 194,`overlapsInput` 三条全 false —— **是圆角相邻的错觉,没报上去。**(注意这和 §十.3 第 5 条不是一回事,那条是 `/ai` 工作台 en 下的真相交。)

### 10.3 新发现、没修(这是本节最重要的部分)

**A · 视觉走查第三批 + 逐张肉眼过第一批,揪出来的(全部既有缺陷,非本轮引入,一条没动手)**

1. **🔴 账套硬门上的错误提示物理不可见 —— 用户看到的是「点了确定什么也没发生」。** 零主体新用户在「创建账套主体」里手填一个**不含泰文**的名字点「创建并进入」→ 后端 422 `workspace.thai_name_required`(四语文案齐全)→ 前端 `doCreate` 的 catch **确实** `showToast` → toast 也确实进了 DOM,但 DOM 三拍实测(300/700/1500ms)`#mp-toast-wrap` 的 `textContent` 有话、`innerText` 为 `''`、computed `visibility` 全程 `hidden`,3 秒后消失。**而且表单上一个字都没提「名字必须有泰文」。** 根因:`home.html:19` 的 `body.workspace-gate-preboot > :not(#workspace-gate-root):not(script):not(style){visibility:hidden}` 把 body 直系子节点全按住,而 `#mp-toast-wrap` 正是 body 直系子节点 —— **反证坐实**:只在浏览器内存里 `classList.remove('workspace-gate-preboot')`,同一元素 rect 一模一样但 `visibility` 变 visible、`innerText` 有字。**影响面不止这一条**:同一屏的税号查询失败(`subj-tax-len` / `subj-lookup-fail`)同样被按住。修的方向不是改 toast,是让门这一层自己出错误位(或把 toast 容器排除在那条规则外)。
2. **🔴「后端 500 显示成空态」是系统性的,不是一处 —— `/home` 三个面同病(本轮只修掉一个)。** ① 识别记录 → **已修(`b14059e7`)**;② **对账中心「最近对账」→「暂无对账记录」**(拦 `/api/recon/bank-v2/tasks` 500,拦到 2 次,zh/th/390 三组合全复现);③ **客户管理→买方客户 →「还没有客户」+ 计数 0**(拦 `/api/clients` 500,拦到 3 次);另有首页额度记录只缺重试键。**对照组同仓就有正解**:`/ai` 待我处理与 `/dms` 三个面在同样 500 下给「读取失败 · 网络或服务暂时不可用,请重试」+ 重试键。
3. **🟡 OCR 读失败时把 Python 异常类名原样印给会计,四语都印。** 截图上分别是「OCR 识别失败:`Layer1AuthError`」「OCR อ่านไม่สำเร็จ: `Layer1AuthError`」「OCR could not read this document: `Layer1AuthError`」「OCR 読取に失敗:`Layer1AuthError`」。链路:模板 `static/ai/ai-i18n-{zh,th,en,ja}-2.js` 的 `verdict_ocr_error` 直接填 `{error}`,而 `{error}` 在 `services/ocr/error_format.py:45` 有一条 `msg = str(e) if e else e.__class__.__name__` 的回落 —— **异常没带消息时填的就是类名**。机器闸抓不到:它既不是 snake_case 生 key、也不溢出、也不是 undefined/NaN。
4. **🟡 `/ai`「报表」与「工资表申报」的客户下拉:三种截然不同的处境长得一模一样。** 数据源 `/api/workspace/clients` **载入中零指示**(2.4s 处与有数据态像素级没差别)、**500 时退化成只剩占位**「选择客户 / เลือกลูกค้า…」—— 「还没有客户 / 正在读 / 后端挂了」用户分不出,**而这两页除了这个下拉没有别的入口**。
5. **🟡 390 下 `/ai` 工作台顶部的 KPI 徽章行被搜索框压住,而且只测 zh 会漏。** en 实测徽章数字「0」rect=[192,121,7,20] 与搜索框 rect=[194,112,166,40] **相交 5×20px**,`elementFromPoint(中心)` 命中 `input`;第一批截图里更狠 —— en「Needs your review 1」与 th「รอคุณตัดสินใจ 1」的**数字整个被盖住**,盖掉的正好是这行唯一有信息量的部分。**zh 因文案短侥幸不撞**,zh/390 同一面 `overlapped=[]`。
6. **🟡 `/ai` 客户列表 en 复数错**:「1 obligations this period」应为 obligation(与第二轮那条「1 pages」同族,建议一起收)。
7. **🟡 `/ai` 客户列表 390 下丢掉税号整行、公司名截断成「Bangkok Noo…」「Sister Makeu…」,而「当期义务 8 项」徽章仍占全宽** —— 与 `/home` 客户管理「公司名列宽 0」是同一取舍问题的轻症版。
8. **🟡 `/ai` 侧栏 390 窄轨在 ja 下标签被**两头**切**:当前项「ダッシュボード」渲染成「ッシュボ」、「財務ファイル変換」渲染成「務ファイ」(居中对齐 + 定宽),比 th/en 的「尾部省略号」更难认。第二轮只记了「被省略号截断」,ja 这种值得单列。

**B · 工程债(不是 UI)**

9. **🛑 那几张 legacy 表的建表 DDL 从来没进过版本控制 —— 任何空库都建不出它们。** `users` / `erp_endpoints` / `ocr_history` / `erp_push_logs` 四张表,在 96 条 alembic 迁移里**一条 `CREATE` 都没有,全是 `ALTER`**(实测:四张表逐个 grep,`CREATE`-in-migrations = 0;而 `ocr_history` 被 16 条迁移 `ALTER` 过)。后果链已经实测到底:空库 `alembic upgrade head` → 第一条碰 `ocr_history` 的迁移就 `UndefinedTable`;空库 `import app` → 只建得出 5 张表;**全仓 `CREATE TABLE … users` 只在 `tests/integration` 自己的桩里出现过**。这是「`tests/integration` 进不了 CI」的第三条理由(§四.9),也是本仓恢复能力上的真窟窿 —— **今天本机开发库能修回来,靠的是从生产拉 schema-only dump,不是靠仓库**。值得单独立项:把基础表补进迁移史。
10. **🟡 视觉照搬闸的登记表里还有 32 条旧账**(`tests/visual/design/_freshness-allow.json`,`--list` 可逐条看理由)。**最该修的两组**:① `pur-form.html` / `pur-detail.html` 的 `.grid` —— **生产早改成一体化 `.sheet` 白卡**(MAPPINGS 该页注释自己写着),稿还是左右两栏;② `dashboard-final.html` 的 `.bl` / `.bn` —— 随 6-28(`627aa1d6`)删掉的余额带一起失效。这几份要按生产实况**重画整段基准**,不是顺手能做的,也不该由一个人替那几页拍板。
11. **🟡 `direct_read` 的 sanity 闸把 VAT 内含的 ABB 小票判成「结构不可能」,每张白花一次 Vision 的钱。** 判据是「明细行和 > 小计 = 结构不可能」,而 **VAT 内含的 ABB 小票天然如此**(实测 2690.00 > 2514.02)。**16 张样本里 9 张因此从直读主路回落 Vision 路** —— 等于几乎每张 Ocha 小票都白跑一次 Vision(每页约 $0.0015)。日志原文:`pipeline: image direct-read fell back to Vision path: page 1: sanity: 明细行和 2690.00 > 小计 2514.02 — 结构不可能`。
12. **🟡 `copyFlash` 还有两份没收口**:`static/dms/dms-billing-topup.js:244` 与 `src/home/billing.ts:264` 各有一份逐字同形的私版 `flash()`(点击时才抓 `prev`,连点两次卡死),**后者更糟** —— `if (!navigator.clipboard) return;`,非安全上下文下点了**一点反馈都没有**。正确解法是把模块从 `static/ai/ai-copy-flash.js` 挪到壳中立位置(名字与 `AI.*` 命名空间都不该跨壳)、同时接进 `dms.js` 与 `main.js` 两条打包链 —— 要改 `scripts/build-home-js.mjs` + 现有单测 + 反证。**半修一份「第三份私版 flash」等于把刚收的口又开回去**,建议下一轮单独一笔做。
13. **🟡 §3.21 那张补选卡有两个已知边界**:① **批量补选没有** —— 真实场景 escalate 通常是整批(50 张票同一账套同一批上传),现在得一张一张点;批量栏那个「批量重推」走 `/api/erp/logs/batch-retry`,**不带 `posting_kind`、只读票上那一列** → 对没声明过的票会原样再 escalate 一遍。建议:批量栏在选中项全是 `posting_kind_needed` 时多一个「按同一去向重推」,复用同一条路由逐条打。② **2026-07-31 之前 escalate 落库的行,`request_body` 里没有 items**,补选卡的商品行会是空的(`derive_posting_fix` 已兜住,卡不会崩,但那批票看不到票面商品)—— **没做回填,老 `request_body` 里连 fields 都没有,重跑 mapper 也补不出来**。③ 补选去向这个动作**没写 `operation_logs`** —— 现有三张修复卡也都没写,保持一致;要加应该四张一起加,不单给新的这张开先例。
14. **🟡 想给「所有云端产的 `EXPRESS_MANUAL` 原因码都必须有前端映射 + 四语文案」装一道通用闸,没做。** 这一族缺陷已经犯过三次。没做的理由:那道闸会把存量码全扫一遍,**大概率一装就红一片**,属于独立一批活,硬塞进这条钱路径的改动里会把它淹掉。本轮改成把「产码的地方」与「翻码的地方」钉在**同一个用例**里(`test_confirmed_unacked_seam`),只覆盖新增那一个码。**建议单独立项。**
15. **🟡 未跟踪文件的格式债(与第二轮同款,数字更新)**:`prettier --check` 全仓红 **10 个文件** —— 9 个未跟踪(本机验收 spec + `_artifacts/**/evidence.json`),唯一跟踪的 `tests/e2e/_artifacts/ai_blocked_reasons/evidence.json` 拿 `origin/master` 版本单独跑是**绿的**,红的是工作树里未提交的脏改动。clean checkout 上没有这些,所以 CI 今天是绿的;**将来谁要把这些脚本入库,得先跑 prettier**。
16. **🟡 既有工程债原样在**:`static/ai/ai-steward.js` 511 行仍越 500 行标准;`ci.yml` 的 step 清单**已经有了**覆盖率兜底(`test_pre_push_hook_wiring` 查钩子 ↔ GATES.md 双向 parity + 标题道数 == 表体行数),**但 `ci.yml` 自己那份 step 清单仍没有** —— §六.2 的老账只还了一半。
17. **🟡 `tests/unit` 里两份 `pg_smoke` 没纳入破坏性 DB 哨兵**:`test_steward_budget_pg_smoke.py` / `test_erp_bridge_pg_smoke.py` 会 `DISABLE ROW LEVEL SECURITY` 和建表,但不 `DROP` 真表,所以静态闸扫不到。它们只认 `PEARNLY_PG_SMOKE_URL`、**绝不回落 `DATABASE_URL`**,风险低一档。**要扩得先给「`DISABLE RLS` 算不算破坏性」定个判据**,那是另一次拍板。
18. **🟡 `tests/integration` 里 7 个模块隔离后仍红,没归到底,故意不动**:`test_workorder_notify_real`(2)· `test_workorder_ocr_ledger_real`(1F1E)· `test_ocr_feedback_rls_real_tables`(1E)· `test_line_answer_autoresume`(1F)· `test_billing_rls_real_tables`(2)· `test_erp_push_rls_real_tables`(1)· `test_exceptions_rls_real_tables`(1);另 4 个走真 app 的:`test_auth_integration` 打 `/api/auth/login` 拿 404(真路由是 `/api/login`,像是测试路径过期)、`test_bank_recon_integration` ×2 与 `test_vat_excel_integration` ×1 是「期望 400/200、实得 401」(守门排在校验前面,测试没跟)。**没有一个被 skip 掉充数,也没有一个被顺手改** —— RLS 那几条的失败形态是「租户看不见自己的行」(**过度阻断,不是泄漏**),要判是产品错还是测试错得有对应域的人拍板。triage 台的局限也量过:补 `GRANT` 后红的条数一条没变(排除缺权限),改用全空库再跑 `test_clients_ocr_history_rls_real_tables` **转全绿**、其余仍红 —— 说明它只在独占一个处女库时成立。

### 10.4 明确没做的(别当已做)

- **Accserver 一族全部没做,三轮都是**:桥装真机、四类新单据上真靶场、靶场脏数据还原、Express 界面 `LOCK='Y'` 验证。本机实测在 `172.20.10.3`(手机热点网段),`192.168.0.11:445` **物理不通** —— 不是没排期。同理任何 Express 侧的日期口径实证(§3.14)也做不了。
- **companion 1.1.59 没发版,而且连 1.1.58 现网也是 0 台升级。** 两笔改动(`6c93b3d` 幂等取最后一条活行 / `16c2236` 采购明细行补 `REFNUM`)**已提交但未推、未 bump、未跑 `release.ps1`、未装机** —— 按铁律它们在会计电脑上一行都没生效。**「验在用小助手真更新」这一步从 1.1.56 到今天一次都没做过。**
- **视觉走查三轮共 383 张图,但 `/pos` `/admin` 仍只到公开页与未授权落地态。** 这一轮查清了原因是**产品里现在一个超管都不存在**(`is_super_admin` = 0 行),不是密码不知道;`/pos` 那把锁挂在同一个超管上。按「别硬造账号绕过入口授权」**没去改库提权**。肉眼逐张覆盖约 161/383。
- **§3.1 的「我知道,还是推」按钮一个字没放开,是判断不是没时间** —— 三条理由 + **四个**前置写在 §3.1,别当遗漏重排一次。
- **§3.14 的提示词那一步判为不做**,理由是拿抵扣凭据换一天入账日不划算(§3.14),**不是评测没跑** —— 16 张配对样本 before/after 全臂真跑过,关键样本另跑 2-4 次重复以区分回归与抖动。
- **`tests/integration` 那 28 个破坏性模块不进 CI,是判断(§四.9)**;顺带更正:**整个 `tests/integration` 目录**从没在 CI 跑过,包括 README 里写着「CI 每次都跑」的那 11 份纯函数安全网(157 条,3.5 秒,无条件跑)—— **那批加进 CI 几乎零成本,建议单排一条。**
- **`pos-barcode-scan` 那棵树会不会被新钩子拦,没有答案,不猜。** 只在 ref 层面评了两道(棘轮 PASS;`black` 两个文件按 tip 内容红,但它工作区已格好、下次 commit 就干净);`import app` / 全量 unittest / `npm run build` / dist 一致性 / `check_file_size` / eslint 这六道**必须在它的工作区跑,跑了就会改它的文件**(`npm run build` 会重写它的 dist),按令没进它的树。**这是范围声明,不是「应该没问题」。**
- **force-push 到 master 那个场景,钩子仍然盖不住。** 拿一条落后的分支 `git push --force <branch>:master` 会把 master 上的文件退回旧版本,三点范围看不见这些文件。判断:本仓已有硬规禁止对共享 master 做 force/reset/rebase,而且非 ff 的普通 push 会被远端直接拒;**要机械堵这个口子得让钩子读 stdin 的 `<local ref> <local sha> <remote ref> <remote sha>` 判目标 ref —— 但那条路没法在不真 push 的前提下验,装一个自己验不了的闸就是本单反复强调的「闸在但扫不到」,所以停手,交给主控/Zihao 定。** 同一个 stdin 思路还能省一件事(只在 `remote_ref == refs/heads/master` 时才跑那趟全量单测),同样因为验不了而没做。
- **`check_new_debt` 在钩子里仍是裸调**(用脚本默认的 `HEAD~1..HEAD`),而行数棘轮看整条分支 —— **钩子对「新债」只看最后一笔,两个口径本来就不一致**。这是既有问题,改它是行为变更,单独提出来。
- **本机残留,交给下一个窗口**:① 开发库 `pearnly` 里两行合成数据 —— `erp_push_logs` 的 `invoice_no='IV69/09901'`(`_confirmed_unacked_card_local.spec.js` 靠它)与 `workspace_clients id=14`(第三批走查为进 `/home` 建的),不要就 `DELETE FROM erp_push_logs WHERE invoice_no='IV69/09901';` / `DELETE FROM workspace_clients WHERE id=14;`;② `stw_e2e_orphan` 这个号(库被拆后误建的孤儿身份,名下还有行,删会 CASCADE,故改名保留);③ `stw_e2e_seat2`(`b2000000-…-0009`)**的原始用户名查不到,是恢复时起的名** —— 它名下 47 条 session 与 `user_company_roles` 行是真的,但如果哪个 spec 依赖它的原名,得再改一次;④ `knowledge_embeddings` 本机建不出来(容器 `postgres:16-alpine` 没有 pgvector,`pg_available_extensions` 0 行),**不是漏灌**,只影响 RAG;⑤ 工作树里还留着 10 个「零 diff 的 CRLF 脏 dist 文件」(`console.css` / `console.js` / `dms.js` / `invite.js` / `landing.*` / `pos.*` / `post.js` / `pre.js`),**内容与 HEAD 逐字节相同**,只是 git 认为被碰过,按令没塞进提交也没 checkout 还原。
- **本机跑闸必须带 `PYTHONIOENCODING=utf-8`**:`check_i18n.py` / `check_test_git_writes.py` / `scripts/agent_capability_audit.py` 在这台机器的 cp874 控制台下 print 时直接 `UnicodeEncodeError` 崩掉(退 1),**看着像闸红其实是码页**。CI 在 Linux/UTF-8 下不受影响。
- **11 笔提交的作者是 `t <t@t>`**(`95e9c775`…`5d9be77c`),是 §六.12 那次 P0 的连带残留。**禁 amend,改不回来了** —— 别当成有另一个人在推码。

---

## 十一、2026-07-31 第四轮(本地 Express 靶场 + D 类新缺陷 + 1.1.59)

### 11.1 前提纠正:A 类那六条不是「物理做不了」

前三轮反复写着「Accserver 不通所以做不了」。**这句只对了一半**,本轮实测:

```
D:\pearnly-erp-lab\_mirror_20260727_accserver9EXP_program\   Express 程序本体(ExpressI.exe / ADM32.EXE / DLL / 字体)
D:\pearnly-erp-lab\_mirror_20260727_accserver9EXP_test\      完整账套 145 个 DBF —— 可读可写可删,TEST 靶场的本地副本
D:\pearnly-erp-lab\_mirror_20260726\                            7 套真账只读镜像 389M
```

六条里**五条本地就能做**,真正卡住的只有「桥装第三台常开 Win 机」——那是部署拓扑,不是数据。
**「装不上」和「写得对不对」是两件事,前三轮把它们混成了一件。**

### 11.2 靶场上真做掉的

| 欠账 | 做到什么程度 |
|---|---|
| §3.6 四类新单据 | 收款/付款/手工凭证/库存调整**直接写进靶场账套本体**(不是 copytree 到 tmp),每类覆盖正常写 + 幂等重投 + 读回逐列校验,9 条全绿。CDX 走**真 Harbour runner** —— 这是「Express 查得到这单」唯一验得掉的地方。载荷里每个标识符都从靶场现读,没有一个写死 |
| §3.7 `make_backup=True` | 备份目录真建出来、位置在账套兄弟位、只装本单会改的那几张表且是**写前**原字节;注入读回失败 → 账套逐文件 sha256 回到写前。**反证**:同一处失败改成 `make_backup=False` → 账套真被改脏、半张单留在里面;两侧 `_restore` 同时改空实现 → 3 条回滚用例如期见红 |
| §3.5 桥写路 | 云端派活 → 桥接单 → 真写 → 读回 → 回 ack 端到端跑通;盘上写互斥跨进程真拦得住;超时不吞在途写活;迟到 ack 留痕;四类新单据走 `cloud_jobs` 载荷键白名单一张没死在 `bad_payload` |

靶场跑完**逐文件 sha256 与跑前 165 个全等**,无 `*_pearnly_bak_*` 残留,只读镜像没碰过。

### 11.3 只有真跑端到端才撞得出的一条:最后一跳把业务失败读成了成功

`services/steward/erp_push_tool.py` 拿 `bridge_jobs.status=='done'` 当业务成功。而桥对**传输层**一律 ack `ok=True`(`bridge/cloud_jobs` 文件头写死的口径),业务成败在 `result.ok` 上 —— 于是桥挡下的每一种诚实失败(`DUPLICATE_CONTENT_DIFFERS` / `ACCOUNT_BUSY_LOCKED` / `READBACK_VERIFY_FAILED` / 写一半回滚)**都被报成「推成功了」,而账套里零字节**。

修在 `285c55a3`,3 条用例载荷照抄真跑结果体,反证:退回旧写法前两条当场红。

**这条是本轮最贵的发现,而它躲过了 27 道机械闸和 11683 条单测** —— 因为两侧各自的单测都是绿的,错在接缝的语义理解上。跟第一轮那条「两仓各照各的镜子」是同一类。

### 11.4 D 类(前三轮真浏览器走查揪出、一直没修的)

- **账套硬门的报错一个像素没进用户眼睛**:后端 422 四语文案齐全、前端确实 `showToast`,但 toast 的 computed `visibility` 全程 `hidden` —— 用户看到的是「点了确定什么也没发生」。修法不是把 toast 弄可见就完,而是**把失败原因留在表单上**(会自己消失的冒泡不该承载「名字必须有泰文」这种信息)
- **「后端 500 显示成空态」是系统性的**:按判据扫 `src/home/**` 命中 7 处,4 处真缺陷全修 + 顺带发现第 5 处,错误态的脸收进**一份**共用件,并装了闸挡第四次
- **OCR 把 Python 异常类名印给会计**(四语都印 `Layer1AuthError`)→ 机器码下屏,收进可复制小字
- **`/ai` 客户下拉三态不分**、**390 下 KPI 徽章被搜索框盖住**(zh 因文案短侥幸不撞,只测 zh 会漏)
- **`direct_read` 的 sanity 闸把 VAT 内含小票判成「结构不可能」**:真语料改前回落 **24/56 = 42.9%**,24 条 100% 同源;改后 **0/56**。反证:对被放行的 26 张注入 **104 条真实误读形态**,103 条当场回落、**漏放 0**
- **生产表结构进版本控制 + DDL 覆盖闸**:`users` / `erp_endpoints` / `ocr_history` / `erp_push_logs` 等表的建表 DDL 从没进过迁移史(97 个迁移里一个 `create_table` 都没有),任何空库都建不出来

### 11.5 1.1.59 已发

线上 `latest.json` = 1.1.59,下载读 PE 的 ProductVersion = 1.1.59(61,851,247 字节)。
`.bak` 命名修复第二次真机确认:`.bak-1-1-58` = 61,848,982 字节 = 上一版真发出去的 1.1.58。
**现网仍是 0 台升级**,最高 1.1.56 —— 发版只保证包在服务器上。

### 11.6 本轮新发现、没修的

1. **小助手侧根本没有四类新单据的实现**:`src/companion/` 下 `doc_*` 文件 0 个,`bridge/writepath/` 下 17 个,分发表只在桥里。所以「两侧都要跑」这个前提不成立,不是漏跑
2. **库存调整在「库存流水全由 Pearnly 推」的账套上永远走不到写盘**:靶场 STCRD 活行 `MLOTNUM` 全空,而 `_pick_lot` 硬要求指向真实来源批,Pearnly 的采购/销售写路又都不产批号(批号只能 Express 自己产)。要么放宽到「无批次时允许空 `MLOTNUM`」,要么在云端就把这类账套的入口关掉,**别让会计推完才收到 `TARGET_DOC_NOT_USABLE`**
3. 桥占用时回的「稍后自动重试」是旧队列的口径 —— **桥直写这条路不自动重推**,这句话对会计是错的
4. 桥被 kill 时云端那一拍仍会把活派给已死的桥,**要等租约 300 秒才退回来**;行为符合 at-least-once、不造成多写,但真机上会被当故障报上来
5. **那批靶场用例进不了 CI**(CI 机器没有 D 盘),做成了 clean skip = **本机手工闸不是 CI 红线**。要变常驻闸得有一台常开、挂着靶场副本的自托管 runner
6. 银行对账会话列表与 POS 老板后台异常下钻:**代码改了但没能真浏览器验** —— 前者在当前生产点击路径上摸不到,后者本机 `tenant_modules` 0 行。按硬门口径只算「代码级修好」
7. 「不看 `resp.ok` 就当正常载荷解」那半边**没做成闸**:本仓有两套合法成功契约(HTTP 状态码 vs `{ok,data,error}` 信封),裸判 27 处里 24 处是纯误报。剩 7 处清单写在闸顶注
8. OCR 语料只跑通 56/104(48 张超时,判断是 API 侧限流),省钱数是按 56 张口径算的**上限**
9. legacy 表 **baseline 迁移没补**:prod 与迁移史已漂 71 条,单补 baseline 会让迁移史更像真的却仍跑不通,四步方案与代价写在那笔提交里
10. 照搬闸登记表还剩 **28 条旧账**,多是「稿画了而生产另一套 class」的概念件,要清得先决定那些屏的稿要不要整页重画

### 11.7 明确没做的

- **§3.19(关账期 Express 界面拦不拦)**:要开 `ExpressI.exe` 人工点 + 截图,Zihao 当轮要求推后
- **§3.5 的部署那半**:桥装第三台常开 Win 机、跨网 SMB 写(而非本地盘),要事务所网络拓扑
- **§3.2 装机**:要人去每台会计电脑点托盘
