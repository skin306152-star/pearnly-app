# 📊 STATE · Pearnly 项目状态

<!-- ╔═══════════════════════════════════════════════════════════════╗
     ║  状态卡 · 每窗口收尾【重写这一段·别追加】· 保持 ≤30 行         ║
     ║  数字只信 `python scripts/refactor_progress.py`,本卡是快照     ║
     ║  历史明细 → CLAUDE.md/STATE_ARCHIVE.md(按需查·不必每窗口读)   ║
     ╚═══════════════════════════════════════════════════════════════╝ -->

## 🎯 状态卡（2026-06-05 · **✅ 知识库报告4修复(P1a已补全)+ 出处原文片段高亮上线 `11850608`**）

- **最新一批**(master `9fc3362` · 版本 `11850608` · prod 活)：① 出处**原文片段高亮**(对齐桌面设计预览)② 补全报告 **P1a**。守门全绿 · 全量单测 **2293 OK**。
- **原文片段高亮**:点问答出处卡 → 后端 `GET /api/knowledge/chunks/{id}`(`search.get_chunk_context`·RLS+账套隔离·取命中 chunk + 邻段)→ 前端 `knowledge-sources.ts` 弹窗(`.modal` 非抽屉·守 D1 闸)黄底高亮命中段 + 上下文 +「可核对」脚注。i18n 4 语。**数据现实**:chunk 不存页码/章节(切片时页边界丢)→ 不显「第X页」(设计预览的页码是 mock·要做需改入库管线另议)。
- **P1a 补全**(Codex 实测发现上批只修一半):坏 PDF 在 prod 走"OCR 抽不到文字"分支仍返回 `ERROR_UNSUPPORTED`(显"不支持的文件类型")。改:支持的类型但 OCR 抽空 → `ERROR_PROCESSING`(显"文件无法解析,可能已损坏或加密")。
- **Codex prod 实测**(报告在桌面 `知识库修复验证报告_2026-06-05.md`):上批 11850607 — P0 off-topic 不扣✅/正常问答扣฿0.50✅/低余额提示+不扣✅/上传即显 Uploading✅/坏PDF 不500✅;**唯一 FAIL = P1a 错误码**(本批 11850608 已修)。
- **⚠️ 交接补漏**:知识库 UI 设计预览在 **`桌面\Pearnly知识库UI预览\index.html`**(原文高亮/费用弹窗/猫等全套设计稿)。之前 STATE 没链它,导致本窗口一度不知有设计稿。**改 UI 先翻这个**。见 [[kb-ui-design-preview-desktop]]。
- **未提交残留**:无(全 push)。**未闭环**:页码/章节标(需入库管线存 page→chunk·设计预览有但数据没有);问答偶发 Gemini 503(瞬时·已降级不扣)。
- **下一步(下个窗口)**:**请 Codex 复验 11850608 两点** ① 坏PDF 文案变「文件无法解析…」② 出处卡点开显原文+高亮。都过即知识库整条闭环。全貌见 [[kb-rag-sandbox-project]] / [[knowledge-frontend-ocr-billing-shipped]]。

<!-- ═══════════════ 历史明细已移至 CLAUDE.md/STATE_ARCHIVE.md ═══════════════ -->
<!-- 新窗口:读上面状态卡 + 跑 scripts/refactor_progress.py 就能开工 -->
