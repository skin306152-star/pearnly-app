# 全局诊断 · 按文档交接做的「已完成 / 待完成 / 不做」分类(2026-06-24)

> 方法:读 STATE 状态卡 + 5 份 HANDOFF + 全量 ~90 份规划文档(express-push 30 / line-platform 13 / sales-module 18 / refactor+cleanup+ocr-migration+audits / mrerp 集成 / 根级旧清单),四路并行扫读,再把**文档里写的状态**与**最新权威状态(STATE + git + CI 实测)**对账,标出「文档说待办其实已完成」「文档说完成其实没真完成」「不做的原因」。
> 关键校验已用 git/CI 实跑过(见文末「对账证据」),不是只信文档手写。

---

## ★★ 二次核对修正(对照实际代码/git/dist · 2026-06-24 第二轮)

> 第一版是"读文档 + 5 处 git 抽查"。第二轮**逐条去翻实际代码**,发现一个系统性问题:**规划文档/交接是点位快照,后续窗口把它们写的"backlog/待办/没起"做掉了但旧文档没更新**。以下 6 条是实测纠错,**比下文正文更新**(正文保留原貌+已就地标注):

| # | 我原来写 | 实际代码真相 | 证据 |
|---|---|---|---|
| 1 | **Express 防呆闸**(币种/折让/押金/日期/税号)= 待办 backlog #1(STATE 也这么写) | ✅ **已建并接线**(`doc_sanity.py` + `preflight.py` 调 `check_document`)。commit `adf53346` 在 STATE 收尾 commit `9769a4a7` **之后**才落 → 连 STATE 都过时了 | `doc_sanity.py` 93行实现 currency_not_thb/credit_note/deposit_receipt/date_future/tax_id_invalid;preflight L21 import、L231 调用 |
| 2 | **LINE P2E AI Gateway** = 完全没起 | ✅ **已上线**(P2E-1 空接 + P2E-2 文本/分类接线) | `services/ai_gateway/`(router/tasks/providers/costing)存在·commit `f7a1937` |
| 3 | **销项前端 PO-10(8屏)** = 全项目最大未建件·卡设计稿 | ✅ **已建并打包上线** | 14+ `src/home/sales-*.ts`(sales-wizard 455行/workbench/products/detail/report/account/settings)·`dist/main.js` 含 sales-wizard·po-ui S3–S9 commits·module-nav 接「销售开票组」 |
| 4 | **LINE 锚定 Slice3(强锚定)** = ❌ 未做 | ✅ **已上线**(doc06 写❌是 06-19 当时·Slice3 06-20 才上) | `services/expense/line_anchored.py` 存在·记忆 `e47870eb` |
| 5 | **LINE Fuzz 测试** = 没跑·只给语料 | ⚠️ **runner 已建**(`scripts/_line_fuzz_run.py`)·测试设施实质在(replay/Tier A 归零 per 记忆)。"没跑"太重·但特定文件 `test_line_fuzz_deterministic.py` 确不在 | `scripts/_line_fuzz_run.py` 存在·`tests/fuzz/line_fuzz_corpus.py` 存在 |
| 6 | **doc29 自动建档/autonomy/preflight** = design only·pending | ✅ **建了相当一部分**(autonomy 三档+preflight 八体检项落地) | `autonomy.py` 59行(manual/auto/hold)·`preflight.py` 294行 |

**经核对仍成立的(没冤枉)**:整顿所有源文件≤500(`check_file_size` PASS·home.js/home.css→0·db.py 345·app.py 419)、目录重组、CI 闸 lint-size/lint-ui 硬门+lint-routes 软门、Express 5 个关键 commit 全在 master、`push_exception_classify.py`(106行)拆分到位。

**修正后的"真·剩余待办"收敛为**:
- Express:防呆闸**真机验证**(代码已写·还没用陷阱票实跑过)+ DATAT 清数据 + companion 提速真机验收。**不再是"防呆闸没做"**。
- LINE:P1E-2(改错/澄清统一·确在施工)+ 锚定 Slice4(无引用猜目标·Slice3 已上)+ 回归 fixture + fuzz **实跑出报告**(runner 已有)。P2E-3(OCR L2/L3 接 gateway)。
- 销项:不是"没建前端"·而是 doc16 后端剩余项(§C含税价/§E纸/§F审批/§H模板/§L预扣税)+ PO-7 LINE发送 + 把 `426caee` 批次推上线/E2E。
- 整顿:C5 TS 迁移、性能/数据/合规阶段、lint-routes 切硬门——这些核对无误。

---

## 〇、一句话总览

整个项目分 **5 条线**:① Express 推送(当前主战场)② LINE 记账助手(并行)③ 整顿/重构(核心已收官)④ 销项开票模块(后端大半成、前端没做)⑤ mrerp/DMS 集成(已上线)。
**最值得你知道的事**:很多文档/交接里写的「未完成」「推送全失败」是**点位快照**,后续窗口已经修掉了。下面每条都标了"文档说 vs 现在真相"。

---

## 一、Express 推送(最活跃 · 文档最多 · 矛盾最集中)

### ✅ 已完成且真机验证(现在真能用)
- **全链路打通**:上传→OCR→判方向→入队→companion 拉取→直写 DATAT→报表可见。**2026-06-24 用 49 张真实发票真机端到端测过**:46 推送 = 41 成功 + 5 正确转人工。核心管道(方向/金额/去重/科目/建主档)**稳**。
- DBF 直写(采购 + 销项)、PACK 必需性已证(doc13)、companion 双 exe 打包 + 「下载小助手」、配对窗、自动更新(latest.json)、账套白名单放开(代码层早不锁 DATAT)、科目按账套 ISINFO 解析、推送状态诚实化、绑主体面板、自建供应商/客户去重守卫。companion 已发到 **1.1.11**。

> **⚠️ 文档矛盾(重要)**:`doc26-pm-handoff-push-blocked` 写「Owner 真测推送**全失败**·核心happy-path从没端到端验过」,这是 **2026-06-22 的快照**。`doc27/28/29` 是当时开的修复/设计单。**现状(2026-06-24 STATE):三个根因全修完上线**——① 主体绑定 NULL(persist clobber)已修+绑主体面板;② 科目映射缺失已补(科目来源诚实化 + 待补科目卡 UI);③ companion 账套选错已通过配对选真账套解决。**doc26 的危机已解除**,别再当成 open issue。
> 同理 express 扫读 agent 说「direction 修复在分支未 push」——**那是 doc27 时点,现已 push 并真机验过**。

### ⏳ 待完成(真待办)
1. **防呆闸**(🔧 **已纠正·见顶部★#1:代码 `doc_sanity.py` 已建并接进 preflight·真待办只剩"用陷阱票真机验证"**)·本轮 49 张测试**如实暴露**的缺口·doc28 §8 早列为 v1 之后):
   - **币种检查(最优先)**:美元票 `INV-US-0091` 当泰铢记了(金额性质错·最严重)
   - **折让/退货单(CN)**:`CN6812-0005` 当正向采购推了(应走负向冲销)
   - **押金收据**:`DEP6812-01` 当普通费用推了(应转人工)
   - **日期合理性**:未来日期/倒签照推(应标可疑)
   - (轻)税号非 13 位:照推(应标无效)
   > 性质:不是"坏了",是 preflight 目前只查 feature/account_set/direction/金额/科目,**没做币种/日期/押金/折让**。做法多为「转人工」即可。
2. **DATAT 本轮测试数据清理**:41 张 success 留了数据 + S4a 重测残留。等 Owner 拍哪个是干净基线。
3. **companion 提速真机验收**:每张票写前 copytree 整账套 255 文件→已改选择性备份(~8×·1.1.9+)。待 Owner 重启小助手验"秒级"。
4. **GL 期初偶发截断**:已加首笔运行余额反推安全网(prod `3d7902bc`)。盲区:走 Gemini 兜底(行无运行余额)时救不了→留观察。

### 🚫 不做 / 暂缓(有明确原因)
- **S7 自建账套**:🚫 **Owner 拍板暂缓存档**。原因=全系统**风险最高的写**·Express DBF 层"怎么建一套账"**未知**·规矩是"先逆向勘探证明能 100% 复刻才建,否则改走 Express 界面"。需 Owner 真机配合抓数据才能起。
- **RPA 模拟录入(第二录入方式)**:🚫 **2026-06-22 Owner 拍板泊住不再追**。原因=两道结构墙(采购 RR 货品网格模型不匹配→只能销项;销项服务项 qty/price 单元是 Codejock 自绘→读不出+光标定不准→盲键不可靠)。已作"安全降级·默认关"的泊住功能合入,等客户坚持要 GUI 录入+提供操作员录一次键位才复活。**DBF 已覆盖采购+销项且稳定上线,RPA 无产品增量。**
- **T3 代码签名(SmartScreen)**:🚫 **Owner 冻结**。原因=要 Owner 买 OV/EV 证书·量产/被 SmartScreen 拦再解冻。
- **T7 PyArmor 加固**:🚫 没上(release 行明确 no PyArmor)·排在功能签收后的"最后一棒",那一棒没走完。
- **科目全自动新建 / 预扣税 WHT / 存货vs非存货 / 改错冲销-重过账**:🚫 v2·现保守版(落已有/人确认)。
- **defer② acctfix 句柄改名**:🚫 低优·等第三个消费者出现再做(现 2 个:科目卡+绑主体卡·机制通用行为正确)。
- **删 express_pw 字段**:🚫 已否决(doc13 证 PACK 必需→字段保留改 DPAPI 加密)。
- **导出 Express 数据手动导 MR.ERP**:🚫 只在讨论·未立单。

### ⏳ 待完成但有"包装最后一棒"没收口
- **T9 PACK 前整套备份 / T6 继续瘦身**:⏳ 排在 companion 最后一棒(和 T7/图标一起),那个窗口在功能签收前没做完→**这棒悬着**。

---

## 二、LINE 记账助手(并行车道)

### ✅ 已完成且上线
- 记账车道是**参考实现**(已反超 Paypers):四态卡/在聊天里改额/撤销/批量撤销。
- 锚定框架 Slice 1/2a/2b(死单不改/恢复/草稿软删恢复)、强锚定+可引用卡片契约、语言中枢(自动跟随+明说锁定)、登录自动绑+用LINE连接+解绑闭环、设计图卡全套(imagemap+Flex)、Rich Menu、凭证 PDF(整月打包)、确定性分类闭环、反伤账加固(金额接地+噪声数字/笑声/型号/外币/押金全封)。

### ⏳ 待完成(车道还欠的)
1. **P1E-2 改错/澄清/动作统一**:在施工中·canon §10 排第 1·最大活口。
2. **锚定 Slice 3 & 4**:Slice3(引用一张卡=只围绕它·不另记一笔——现在"引用卡+'咖啡65'"会另记)·Slice4(无引用按置信度猜目标+列候选)。Phase A 没收口。
3. **回归测试 fixture**(Amazon/CP/Seafood/Little Betong 真票)——反复列·没做。
4. **Fuzz 对抗测试执行**:语料(~19800 变体)+脚本已给·但 Tier A 确定性回归 + Tier B 真大脑批跑 + ≥2000 例违规报告**都没跑**。
5. **场景矩阵 04 残留**:⚠️ D6(多笔补价丢日期/卖家)、E1(卡片"已记账"和"请核对"打架文案)·❓ A6/A7/A8/B8/C7 待验·批量撤销待真机验·pip-audit 14 CVE(注:pip-audit 已另窗清零·见对账)。
6. **P2E AI Gateway**:🔧 **已纠正·见顶部★#2:P2E-1/P2E-2 已上线**(`services/ai_gateway/`·`f7a1937`)·仅 P2E-3(OCR L2/L3 接 gateway)pending。
7. **设计图卡**:A2/A9/A10/A11/A13 的**泰文文案还欠 Zihao 提供**。
8. **菜单/交互 doc01**:还是"方案稿待 Zihao 确认"·没施工。
9. **Roadmap Phase B/C/D**:没起·B 首选=学习按钮 UI·#11月底催缺票/#12交接包/#4群组/#5@提及/#6自适应菜单全❌·D 闸住(单用户车道做好再开)。

### 🚫 不做(明确红线·有原因)
- intake_items/采购收件箱(已下线)、图片优先 OCR 重写(确定性已拿到收益)、Drive/Sheets 导出(未来模块·OAuth 范围)、全站 AI 助手+Rich Menu 全遥控(未来平台层)、聊天里报税/银行对账确认/反向过账/改公司税号/AI 批量删票/全推送审批(永久红线)。
- 拼写数字`ห้าสิบ`/型号数字`M150`/倒签语义:🚫 **下放给大脑(BRAIN_DEFER)**·确定性层证实不安全(泰语分词歧义)·非 bug。

> **⚠️ 文档缺口**:LINE 这条线"2026-06-20 暂停·未来再开"的说法,在 line-platform 文档里**找不到记录**(那句"未来再开"在记忆/STATE 里、指的是不同上下文)。最近 LINE 工作落到 **2026-06-21**(设计卡+语言中枢+解绑)·测试研究文档到 06-22。**实际 master 上仍有别窗口的 LINE 提交**(STATE:`d8c7cd8f 含别窗口 LINE 工作`)→ LINE 可能仍在并行推进,不是停了。建议你确认这条线现在到底"停"还是"在做"。

---

## 三、整顿 / 重构(核心已收官)

### ✅ 已完成且收官
- **所有源文件 ≤500 行达成**:home.js 33k→~200(入口)、app.py→419、db.py→345(10663→332 facade)、home.css 16124→0(全拆)、home.html→314。`check_file_size` FAIL 0。
- 目录重组(122 root .py→routes/+core/+services/)、Vite 打包成 minified bundle、A 工具链 8/10(Vite/Alembic基线/CI lint+security+coverage+deps)、B 后端骨架(B1/B2/B4/B5/B6/B7/B9/B10)、C 前端 C1/C2/C3、G 文档 7/7(11 ADR)、I7 目录重组、I1 静默错误归零、OCR 三层架构(后又 WA_OCR 再拆<500)、erp 开箱即用 P0-P3、通用导入器 ADR-006、银行对账闭环、D2/D5 测试。

> **⚠️ 文档过时(已对账纠正)**:refactor 扫读 agent 说「lint-size/lint-ui 还在 warning 模式」——**错·已切硬门**。CI 实测:`lint-size` FAIL 模式(2026-06-03 切)、`lint-ui` FAIL 模式(2026-06-05 切)、**唯 `lint-routes` 还是 warning(continue-on-error·唯一仍软的闸)**。
> 同理 `REFACTOR_MASTER_PLAN.md` 自己的阶段表把 A3/A4/B1/B2/C1 标 🟡 在做——**表本身 stale**·实际这些已完成(信进度看板+卫星文档,别信阶段表)。

### ⏳ 待完成(真剩的整顿)
- **C5 TypeScript 迁移 59/151**(在做)·C4/C6/C7/C8/C9 没起(C9 闸住·撞 home.js 底线+动 VAT 金额状态)。
- **C3 app-shell 抽取**(顶栏+侧栏外壳→JS·最后最高危一块·有作战手册没执行)。
- D1(E2E 已长到 10)、D3 死代码闸(等 Zihao 死代码清单)、D4 70% 覆盖率(现 baseline ~22.5%·棘轮 21)。
- **B8 RLS、E 性能 0/6、F 数据 0/3、H 合规 0/6、I2/I3/I4/I5/I6** 都没起。
- **lint-routes 切硬门**(唯一仍软的闸)。

### 🚫 不做 / 闸住(有原因)
- **A3 闲置笔记本 staging(Wave3)**:🚫 Zihao-gated·要装 Docker/动硬件·从没执行·不阻塞主线。
- **A4 Doppler 生产切换**:🚫 只做了 step1/4(导入 39 secrets)·其余 Zihao-gated(动红线)。
- **全部 Workspace 重设计文档**(workspace-rollout/b2-work-mode/b3-employee-permission/b6-backfill):🚫 新功能·被"0 新功能"冻结·"等 Zihao 解禁"。
- **batch-center B5 / seller-routing Phase2 / erp B2 种子提示**:🚫 deferred。
- **OCR Phase 2/3 用户纠错学习 + 按供应商/按银行模板记忆**:🚫 "全部不做"(Zihao:不要半成品壳)。
- **OCR Phase 4**(自定义公式/100%自动/自训模型):🚫 **永久锁死**。
- **B3 19 张存量 ensure_* 表回溯迁移**:🚫 零价值跳过(只做前向 Alembic)。

### ❓ 已陈旧 / 历史(别再当任务)
- 整个 `docs/cleanup/`(2026-05-18·Round1/2 已完成或被整顿 B1/I1/I2 吸收)
- 整个 `docs/ocr-migration/`(2026-05-18·设计已建成·又被 WA_OCR 重拆)
- `exception-rules-audit`、`2026-05-21 只读体检报告`、`STATE_2026_05_19`(自标已归档)、`code-cleanup-audit`、`deploy-sync-analysis`、`startup-check-report`(都 pre-freeze·老路径)
- `WA_OCR_CORE_REALREFACTOR_PLAN`(被 SPLIT 计划完成所取代)

---

## 四、销项开票模块(后端大半成 · 前端是最大未建)

> 注:此模块是在"0 新功能"冻结期里做的**授权例外**,不是被冻结杀掉。

### ✅ 已完成(后端 Phase1 核心·已 prod + 真账号 E2E)
- **PO-1~PO-6**:schema、产品 CRUD+Excel 导入、开票/作废/编号/不可变、贷项/借项通知单、合规 PDF。
- 09-open-decisions **全部已拍板**(POS 分离/菜单图选/合规分层/产品来源/发送渠道/LINE点单 Phase2)·**零遗留开放决策**。
- UI 硬闸 D1/D2(原 doc11 草案·已落 pre-push)、L5 发送(邮件+LINE 分享链接)已上线。

### ⏳ 待完成(真正"规划了没建")
- ~~**整个前端(PO-10·8 个屏)**:🔴 全项目最大未建件·卡在设计稿~~ → 🔧 **已纠正·见顶部★#3:前端已建并打包上线**(14+ `sales-*.ts`·dist 含·po-ui S3–S9)。真待办= PO-11 i18n 收尾 + PO-7 LINE 发送(高敏) + 把已 commit 未 push 的 `426caee` 批次推上线/E2E。
- **doc16 后端剩余**:§C 含税价、§E 热敏/两联纸、§F 审批、§H 模板管线、§L1 PromptPay、§L2 预扣税分档、§L3 报价→发票转换;以及把已 commit 未 push 的 `426caee` 批次推上线+E2E。
- **买方信息 doc15 前端块**:后端已发·前端草稿。

### 🚫 不做 / 暂缓
- **e-Tax 直连申报**:🚫 deferred(需证书·"可行才做")。
- **POS 项目**(扫码/QR/拍照选品):🚫 独立未来项目·不在本模块。
- **Phase2 库存 + LINE 点单 + 应收**:🚫 deferred。
- **LINE 一句话开票 doc14**:🚫 自标 spec-only·依赖未建的凭证中心·冻结期不进主仓。
- **私人 Gmail 发送 / LINE 官方账号推送**:🚫 砍。

---

## 五、mrerp / DMS 集成(已上线)

### ✅ 已完成
- **mrerp 财务发票推送**:适配器 + E2E 绿 + 真单号(`SI690519-001`)。
- **mrerp 客户 + 产品主数据自动建档**(copy-from-seed)。
- **DMS 身份证→预订自动流**:已上线·迭代到 6 月中。

### ⏳ 待完成(适配器缺口)
- 发票修改、多发票预览、并发推送安全、非 sales_credit 业务类型。

### ❓ 文档头过时(代码已落地·与文档说法相反)
- `mrerp-master-data-sync-design.md` 和 `mrerp-product-form-fields.md` 头部写"design only / 还没代码"——**已 shipped(§4.6)**·头部该改。
- DMS 一步式 endpoint 已被两步式(recognize→push)取代。

### 🚫 不做
- BUTTON_TAKEOVER §2.5-② tab-active 蓝:🚫 有意 closed-won't-do。
- `knowledge_rules_unification`:⏳ 设计已拍板·在 `feat/knowledge-backend` 分支 flag 闸住·切换(开 flag+删旧引擎+客户规则 UI)未确认上线·冻结敏感。

---

## 六、对账证据(我实跑核实的,不是只信文档)

| 文档/交接说 | 实测真相 | 证据 |
|---|---|---|
| HANDOFF-06-23 任务2:oauth_routes 539 行待拆(登录主路径) | ✅ **已拆完** | `routes/oauth_routes.py`=213 + `oauth_line_routes.py`=322 |
| HANDOFF-06-23 任务3:erp_push 507 行待拆(推送主路径) | ✅ **已拆完** | `erp_push.py`=410 + `erp_push_adapters.py`=115 |
| refactor 文档:lint-size/lint-ui 仍 warning 模式 | ❌ **已切硬门** | ci.yml:lint-size/lint-ui = FAIL mode·仅 lint-routes `continue-on-error:true` |
| doc26:Express 推送全失败·happy-path 没验过 | ✅ **三根因已修·49 张真机端到端通** | STATE 2026-06-24·prod `62626323`/`8c79d556`/`33582a24` |
| doc27:direction 修复在分支未 push | ✅ **已 push 上线** | STATE defer① bind_fix prod `8c79d556` |

---

## 七、给你的三件需要拍板的事

1. **Express 防呆闸优先级**:币种>折让>押金>日期——先做哪个?(都属"转人工"即可·doc28 §8)
2. **LINE 车道现在到底停没停**:文档说停在 06-21·但 master 有别窗口 LINE 提交。要不要继续推 P1E-2 / 锚定 Slice3-4 / 跑 fuzz 测试?
3. **销项前端(PO-10·8 屏)**:全项目最大未建件·后端就绪·要不要立项做?(冻结期授权例外)

> 还有两件低风险维护:① 把 mrerp 两个文档过时头部("还没代码")改对;② lint-routes 切硬门(唯一仍软的闸)。
