# Express Push 真机实测受阻 · PM 收尾交接(2026-06-22)

> Owner 真机实测:上传发票 → **推送全失败**。PM(本窗口)收尾交接。
> **诚实定性**:外围(账套列表/i18n/RPA/测试票)推了一堆,但**核心 happy-path 从没在活产品上用真发票端到端验过** → Owner 一测,第一个集成点(发票绑主体)就塌。**PM 失职:该先证核心推送链,再让 Owner 测。**

## 一、Owner 实测暴露的 3 个真问题(下一窗口优先修)

诊断证据(真机):`11_direction_own_as_buyer.pdf` / `RR6812-0220` → `workspace_client_id = None`、卖方识别对(สยามวัสดุก่อสร้าง)、但 **direction_unknown**(判不出买/卖)。

| # | 问题 | 根因 | 修向 |
|---|---|---|---|
| **1** | **上传的发票没绑到工作区主体**(`workspace_client_id=None`)→ 方向判不出 → 不推送 | 上传/OCR 入库**没绑当前 active workspace_client**;方向判定靠"票上某方税号==自家税号",没绑就没有"自家" | OCR 入库时**绑定当前 active 主体**;且该主体须有 own tax(测试=`0735527000289`/มานะชัยบริการ)。**这是推送全失败的头号原因。** |
| **2** | **会计科目映射未配 + UI 甩给用户填** | 收入/采购/进项税 GL 科目无默认值,界面要求用户填 | 给**合理默认值**(后台一次性设),正常产品不该甩给用户。无映射 → 推送拼不出分录 |
| **3** | **小助手选的账套是 ASIA-SPORT(PDATAT)不是 DATAT** | 默认/记住选择逻辑选了 row1 | 选 DATAT(row2·มานะชัยบริการ);查默认选择/持久化逻辑 |

> 三件全满足才推得成。当前 3 件都没到位 → **这是"功能没做完",不是"配置没配对",Owner 点几下解决不了。**

## 二、已交付 + 已上线(别丢·可续)

- **直录(DBF)主路**:采购+销项写盘/PACK/三重闸/凭证(DPAPI 加密·机器绑定)·真机 DATAT ALL_MATCH。
- **配对修复批**(prod `401e83f1`·`?v=11850929`·新瘦身安装包在位):账套读 `secure/SCCOMP.DBF` 登记表(非删除行=与 Express 登录 grid 一致·唯一键 PATH)、配对窗加载标签、网页 step3 镜像真选择(去 DATAT 硬默认)、TH/ZH i18n(默认按系统语言+切换+记住)、瘦身(setup 124→72MB·无 PyArmor)、图标。
- **RPA**:已泊(`feature/express-rpa`·待 ff-merge 进 companion master·默认不启·安全转人工)。两次结构墙(货品 grid / Codejock 单价格)→ 收手,DBF 是主路。详见 `21-`。
- **测试票语料**:彼得窗口生成(`25-` 规格·自家侧税号须 `0735527000289`)。

## 三、下一窗口的第一优先(别再先做外围)

**证通核心 happy-path 端到端(活产品·真发票)**,顺序:
1. 修问题 1:上传发票**绑定 active 主体**(workspace_client_id)+ 主体设 own tax `0735527000289`。
2. 修问题 2:科目映射**给默认值**(收入/采购/进项税),不甩用户。
3. 操作/修问题 3:小助手切 DATAT。
4. **端到端实证**:Pearnly 上传一张采购票(买方=มานะชัยบริการ)→ OCR → 判采购 → 入队 → 小助手 lease → 写 DATAT → ack → Express 报表可见 → 还原。**这条不通,绝不再说"可以测"。**

## 四、PM 教训(进记忆)
- **核心 happy-path 端到端没在活产品证通前,绝不让 Owner 测。** 外围打磨(账套列表/i18n/RPA/测试票)再多,核心一塌全白费。先证"一张真票从上传到进 Express"再做其它。
- 多窗口并行时 PM 易被各窗口"绿了"带着走外围,**忽略了把整条用户主路串起来证一遍**——这是本次最大失职。

## 五、git 收尾
- companion master:`71dfbad`+`0123fd9`(配对批)·`d484b5b`(T6/T7)。RPA `feature/express-rpa` 待 `git merge --ff-only`(直录窗口提交脏文件 `scripts/_t5_pairing_shot.py` 后)。
- pearnly-app:`401e83f1` 已 push 部署。
