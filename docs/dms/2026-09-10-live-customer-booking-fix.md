# DMS 客户建档后订车核验修复

## 事件与核查

2026-09-10 泰国时间 13:25:20，生产推送台账记录客户创建成功；13:32:59 订车前客户搜索为空，抛出 ERR_DMS_CUSTOMER_SAVE，尚未调用订车写入。Web/Worker 均为 ffc4bf091acd-s2。

以原订车账号只读回查，同一客户号能够直接读取，完整身份证匹配，订车必填主档字段齐全，稍后的搜索也恢复命中。未重复建档、未重试订车、未修改真实业务数据。搜索为空的具体外部原因未证实，不将历史列表延迟案例当成本次已证实根因。

## 修复与范围检查

- 订车前按已选客户号通过当前订车账号实时读取详情，核对完整身份证；不再依赖搜索列表。没有切换到管理员账号绕过客户可见权限。
- 空身份、身份不一致、详情缺失或必填主档不全均阻断订车，使用独立 ERR_DMS_CUSTOMER_LOOKUP；LINE 与推送记录文案明确订车尚未提交，避免误导为客户建档失败。
- 该建单前错误保留草稿并发新的一次性重试按钮；已有 booking_id 或写入阶段错误不新增重试资格。每次重试重新核对实时主档与客户详情。
- 扩大检查覆盖 OCR 查重、建档、覆盖更新、建档后复核、订车确认。OCR 查重已有“台账候选客户号→直读→完整身份证核验”，此次补齐订车缺口。更新已有详情回读；创建已有写后搜索复核及重复拒绝后的有限复搜。
- 创建后的搜索仍可能暂时为空并返回失败，因缺少已证实的新客户号，本次不凭 HTTP 200 判成功，不自动重复创建，不扩大改写外部建档协议。此边界需要独立样本才能继续安全改进。

## 验证与发布

定向 66 项测试通过，覆盖客户详情、身份错误与缺失、实时字段、建单前阻断、草稿重试、nonce、防重复建单、建档/更新及管理员凭据边界；格式、ruff、文件大小检查通过。完整 pre-push 1168 模块/6 片及全部机械闸通过。

无 SQL/schema/前端静态资产改动。真实客户只读回查已通过，用户重试建单与手机验收尚未完成。

## 发布结果

源码 `db98e6f5ef1f2b8e0139f6bebb72b977363deef0`，镜像摘要 `sha256:65229f87e5e2c22c36b3e1a8f2e4c558167cc2295a55088aa57bcade82d5136e`。Web/Worker 各 100% `db98e6f5ef1f-s2`，原发布脚本 exit 0，候选及正式版本/健康/就绪/完整安装包校验通过，正式域名 readiness 通过。

首次 GitHub CD 34447973781 在 schema 被阻断：此次诊断使用的会话级只读设置被事务池保留。已 RESET 会话状态，确认三个新连接为读写，改用事务级只读；07:10:05 UTC 后台恢复调度已成功。没有改变数据库或角色级配置。之后复用同一镜像执行原发布脚本全流程，schema execution `pearnly-schema-wj7kd` 成功，不重建、不跳检查。首次 GitHub 状态仍为失败，恢复发布证据见部署账本。

原用户草稿仍在，旧确认 nonce 已消耗，需重发确认卡才能继续原草稿；未向真实用户发送消息，未自动重提订车，未声称用户验收通过。
