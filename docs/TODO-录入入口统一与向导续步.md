# 交接待办 · 录入入口统一 + 向导续步(Zihao 2026-06-30 提出,当场暂缓)

> 状态:**未开始 · Zihao-gated**。本批因斜杠同步问题占用过久 + Zihao 要求谨慎,故新任务撤下、记此待办。
> 做之前先读本文件 + 真站点对照截图,别凭想象改。

## 任务 ① 录入向导"续步"(4 步状态记忆)
**诉求原话**:"把 1234 做成记忆,用户在某一步切换到别的地方,重新点录入工作台,还是上传未完成的那一步。"

- "1234" = 录入工作台的 4 步(① 上传文件 ② 整理内容 ③ 结果确认 ④ 导出/推送)。
- **现状**:切走再回来,从第 1 步重来,丢失进度。
- **要做**:记住用户当前停在第几步(及该步的上下文),重新进入"录入工作台"时回到那一步而非从头。
- 待定的设计点(做前与 Zihao 确认):
  - 记忆范围:仅"当前步号",还是连同已上传文件 / 已识别结果一起恢复?
  - 存储:前端 sessionStorage/localStorage 即可,还是要后端持久(跨设备)?
  - 与"草稿态"的关系:第4步未完成的草稿已在库(staged=TRUE),续步可借它恢复 results。
  - 失效:多久 / 换文件 / 点"重新上传"时清空。

## 任务 ② 两个录入入口布局统一
**诉求原话**:"注意这两个入口,全部做成一样。"(截图圈了"发票/收据录入" + "身份证 → DMS 客户")

- 两个任务卡进入后的页面布局做成一致:**4 步进度条 + 右侧"当前流程 / 设计原则"两块说明面板 + 上传区**。
- 对照:身份证入口已有右侧面板;发票入口的右侧面板/排版需对齐成同一套(或反之,以 Zihao 指定的为准)。
- 做前确认:以哪个入口为基准模板?两者步骤文案不同(发票=上传/整理/结果确认/导出推送;身份证=上传证件/客户匹配/资料确认/推送 DMS),统一的是**布局骨架**不是文案。

## 任务 ③ 对账中心流程展示统一
**诉求原话**:"还有对账。"(对账中心也做成一样)

- 对账中心(银行对账 / 销项税报告核查 / 收入对账)顶部已有 4 步流程条(上传文件/导入前检查/执行对账/处理差异)+ 右侧说明。
- 要做:与录入工作台的视觉/结构语言对齐成同一套设计件(进度条样式、右侧面板样式、间距)。
- 注意:对账有 3 个子 tab(银行/销项税/收入),统一时别破坏各 tab 的差异内容。

## 验收标准(三件共通)
- 真浏览器抓 isVisible + getComputedStyle + 截图比对,grep 类名不算数。
- 四态 UI / 设计语言一致 / 去 AI 味,按大厂标准。
- 改 src 必 `npm run build` + 提交 `static/dist`;改 home.* 走 CRLF 安全写;bump `?v=`。
