我需要你帮我梳理 Pearnly 产品当前的 OCR 系统全貌。这一轮你只做调研和文档整理,严禁修改任何代码。

请按以下顺序完成:

## 第一步:扫描代码,找出所有 OCR 相关模块
在整个项目里搜索关键词,定位所有 OCR 相关代码:
- "ocr", "vision", "gemini", "extract", "invoice", "receipt", "document"
- 任何调用外部 OCR/AI API 的地方(import google, openai, anthropic 等)
- 任何处理图像/PDF 的入口(upload, file handler 等)

列出所有相关文件的路径,标注每个文件的作用一句话。

## 第二步:梳理引擎清单
列出当前代码里实际调用的所有 OCR/AI 引擎,包括但不限于:
- 模型/服务名(如 gemini-2.5-flash)
- 调用方式(SDK 还是 HTTP)
- API key 来源(env 变量名)
- 在代码哪些地方被调用
- 每个引擎承担的具体职责

## 第三步:画出业务流程
从"用户上传文件"到"输出结构化数据"的完整链路:
- 入口在哪(API 路由/前端上传组件)
- 文件经过哪些处理步骤(预处理、OCR、字段提取、校验、落库)
- 每一步用了什么引擎或工具
- 错误处理和兜底逻辑
- 数据库里最终存的是什么结构

用 Markdown 流程图或文字描述都行,要让我能一眼看懂。

## 第四步:识别痛点
基于代码,告诉我现在的方案有什么明显问题:
- 成本高在哪(哪个引擎/调用最贵)
- 准确率风险在哪(哪些场景容易出错)
- 速度瓶颈在哪
- 没有兜底或校验的环节

## 输出格式
把上面所有内容整理成一个文档,保存为 `docs/ocr-current-architecture.md`,内容要详细完整。

完成后告诉我文档路径,等我审阅后再讨论下一步。**这一轮绝对不要动任何业务代码。**